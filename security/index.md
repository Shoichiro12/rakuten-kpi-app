# セキュリティチェック索引

このリポジトリのセキュリティ関連チェック・監査の実施記録と、指摘事項の解決状況を管理する台帳。
CLAUDE.md 申し送り表と同じ考え方（決定事項とその実装状態を1箇所にまとめ、後のセッションで
同じ調査を繰り返さない）。

## ⚠️ このファイルは重複だった可能性がある（2026-08-18 判明）

このファイル（`security/index.md`）を2026-08-18に新規作成した直後、**同じ名前・同じ役割の
マスター文書がリポジトリ外（プロジェクトdocs側）に既に存在する**ことが判明した。プロジェクト
docs側の記述によると、そちらが正であり、このリポジトリ側のファイルは「Claude Codeがプロジェクト
docs側の存在を知らずに作成したもの」とされている。プロジェクトdocs側は「次回チェック時に本ファイル
（プロジェクトdocs側index.md）の内容へ同期するか削除するかを判断する」としている。

**このセッションではその判断を行っていない**（次回の定期セキュリティチェック側の判断事項と
理解したため）。react-router移行の完了報告のみ、指示書の指定どおりこのファイルにも反映した。
このファイルを次回チェック時に同期／削除する場合は、実施記録・未解決リストの最新状態（下記）を
プロジェクトdocs側へ引き継ぐこと。

## ⚠️ 既知の欠落（2026-08-18 発見）

`security/security_check_2026-08-17.md`（2026-08-17付の定期セキュリティチェック。この
`index.md` が2026-08-18の作業指示書から参照されていた原本）が、本リポジトリ・全ブランチ・
全履歴のどこにも存在しない（`git log --all -- 'security/*'` で0件、`security/` ディレクトリ
自体が今回新規作成）。

このディレクトリ自体も本セッションで初めて作った。つまり2026-08-17時点のチェックは実施は
されたが、その成果物（チェック結果ドキュメント）がコミットされないまま失われたと考えられる。
CLAUDE.md 申し送り表のルール4「調査・監査・設計の成果物は、それを使う実装より先にコミット
する」が守られなかった実例が、皮肉にもこの `security/` ディレクトリ自体で再発した形になる。

2026-08-18の作業指示書に書かれていた指摘3件（npm audit 5件・CSP欠如・consulting.py文字数
上限欠如）は実際にコードと一致していたため、チェック自体は実在した内容だと判断できる。
今後 `security_check_2026-08-17.md` の実体が別の場所（ローカルの別ディレクトリ、チャット
ログ等）から見つかった場合は、このディレクトリに追加すること。

## 未解決の指摘事項

| 指摘 | 発見 | 状態 | 備考 |
|---|---|---|---|
| npm audit: nanoid / postcss / vite（high 3件） | 2026-08-17（推定） | ✅ 解決（2026-08-18） | `npm audit fix`（--forceなし）で解消。package.json変更なし、package-lock.jsonのみ更新 |
| npm audit: react-router / react-router-dom（moderate 3件、うち1件は2026-08-18のaudit時点で新規検出） | 2026-08-17（推定） | ✅ 解決（2026-08-18、本番デプロイ・実測確認済み） | react-router-dom@7.18.2へメジャー移行し解消（npm audit 0件）。段階1でv6のままfuture flagを先取り有効化し挙動不変を確認、段階2でv7へ更新。使用APIがBrowserRouter/Routes/Route/useLocation/useNavigate/Link/NavLinkのみでデータルーターAPI未使用のため挙動変更なし。PR #18（`f22a854`）でmainへマージ・本番デプロイ・本番実画面確認済み |
| `backend/main.py` の `security_headers` にCSPが無い | 2026-08-17（推定） | ✅ 解決（2026-08-18、本番デプロイ・実測確認済み） | 外部依存を棚卸しのうえ追加。ポリシー全文・本番検証結果は作業報告書参照 |
| `backend/routers/consulting.py` の `InquiryPayload` に文字数上限が無い | 2026-08-17（推定） | ✅ 解決（2026-08-18） | `feedback.py` と同じ方式（サーバ側手動チェック＋400）に統一。name/company_name=200、scale_hint/contact_phone=100、message=5000 |

## 実施記録

| 日付 | 内容 | 詳細 |
|---|---|---|
| 2026-08-18 | npm audit / CSP / consulting.py上限の3件対応 | 作業報告書: `docs/作業報告_2026-08-18.md` 参照。[PR #16](https://github.com/Shoichiro12/rakuten-kpi-app/pull/16) マージ（`61346e3`）で本番反映済み。コミット: フロント依存更新・CSPヘッダー追加・consulting.py上限追加・ドキュメント（4コミットに分割） |
| 2026-08-18 | react-router-dom v7へのメジャー移行 | 作業報告書: `docs/作業報告_2026-08-18_2.md` 参照。[PR #18](https://github.com/Shoichiro12/rakuten-kpi-app/pull/18) マージ（`f22a854`）で本番反映済み。npm audit 2件→0件（フロント依存の残件すべて解消） |

## RLS（行レベルセキュリティ）について

Supabase/Postgres の RLS 強制の仕組みは CLAUDE.md 冒頭「⚠️ セキュリティ最優先事項」節を参照。
本番の適用状況は `GET /api/security-status` で確認できる（`unprotected` が空であることが正常）。
