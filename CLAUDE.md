# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 📌 申し送り（セッション開始時に必ず読む）

**このセクションは「決定事項とその実装状態」の台帳。** 過去に「決まったはずのことが
コードにもドキュメントにも残らず、後のセッションで繰り返し議論される」事故が複数回起きた
（例: past_due UI・機能ロックが3〜4回「未回答」として再浮上した）。再発防止のルール:

1. **オーナーが方針を決めたら、その場でこの表に1行追加する**（実装が後日でも先に書く）
2. **実装が完了したら「実装済み」に更新し、コミットハッシュを書く**
3. 新しいセッションで作業を始める前に、この表の「未実装」を確認する
4. **調査・監査・設計の成果物は、それを使う実装より先にコミットする。**
   実装だけが残って根拠が消えると、後のセッションで同じ議論をやり直すことになる。
   実例: LP Hallmark監査（2026-07-30）の結果ドキュメントが一度もコミットされないまま消え、
   critical指摘3件の原文が照合不能になった（2026-08-07に発覚）。フォーカスリング修正
   （955f95f）だけが実装として残り、その根拠は失われていた。
5. **セッションを終える前に `git status` の未追跡ファイル（`??`）を確認する。**
   残すなら add、捨てるなら明示的に消す。「判断せず放置」にしない。
   実例: typeui.sh対応（区切り0〜3）が2026-08-05にコミットされないまま9時間放置され、
   翌日の別作業中に偶然発見された（ファイル更新時刻とreflogから状況を再構成し救出、49f6273）。
   同様に ureshiru_logic_tree.html も未追跡のまま消失した。
6. **ファイルパスを参照として書くときは、その時点で実在を確認する。**
   存在しないファイルへの参照は、失われたこと自体を見えなくする。
   実例: lp/index.htmlのコメントが存在しない docs/LP_lowya_dna_2026-07-30.md を
   「参照」と書いており、参照があることでファイルが失われた事実に誰も気づけなかった。
   （この参照は2026-08-07のLP全面リニューアルで解消済み）
   **⚠️ 実在チェックを機械的に回すと「生きた参照」と「失われた記録としての言及」を
   区別できない。** 上のルール4・6が挙げている2ファイルは後者＝わざと不在のまま
   記録しているので、検査で引っかかっても消さないこと。参照として書くのか、
   失われた記録として書くのかは文中で区別する。
7. **台帳・軍令帳（`docs/office_map.html`）を更新する作業を開始する前に、同じ内容の
   オープンPRや直近コミットがないか確認する**（セッション並行による重複PR防止）。

**4〜6は2026-08-07に追加。** 同じ型の事故（成果物が残らない・未追跡のまま消える・
宙に浮いた参照が損失を隠す）が短期間に3件起きたことを受けた再発防止。
自動化（Stop hookでの未追跡ファイル警告）は**意図的に見送っている** — 毎回出る警告は
無視されるようになり、「機械的チェックはあるが意味のある確認にならない」状態を作るため。
ルールの明文化で様子を見て、それでも再発するなら自動化を検討する（オーナー判断）。

**7は2026-08-27に追加。** オーナーが同じ「台帳整理」指示を2つのセッションへ並行して出し、
両方が同じ3件（CSVインジェクションのクローズ記録・管理者閲覧機能の進捗反映・区切り3/6の
文書作成）に着手して[PR #61](https://github.com/Shoichiro12/rakuten-kpi-app/pull/61)と
[PR #62](https://github.com/Shoichiro12/rakuten-kpi-app/pull/62)という重複PRが生まれた
実例を受けた再発防止（詳細は決定事項表を参照）。再発防止の本質は運用側（同じ指示を複数
セッションに投げない）にもあるが、セッション側の作業開始チェックとしても1行加える。

| 決定事項 | 決定日 | 状態 |
|---|---|---|
| 機能ロック: 未契約者は主要APIを使えない（402→/billing誘導）。`require_active_subscription` | 2026-07-29 | 実装済み（backend/subscription_guard.py） |
| past_due/unpaid は「未契約」ではなく「支払い確認が取れていない」表示＋ポータル導線 | 2026-07-29 | 実装済み（Billing.tsx） |
| 退会時にコンサル問い合わせ・フィードバック記録は削除しない（営業記録として保持。プライバシーポリシー第5条と整合） | 2026-07-28 | 実装済み（削除対象に含めない、が実装） |
| Stripe顧客の重複防止（メールで既存顧客を検索して再利用） | 2026-07-28 | **未実装**（公開後でよい、と決定済み。実装時はcheckout作成前にemail検索） |
| 専門家確認（法務文面・インボイス様式） | 2026-07-29 | **未実施**（審査完了と機能ロック実装後にまとめて手配、と決定）。**優先確認事項に「特商法第26条（営業目的取引の適用除外）への依拠」を追加（2026-07-30）**: 解約を問い合わせ経由（2〜3営業日以内に手続き完了）に変更した設計は、顧客全員が「営業のために」契約する前提で26条の適用除外に依拠している。この依拠の妥当性を最優先で確認してもらうこと |
| 解約はポータル自己完結ではなく問い合わせ経由（受付後2〜3営業日以内に手続き完了）。**Stripeカスタマーポータルの「顧客によるサブスクリプションのキャンセル」は意図的に無効化**しており、「解約ボタンがない」は不具合ではない（直さないこと）。アプリ内はフィードバック category="cancel"（解約について）で受付、通知メール件名は【解約リクエスト】で始まる | 2026-07-30 | 実装済み（feedback.py / notifications.py / Billing.tsx / FeedbackModal.tsx / lp/tokushoho.html / lp/terms.html）。ポータル側のキャンセル無効化も**実施済み（2026-07-30、Stripeダッシュボードで本番・テスト両環境とも設定・保存を確認）**。**動作確認済み（2026-07-30）**: テストモードの実ポータル画面でキャンセルボタン非表示・Billing.tsx「解約をご希望の場合」セクション表示（ローカル/本番とも）・「解約について」送信で通知メール実送信成功（Renderログ `フィードバック通知メールを送信しました` で確認、宛先はNOTIFY_EMAIL） |
| 退会（アカウント削除）は契約中（trialing/active/past_due/unpaid）はブロックし、先に解約（問い合わせ経由）を完了してもらう。退会APIはStripe契約に触れないため、契約中に退会を通すと「ログイン不可なのに課金継続」事故になる | 2026-07-30 | 実装済み（routers/account.py が409を返す＋AccountSettings.tsx が案内表示・ボタン無効化）。**本番動作確認済み（2026-07-30）**: trialingのdemoアカウントで DELETE /api/account → 409 Conflict＋UI案内・ボタン無効化を実画面確認。※ローカル（認証無効）では409より先に「認証が無効な環境では削除不可」の400が返る仕様 |
| Supabase Redirect URLs に `https://app.ureshiru.com/**` を追加（旧URLと併存） | 2026-07-29 | 実装済み（Total URLs: 2。Site URL も https://app.ureshiru.com に切替済み） |
| Render カスタムドメイン app.ureshiru.com 有効化＋Stripe Webhook URL を https://app.ureshiru.com/api/stripe/webhook に変更（whsec不変） | 2026-07-29 | 実装済み（Verified/Certificate Issued、/api/health 200確認） |
| 旧Render(Oregon)・旧Vercelプロジェクトの削除 | 2026-07-29 | **完了（2026-08-24）**。Render側: オーナーがダッシュボードを目視し、旧Oregonサービスは存在せず稼働中は`rakuten-kpi-app-sg`（Singapore）1件のみと確認（削除作業自体は不要だった。元から残っていなかった）。Vercel側: 旧プロジェクト2件（`ureshiru` / `ureshiru-lp`）を本日削除。`ureshiru.com`・`app.ureshiru.com`の稼働に影響が無いことを確認済み |
| 4万SKU CSV取込のメモリ実測 → Render Starter で不足なら Standard へ | 2026-07-29 | **未実施（延期）**。契約の都合でデータ入手が10/1以降のため延期。10月に再開 |
| テスト・デモ用アカウントのカード登録除外: env `EXEMPT_TEST_EMAILS`（カンマ区切り・空文字設定で無効化）に載ったメールは `/api/billing/checkout` で Stripe Checkout を通さず trialing をDBに直接作成する。判定はJWT検証済みメールのみ。**このメールの受信箱を持つ人は無料で全機能を使えるため、本番は自社管理のメールに差し替えること** | 2026-07-30 | 実装済み（backend/billing.py `is_exempt_test_email` / routers/billing.py `create_checkout`）。**本番envは差し替え済み（2026-07-30）**: Render `EXEMPT_TEST_EMAILS=demo@ureshiru.com` を設定・再デプロイし、demo@ureshiru.com の本番サインアップでカード登録なしtrialing作成・ダッシュボード表示を確認。※exemptアカウントはStripe契約を持たないため `/api/billing/diagnose` に「DBに subscription ID がありません」warnが出るが仕様どおり。**⚠️ 2026-08-24訂正: 既定値は`test@gmail.com`ではなく空文字。** 2026-08-03のセキュリティ指摘（未設定時に汎用アドレスが既定で除外されるリスク）を受けて`c4ea5b7`で空文字に変更されていたが、この行が3週間訂正されないままだった（詳細は下記「Coworkルーチン4週分の突き合わせ」行を参照）。**運用手順・追加削除の台帳は `docs/unyou_exempt_test_emails.md` を参照。** |
| 本番Stripe設定の確認（セキュリティ報告書2026-07-29のフォロー）: `STRIPE_WEBHOOK_SECRET` はRender envに設定済みで、`/api/billing/diagnose` は ok:true・webhook未設定警告なし（=webhook_secret_set true）。税率は10%・内税（総額¥22,000のまま内訳表示）で契約にも付与済み | 2026-07-30 | 確認済み |
| LPフォーカス状態（Hallmark audit punch list対応）: `:focus-visible` はC案刷新時に導入済みだったが、暗色地（CTAバンド・フッター）でリングが背景に同化していたため紙色に反転、`lp/style.css`（privacy/terms/tokushoho）にも追加 | 2026-07-30 | 実装済み（955f95f） |
| アクション提案ロジック第1段階（設計ドキュメント`action_logic_unified_2026-08-01.md`）の6決定: ①RMSジャンルベンチマークは手入力欄で対応（無ければ自店集計→デフォルトへフォールバック）②ROASは2段構え（100%割れ=出血アラート維持、300%未満+Limit CPO超過=停止候補は第2段階で実装）③CTRも2段構え（1%=既存緊急アラート維持、2%=新診断基準）④母数ゲートは月次430に換算（週100×30÷7。従来は月次も100で実質機能していなかった）⑤新商品はRPP診断の最低クリック50に引き上げ（KW単位50クリックの商品粒度読み替え）⑥診断パターン12（商品タイプ分離）は商品マスタ拡張まで見送り | 2026-08-01 | 第1段階実装済み（gates.py / benchmarks.py / products4カラム / genre_benchmarksテーブル）。第2段階実装済み（diagnosis.py の8分類、ROAS300%+Limit CPO複合条件、停止候補の試算+セカンドベスト併記、原価率設定済み商品のcpo_over判定有効化）。第3段階実装済み（item_targetsテーブル、target_calc.pyの確定公式MIN(現状,前年)、実績なし商品の推定+承認フロー、商品分析CSV取込時の自動再計算、目標設定画面のアイテム別セクション。※一括入力UIは別チケットのまま）。第4段階は旧3-G（広告予算按分）を取りやめ、v2（売上予算按分＋ギャップ逆算）として実装済み（次行参照） |
| アクション提案ロジック第4段階v2（売上予算按分＋ギャップ逆算）の決定: ①旧3-G「広告予算そのものの月次按分・上限管理」は**不要と確定**（作らない。広告費をいくらかけられるかは会社ごとの判断のため、許容広告費は都度入力・保存しない）②店舗全体の目標CVR・客単価は案1＝Target手入力を指標ごとに優先→無ければMIN(現状,前年)を店舗合算(site_uu軸)に適用③ギャップ逆算は順序型（アクセス→CVR→客単価のウォーターフォール踏襲）。CVR改善上限=MAX(現状,前年)＝過去到達水準、実績なしはベンチマーク解決値④アイテム別目標との整合性は「合計が月次予算を超えるときだけ警告」・強制同期なし⑤季節指数はMonthlyItemSales店舗合算が正（RPPフォールバックはconfidence上限medium）、有効月=月次430UU通過月のみ、24ヶ月+=high/12〜23=medium/12未満=均等按分/0ヶ月=新規店舗「まず1ヶ月データ収集」案内。指数・按分値は保存せず都度算出⑥予算年度起点はshops.budget_year_start_month（既定1月） | 2026-08-02 | 実装済み（revenue_plan.py / routers/revenue_plan.py / shops.annual_sales_budget+budget_year_start_month / GET /api/revenue-plan / RevenuePlanPanel.tsx / 目標設定画面の年間売上予算セクション / sample_data 14ヶ月化。コミット ae7d697・9013304・fd81a9d・b7fc631）。**本番検証済み（2026-08-02）**: デプロイ反映・`/api/security-status` ok:true・shopsカラム追加・デモ再生成後の按分（confidence=medium/有効14ヶ月）・実画面（案A/案B並列、12ヶ月プレビュー）を確認 |
| 第4段階v2の追加機能（手動補正・12ヶ月フル逆算）の決定: ①**サンプル再生成はShopの設定値を上書きしない**（現在値がNULLのときだけデモ値を入れる。UIで設定した年間予算が再生成で消える事象への対応）②月次売上予算は`targets.target_sales_budget`で月単位の手動補正が可能（null=自動按分）。**保存は専用API `POST /api/revenue-plan/override` のみで、既存 `POST /api/targets`（KGIフォーム）には絶対に載せない**（フォーム保存のたびに補正が消える事故になる）③**手動補正しても他月へは再配分しない**（12ヶ月合計と年間予算の乖離はフロントで情報表示）④12ヶ月フル逆算の目標CVR・客単価は基準月ロジックの対象月一般化（現状値=その月以前の直近実績月、前年値=その月の前年同月、MIN採用。Target手入力が指標ごとに常に勝つ）。前年同月経由で季節性が目標値に反映されるため月ごとに値が変わるのは仕様⑤CPCは各月のRPP実績、無い月は直近実績月で代用（CPCの季節性はスコープ外・UIに注記）⑥過去月にも逆算を表示する（事後検証用）⑦**`revenue_plan.py` のDBアクセスはプリフェッチ3クエリに集約する規約**（月ループ内で `db.query` を呼ばない。素朴ループだと1リクエスト約80クエリになる） | 2026-08-02 | 実装済み（sample_data.py上書きガード / targets.target_sales_budget / routers/revenue_plan.py override / revenue_plan.py build_context・build_month_cascade / 目標設定画面の年間目標プランナー表（サマリ⇄詳細切替・インライン編集）。コミット 36879fa・fc61c30・8ee6242・7f55cfd）。**本番検証済み（2026-08-02）**: `/api/security-status` ok:true・12ヶ月カスケード（応答666ms・全月にrequired_access/target_cvr(basis)/cpc_source等）・目標設定画面のサマリ⇄詳細切替・インライン編集で手動バッジ＋必要アクセス/広告費/達成率の再計算・解除で自動按分復帰・12ヶ月合計と年間予算の差分表示・**サンプル再生成で起点月9月が保持される（ガードの実証）** を実画面で確認 |
| 17パターン改善アクションの動的生成の決定: ①アクション文言は**参照資料の言い回しを使わず自社作成**（publicリポジトリのため。研修名のコード内言及も一般表現に置換済み）②決め打ちの17行テーブルは持たず、`focus`（未達KPIのウォーターフォール順）から見出し・打ち手を動的合成（1KPI未達=3件/複数=各2件、店舗全体・商品ページの2スコープ）③矛盾ケース（KPI全達成×売上未達）は特定KPIを名指ししない総合型見出し。**評価ランクは現行の△・優先度高を維持**（参照資料は○相当だが「診断は正直に・甘くしない」方針を優先。オーナー確認済み）④低母数=アクセス打ち手のみ／判定不可(パターン17)=目標・データ整備の案内⑤ジャンル・商品階層への展開は**今回見送り**（店舗レベルで効果を見てから別チケット。GAP行にはYoY集計が無くAPI拡張が必要） | 2026-08-02 | 実装済み（matrix_actions.py / evaluation.py の「売上のみ未達」時の空ラベルコメント崩れ修正 / /api/evaluation/matrix に actions 追加 / EvaluationMatrix.tsx 表示=ダッシュボード・GAP両画面。16通り＋特殊系の機械列挙で文の破綻なしを確認。コミット c9f828a・6aa349c）。**本番検証済み（2026-08-02）**: matrixレスポンスのactions返却（パターン14「アクセス・客単価を改善しましょう」shop4件/product4件）・実画面のカード表示・`/api/security-status` ok:true・publicリポジトリのrawファイルで研修名の不在を確認 |
| アイテム別目標の一括入力UI（計画書 `docs/jisso_keikaku_item_targets_bulk_2026-08-03.md`）: ①保存方式は一括1リクエスト `POST /api/item-targets/bulk`（対象月共通・1トランザクション、検証エラー時は全体ロールバック）。単発 `POST /api/item-targets` と `_upsert_one` で確定公式のコードパスを共有。②性能はプリフェッチ最適化せず素直なループ（低頻度の明示保存のため許容。revenue_planの高頻度表示系とは性質が違う、とオーナー承認）③絞り込み前提（商品名/管理番号検索・ジャンル大分類・未設定のみ）。ジャンルは一覧API `GET /api/item-targets` の各行に `genre_u1/u2/u3` を追加（実績優先→商品マスタのカテゴリ）④CSV入出力は第2段階に見送り（「Excelで一気に入れたい」要望が出たら着手。masters.pyのexport/import作法を流用予定） | 2026-08-03 | 区切り1(バックエンド)・区切り2(フロント)実装済み。**本番検証済み（2026-08-03、b1b5d0e）**: demoアカウントで3件（ACC-001/002/003）をまとめて入力→一括保存で目標CVR・客単価・必要アクセスが確定公式で一括算出・未保存バッジ/件数/ボタン活性、検索(プロテイン→1件)・未設定のみ(6件)・ジャンルドロップダウン(スポーツ=backend genre由来)を実画面で確認。`/api/security-status` ok:true（item_targets含む全テーブルRLS保護・unprotected空） |
| UIバックログ（計画書 `docs/jisso_keikaku_ui_backlog_2026-08-03.md`・5点承認済み）: 区切りA=テーブルソート共通化（`components/table/useTableSort`+`SortableTh`。降順→昇順→解除、null常に末尾、絞り込み後にソート適用の順で統一）、区切りB=期間UI刷新＋年次（**表示系4画面のみ。診断・アラート系は月次のまま＋注記**。年次=暦年固定・年間売上予算と突き合わせ）、区切りC=ダッシュボード再構成（案A=3層の強弱＋効率指標の表圧縮）。デザインスキルはskills.shから導入（CLAUDE.md Commands節参照・gitignore済み） | 2026-08-03 | **区切りA実装済み・本番検証済み（1b2224c）**: 商品別KPI(CVR降順/昇順)・アイテム別目標(必要アクセス)・RPP(広告費)の列クリックソートを実画面確認。適用5画面=商品別KPI/RPP/GAP/商品マスタ/アイテム別目標。**区切りB実装済み・本番検証済み（53cea66）**: period=yearly を dashboard/gap4本/products/export2本に追加（率は分子分母を年合算後に再計算・前期比=前年・商品ドリルダウンは商品単位合算で月跨ぎ重複排除・`get_shop_yearly`/`period_utils.py`/`MIN_ACCESS_SAMPLE_YEARLY=5220`）。年次のKGI目標=annual_sales_budget→月次targets年内合算フォールバック。PeriodSelector刷新（セグメント＋実集計期間ラベル常時表示＋ラベルクリックでピッカー＋最新データへ。週次ピッカーは日曜起点週に丸め）。**年次中は診断系（今日やること/評価マトリクス/アラート/アクション提案/売上予算プラン/ActionSummary/ActionPanel）を呼ばず注記表示**。本番実画面: ダッシュボード年次(達成率49.9%=1496万/3000万・注記・診断非表示)・GAP年次(KGIツリー年合算・目標未設定表示)・月次リグレッション・security-status ok:true を確認。**区切りC実装済み・本番検証済み・オーナーOK済み（cee19a4）**: ダッシュボードを3層構成に再編。1層=KGIヒーロー（売上vs目標・達成率バー・**着地見込み=進行中期間のみ実績÷経過割合**）＋売上3分解（週次=RPP軸/月次・年次=商品分析軸をgap/shopから取得。**軸を混ぜない**）。2層=今日やること→アラート→評価マトリクス→施策のその後。3層=利益・広告投資4枚（標準カード）＋詳細指標12個を`<details>`表に圧縮（既定畳み・CPO/CPCは下がる方が良い色反転・Limit CPO超過行ハイライト）＋グラフ帯＋売上予算プラン/アクセス逆算。旧「店舗全体の実績」hero4枚はヒーロー帯と重複のため削除 |
| Supabase Auth の確認メール（Confirm signup）を日本語化＋独自SMTP（Gmail）で送信。**設定はすべてSupabaseダッシュボード側で、コード・envには一切持たない**。設定場所は Authentication → Emails。SMTP は Sender email `sales@ureshiru.com`／Sender name `ウレシル`／Host `smtp.gmail.com`／Port `587`／Username `shoichiro.nakamura.0601@gmail.com`（送信認証はこのGmailアカウント。**パスワードはこのアカウントのGoogleアプリパスワード**で、名前は `ureshiru-supabase-auth`。値はダッシュボードのみ・ここには書かない）。**注意点**: (a) Gmail SMTP はFromに認証アカウント以外（sales@…）を使うと受信側で「gmail.com 経由」と表示される（動作上は問題なし。消すには独自ドメイン認証＝SendGrid等への移行が必要）。(b) Gmail の送信上限は概ね1日500通。登録が増えたら SendGrid 等へ切替を検討。(c) テンプレのSubject/BodyはカスタムSMTP設定前は編集不可（読み取り専用）になる仕様 | 2026-08-03 | 実装済み・本番動作確認済み。Confirm signupテンプレを日本語化して保存（件名`【ウレシル】メールアドレスの確認をお願いします`）。**実登録テストで日本語メール受信を確認（2026-08-03、k11mm121@gmail.com宛）**。※途中 `535 5.7.8 Username and Password not accepted`（auth-logs）でつまずいたが、原因はアプリパスワード（Gmailは4文字区切り表示・スペースごと貼ると弾かれる／2段階認証必須）。スペースを抜いて再入力で解決。**追加対応（2026-08-03）**: 同じトーンで Reset password（件名`【ウレシル】パスワード再設定のご案内`）と Change email address（件名`【ウレシル】メールアドレス変更の確認`）の2テンプレも日本語化・保存済み（場所は同じ Authentication → Emails。SMTP設定は共通のため変更なし）。Magic Link は利用有無未確認のため英語のまま据え置き。**追加対応2（2026-08-03）**: Emails画面の**Security セクション**（Authenticationセクションとは別枠の「操作完了の事後通知」）のうち、Password changed（件名`【ウレシル】パスワードが変更されました`）と Email address changed（件名`【ウレシル】メールアドレスが変更されました`）も日本語化・保存済み。**後者は `{{ .OldEmail }}`／`{{ .Email }}` で変更前後のアドレスを本文に併記**（乗っ取りに気づきやすくするため）。両方ともトグルは元からON。同セクションの Phone number changed / Sign-in method linked・removed / MFA method added・removed は**全てトグルOFF＝未使用のため未翻訳**（MFA等を導入する際に合わせて日本語化すること） |
| サイドバー折りたたみ（計画書 `docs/jisso_keikaku_sidebar_collapse_2026-08-03.md`・3点承認済み）: ①折りたたみ時のラベル補完は **`title` 属性を使わずCSS自作ツールチップ**（`title` は表示が1〜2秒遅く、キーボードフォーカスでは出ない。折りたたみ時はラベルが唯一の手掛かりのため `group-hover` に加え `group-focus-visible` でも表示。ツールチップは `aria-hidden` で、読み上げ名は `aria-label` が担う）②トグルは**上部ロゴ行の右端**・lucide の `PanelLeftClose`/`PanelLeftOpen`（目線の起点が上・下部は項目数が多く埋もれるため）③**幅のアニメーションは入れない（即時切替）**。`width` のトランジションは毎フレーム再レイアウトが走り、商品マスタのような重いテーブルを右に抱えると確実にカクつく。**「ふわっと動かす」要望は出ていない＝再浮上しても入れない**④折りたたみ時のアクティブ表示は「アイコンの赤角丸ボックス」＋「行左端の3px縦バー」の2点（64px幅では塗りだけでは見落とす）⑤永続化は `localStorage`（キー `ureshiru:sidebar-collapsed`・lazy initializerで初回描画から確定値・例外は握りつぶす）。スマホ幅のハンバーガーとドラッグ幅可変は**今回スコープ外** | 2026-08-03 | 実装済み（`frontend/src/components/layout/Sidebar.tsx` 単体の変更。App.tsx は無変更＝`<main className="flex-1">` が残り幅を自動で埋めるため）。ついでに全ナビ項目へ `focus-visible` リングと `touch-manipulation` を追加。**検証済み**: `npm run build` 型エラー0／ヘッドレスChromeで展開・折りたたみ・ホバーとフォーカス時のツールチップ表示・リロード後の維持（幅64pxで復帰）を実画面確認。**本番反映済み（68a1c38）**。**追加対応（同日・オーナー指摘）**: サイドバーを畳んでも商品マスタ・目標設定の中身が広がらなかった。原因はこの2画面だけ `max-w-4xl mx-auto`（896px）で頭打ちだったこと（ダッシュボード・GAP・商品別KPI・RPPは元から上限なし）。**上限を撤廃して全幅に統一**（フォーム・カードも横に伸びるのは承知のうえで許容）。**ページ直下のコンテナに `max-w-*` を戻さないこと**（戻すとサイドバー折りたたみの意味が無くなる）。ただし**表・注記など個々のブロックに付ける上限幅は別**で、全幅化の副作用（列数の少ない表の間延び・注記の1行が長すぎる）への対策として意図的に入れている: 目標設定の年間目標プランナー表は `max-w-2xl`（サマリ5列）/`max-w-5xl`（詳細9列）＋`tabular-nums`、その周辺の注記3箇所は `max-w-3xl` |
| 数字とグラフの見せ方の全画面展開（計画書 `docs/jisso_keikaku_design_rollout_2026-08-04.md`／規則 `docs/ui_number_and_chart_rules_2026-08-04.md`／知識 `docs/ui_design_knowledge_2026-08-04.md`）: **オーナー承認済み2点** ①進め方は「先に共通部品を作り、画面には貼るだけにする」＝**区切り0（共通部品）→1（ダッシュボード）→2（商品別KPI・RPP）→3（フォーム系4画面）→4（GAP分析）**、各区切りでデプロイして目視②**GAP分析のロジックツリーは各ノードを弾丸グラフにする**（枝は残す。色だけ差し替える案・ツリーをやめる案は不採用）。**追加承認3点（2026-08-04）** ③**GAPの右サイドのアクションパネルは位置を動かさない**（中身の整えのみ）。VISIONの「最終地点はアクション」には賛成だが、GAPは挙動が独特でリスクを重ねたくないため。区切り4を実画面で見てから、必要ならその後の別チケットとして位置変更を検討する④**GAPのドリルダウンUI自体は触らない**（見た目だけに絞る）⑤**表の金額は丸めず生値のまま**（Few の「表は正確な値を確認する場所、グラフは傾向を見る場所」の切り分けに従う。右寄せ・単位は見出しに1回・`tabular-nums`・1px区切りで可読性は上がるので、精度を落とす必要はない）。**方針の要点**: 金額は万・億（日本語圏は1万単位で数えるため K/M は使わない）、割合指標の前期比は「%」でなく「pt」（数値の正しさの問題）、良い方向は指標ごと（CPC・CPO・広告費は下がったら緑／クリック数・注文件数・アクセス・売上原価・店舗運営経費は中立）、目標進捗は進捗バーでなく弾丸グラフ（上限100%で目標超過を表現できないため）。**採らないもの**: ダークテーマ・ネオン・グラスモーフィズム・3D（`ui_design_knowledge` の決定） | 2026-08-04 | **区切り0実装済み**（`lib/format.ts` / `lib/metrics.ts` / `components/kpi/{Delta,BulletChart,Stat,Sparkline}.tsx` / `components/chart/defaults.ts` を新規作成。既存画面は未変更＝表示は何も変わっていない）。`npm run build` 型エラー0。**区切り1〜4も実装済み**: 区切り1=ダッシュボード（`f2ef06d` KGIヒーローの弾丸グラフ化＋**広告費の増加が緑で出ていたバグを修正**／`4e4f7e2` pt表記・万円表記・グラフの装飾削減で完了）、区切り2=商品別KPI（`e75726e`）・RPP広告実績（`3a3b744`）、区切り3=目標設定・商品マスタ（`47524d0`）、区切り4=GAP分析のロジックツリーを弾丸グラフ化（`47bcbbd`）。**区切り3も完了（`af959e2` / PR #1）**: データ取込みの月次プレビューカードを `format.ts` 経由に（売上合計＝万・億、件数＝3桁区切り、CVR＝`formatRate`、丸めた金額は `title` に正確な値）、`tabular-nums` と `whitespace-nowrap` を追加（「134.1万」と「円」が改行で切り離されていた）。**レポート出力は画面に数値表示が無く、CSVも `export.py` が `round()` した生値＋単位を別カラムで返しているため変更不要だった**（規則どおり。表示用の書式をCSVに流用していないことを確認済み）。あわせて**日本語ラベルに効かない `uppercase` / `tracking-wide` を区切り3の4画面から除去**（データ取込み3・目標設定3・商品マスタ1）。**ロゴの「楽天EC」は英字ブランド表記なので残す。** **区切り4の表（GAP分析のジャンル別参照テーブル・商品別テーブルへの `tabular-nums`・1px区切り・固定ヘッダー・左端固定）も実装済み（次行参照）。残っているのは 区切り5（請求・プラン／アカウント設定の金額表記統一）だけ**。他画面に残る `uppercase` は計画書 `docs/jisso_keikaku_design_rollout_2026-08-04.md` の6章に一覧あり |
| **GAP分析のアクセス軸を site_uu（サイト全体の訪問UU）に変更する（2026-08-04 オーナー決定・実装済み）**: **これは「KGIツリーは `KGI = アクセス × CVR × 客単価` をすべて `RppWeekly` 由来で統一（access=クリック数ct）」という従来の決定を上書きする。** 理由=売上が主で、売上を伸ばすためにアクセスが要り、アクセスを増やす手段のひとつがRPP広告。**RPPクリックは「広告の成果」であって売上分解の「アクセス」ではない**（オーナーの整理）。決定内容: ①月次・年次のGAP分析（KGIツリー・ジャンル別・商品別）のアクセスは `site_uu`（MonthlyItemSales由来の訪問UU）にする②**CVRも必ずセットで site_uu 基準（cv ÷ access_uu）に変える。** 片方だけ変えると `売上 = アクセス × CVR × 客単価` の恒等式が壊れる。現状の CVR 4.31% は 17÷394（注文÷RPPクリック）で rpp_click 基準③**週次は案Aを採用**＝RPPクリックのまま残すが、列名・ノード名を「RPP広告クリック」と明示してサイト全体アクセスと別物だと分かるようにする（週次はサイト全体アクセスのデータが無い。商品分析CSVが月次のみのため）。週次でRPPの動きを追う用途は残す④**同一画面で両軸を混ぜない規約（`access_definitions.py`）は維持。** 期間で軸が切り替わるので、画面には現在の軸バッジを必ず出す。バックエンドは既に両軸を返す経路を持つ（`gap_analysis.py` に `access_axis: "site_uu"` と `"rpp_click"` の実装が併存）ため、繋ぎ替えと目標値・達成率の整合が主な作業 | 2026-08-04 | **実装済み・main反映済み（`af959e2` / PR #1、2026-08-04マージ）。** **調査で分かったこと**: ①②のバックエンド（月次・年次を site_uu にする／CVRもセットで site_uu 基準にする）は**着手前から実装済みだった**（`gap_analysis.py` の shop 軸経路）。**`targets.target_access` の解釈も確認の必要が無かった**＝目標設定画面のラベルが最初から「アクセス目標（UU）／月間ユニークユーザー数」で、目標は元から site_uu 基準で定義されている。**実際に残っていた欠陥は別のところにあった（以下2件を修正）**。 **(1) 4エンドポイントが別々の条件で軸を判定していた。** kpi-tree / shop / genre / product がそれぞれ独自に「商品分析データがあるか」を判定しており、廃盤除外を判定前に効かせるか後に効かせるかもバラバラだった＝**1つの画面の中でツリーだけ site_uu、ジャンルは rpp_click という混在が起こりうる状態**（要件No.5違反）。判定を `gap_analysis.uses_shop_axis()` の1関数に集約し、**廃盤除外は軸判定に混ぜない**（絞り込みで0件になっても軸は変えない）ことを規約にした。**新しいGAP系エンドポイントを足すときも必ずこの関数で判定すること。** `/api/gap/shop` は `access_axis` を返していなかったので追加（全レスポンスで返す）。 **(2) 週次の目標比較が二重に間違っていた。** ロジックツリーは月次目標を日割り按分せずに週次実績と比べており、**同じ画面の評価マトリクス（按分あり）と食い違っていた**（実測: 売上達成率がツリー14.8% / マトリクス65.8%）。さらに目標は site_uu・月次基準、週次実績は RPP広告クリック基準で軸も違う。**オーナー決定（2026-08-04）で週次は目標比較を出さないことにした**＝`target_comparable: false` を返し、フロントは達成率・目標線・GAPを描かず「週次は目標比較なし」と表示する。「目標未設定」と誤解されないよう、ツリー下に理由の注記を出す。**週次に達成率を戻さないこと。** 戻すなら週次専用の目標（RPPクリック基準）を持つところから。 **フロント**: `ACCESS_AXIS_LABEL` を `アクセス（RPP広告クリック）` / `アクセス（サイト全体UU）` に変更（案A の「名前で切り分ける」）、KGIツリーの週次ノード名も `アクセス（RPP広告クリック）` に。**軸バッジは画面ヘッダーに1つだけ**（`components/gap/AccessAxisBadge.tsx`。オーナー決定）。**ジャンルカードに軸バッジを戻さないこと**（同じバッジが3つ並ぶため移設した）。 **検証済み**: 週次/月次/年次それぞれで kpi-tree・shop・genre・product の4本が同じ `access_axis` を返すことを実測（週次=rpp_click ×4／月次・年次=site_uu ×4）。ヘッドレスChromeでヘッダーの軸バッジ切替・週次ツリーの「週次は目標比較なし」表示・週次ノード名・月次の達成率（アクセス26%）を実画面確認。`npm run build` 型エラー0、`from main import app` のimport確認、コンソールエラー0。 **残っている論点**: **評価マトリクスは週次でも目標比較を続けている**（アクセス（RPPクリック数）の目標比28.9% 等）。軸違いはツリーと同じ問題だが、`EvaluationMatrix` はダッシュボードと共用で、17パターンの評価ランク判定がtarget/YoY に依存するため、直すとダッシュボードの週次評価も変わる。**別チケットとして判断すること。** |
| **GAP分析のKPI選択とドリルダウン連動（2026-08-04 オーナー指摘＋実装済み。※以下の3点セットのうち①③は同日中に上書きされている。最終形は状態欄を読むこと）**: KPIノード（クリック数/CVR/客単価）をクリックしても、**下の「ジャンル別内訳」は見出しの文言が変わるだけで中身が変わらない**（実画面で確認: CVRを選んでも客単価を選んでもカードは「スポーツ ¥742,402 / CVR 3.37% / 客単価 ¥6,749 / ROAS 213.1%」で同一）。**選ばせる意味が無い状態。** 選択したKPIに応じて、少なくとも「並び順（そのKPIのGAPが大きい順）」「カードに出す主指標」「強調」が変わるべき。VISION.md の「出力の最終地点は次のアクション」に照らすと、選んだKPIに対して何をすべきかまで繋がるのが本来。**設計方針（2026-08-04 オーナー決定・3点セットで変える）**: ①**並び順** … 選択したKPIのギャップ（目標比・前年比の乖離）が大きい順にジャンル・商品を並べ替える②**カードの指標構成** … 4指標（売上・CVR・客単価・ROAS）は**全部残す。差し替えない。** 選択したKPIだけ `Stat.tsx` の `hero` サイズで視覚的に主役にし、他3指標は `default` サイズに落として補助情報にする（区切り0で作った部品をそのまま使う）③**アクションパネルの連動（最重要）** … 選択したKPIに応じて `matrix_actions.py` の `ACTION_LIBRARY`（access/cvr/av の3バケット）から該当する打ち手を右パネルに出す。**新しいロジックは作らない。** 17パターン改善アクションの既存資産を GAP画面に接続するだけ（CVR選択→CVR改善の打ち手／客単価選択→客単価改善の打ち手／アクセス選択→access バケット）。**この決定が守っていること**: ドリルダウンのUI構造（ステップ・階層）自体には手を入れない＝既決の「触らない」方針の範囲内。変えるのは「選択したKPIに応じて中身のデータをどう出すか」のみ。**アクセス軸統一（1つ上の行）とは独立**で、軸統一の結果を待たずに進めてよい | 2026-08-04 | **実装済み・main反映済み（`af959e2` / PR #1、2026-08-04マージ）。** 着手時の進め方: (1) `matrix_actions.py` の `ACTION_LIBRARY` を GAP分析のAPIから呼べるか（既存の呼び出し経路・引数の形）を確認 (2) 上記3点を実装 (3) いつもの流れ（実装→ローカル検証→push→本番デプロイ確認→実画面検証）。**着手前の調査で判明した重要な前提のズレ（2026-08-04）**: 右サイドの `components/gap/ActionPanel.tsx` は**KPI単位のパネルではなく「商品を1つ選んだときに開く商品単位のパネル」**だった（props は `product` / `shopKpis` / `weekKey` / `onClose`、中身は `detectIssues()` で在庫→アクセス→客単価/CVR の優先度判定をする独自ロジック）。一方 `matrix_actions.py` の `build_matrix_actions()` は**評価マトリクスの結果（result）を受けて shop/product 2スコープの打ち手を返す**もので、入力も出力も粒度が違う。つまり③は「既存パネルに繋ぐだけ」では済まず、**(a) KPI選択に連動する新しい表示先を用意する（ジャンルカードの上など）か、(b) `build_matrix_actions` を KPI キー単体で呼べる薄いラッパー（`ACTION_LIBRARY[kpi][scope]` を返すだけ）を足すか**、どちらかの判断が要る。ロジックを新規に書き起こす必要はない（`ACTION_LIBRARY` の access/cvr/av バケットをそのまま引く）が、**接続先の設計判断が1つ残っている**ので、実装前にここを決めること。**接続先はオーナー決定で案(a)＝フロント完結（2026-08-04）。バックエンドは触らない。** **実装結果（2026-08-04）**: 新規 `components/gap/actionLibrary.ts`（`ActionPanel.tsx` に直書きされていた4P打ち手を切り出した単一の真実。**打ち手の文言をコンポーネント側に書き足さないこと**）／`components/gap/kpiGap.ts`（並べ替えと主役指標の共通ロジック）／`components/gap/KpiActionHint.tsx`（KPI選択に連動する打ち手ブロック。ロジックツリーとジャンルカードの間に置く。年次は診断系を出さない方針に合わせて非表示）。変更 `GenreCards.tsx` / `GapAnalysis.tsx` / `ActionPanel.tsx`。**決定どおりに実装しなかった点が1つ**: ②で「選択KPIを `Stat` の `hero` サイズにする」としていたが、`hero` は同じ規約で「1画面に1つ、多くて2つ」と決めており、ジャンルカードN枚すべてを hero にすると規約と正面から衝突する。そこで**カード内の「主役スロット」を選択KPIに差し替える**形にした（`Stat` の `default` サイズ。4指標は消さず補助行に残す＝決定の趣旨は満たす）。**並べ替えの基準は「目標比」ではなく「前期比」**: ジャンル・商品の階層には目標値が無い（`/api/gap/genre` `/api/gap/product` は目標を返さない）ため。画面に基準を明記してある。**ついでに商品テーブルへ客単価の列を追加**（客単価で並べ替えても列が無く、並びが変わる理由が画面から読めなかったため）。列見出しソート（`useTableSort`）は既定順の上に乗る＝ユーザーが列を選べばそちらが勝ち、3回クリックで解除すると KPI GAP 順に戻る。**検証済み**: `npm run build` 型エラー0／ヘッドレスChromeで access・cvr・av の3通りとも、打ち手ブロックの見出し・ジャンルカードの主役指標・商品テーブルの並び順が切り替わることを実画面確認、並び順はAPIの生値から計算した期待順と完全一致、コンソールエラー0。**⚠️ 実画面レビュー後のオーナー再決定（2026-08-04）で、上の3点セットのうち①と③は上書きされた。以下が最終形なので、元の3点セットの文面に戻さないこと。** **①並び順 → 選択KPIでは切り替えない。ジャンル・商品とも常に「売上の前期比が悪い順」で固定。** 売上が主で、アクセス・CVR・客単価はその分解でしかないため、「どこを直すと売上に効くか」を探す順番は常に売上の落ち込みで決める。実装は `kpiGap.ts` の `orderBySalesGap()` 1本に集約（KPIを引数に取らないのは意図的）。**「選択KPI基準に切り替えたほうが親切では」と後から戻さないこと。** **②カードの主役スロット差し替えは採用のまま。ただしサイズは `Stat` の `default`（22px）で確定、hero 化はしない。** **③KPI選択に連動する打ち手ブロック（`KpiActionHint.tsx`）は不採用。作ったうえで削除した。** 打ち手の表示先はこの画面では2箇所だけ＝上の評価マトリクスのカード（`matrix_actions.py` 由来）と、商品を選んだときの右サイド `ActionPanel`。**3つ目の表示先を増やさないこと**（同じ画面に似たリストが並ぶため）。案(a)/(b) の判断そのものが不要になったので、この論点は閉じている。 **再決定を受けた実装の状態**: `KpiActionHint.tsx` は削除済み。`actionLibrary.ts`（`ActionPanel.tsx` から切り出した4P打ち手）は**消費者が `ActionPanel.tsx` 1つに戻ったが、そのまま残してある**（データと表示の分離・`key` は不変。畳み直したいなら別チケット）。`kpiGap.ts` は主役指標の表示（`gapKpiValue` / `gapKpiDelta` / `gapKpiLabel` / `gapMetricKey`）と `orderBySalesGap` を持つ。**商品テーブルに追加した客単価の列は残してある**（売上3分解のうち客単価だけ列が無かったため。不要なら1行で外せる）。**再検証済み**: `npm run build` 型エラー0／ヘッドレスChromeで access・cvr・av のどれを選んでも並び順が同一かつAPIの売上前期比 昇順と完全一致することを確認（ACC-001 → SPW-001 → ACC-002 → BAG-001 → RUN-001 → SPW-003）、打ち手ブロックの不在、コンソールエラー0。**この段階でKPI選択が変えるのは「カードの主役指標」と見出し文言だけになった。** **⚠️ 2026-08-20 追記: ①（並び順・強調の売上固定）は顧客の実利用フィードバックを受けて再上書きされ、選択KPI基準に変更された。③（打ち手ブロック不採用）と②はそのまま。最下部の 2026-08-20 の行を参照。** |
| **Cowork からの変更の受け渡しはパッチ運用で固定する（2026-08-04 決定）**: Cowork のクラウドサンドボックスから GitHub へ直接 push する方式は**採らない**。理由=**渡す口が無いから**。①サンドボックスに入っている `GH_TOKEN`/`GITHUB_TOKEN` はCowork のセッション用トークンで、GitHub の git 操作では `Invalid username or token. Password authentication is not supported for Git operations.` になる（実測）②Cowork には利用者がシークレット・環境変数を登録する仕組みが無い（公式ドキュメントにあるのは「session-scoped tokens that expire within hours」のみ）③プロジェクトの GitHub 連携は**読み取り専用**（ファイル名と中身の同期だけ。コミット・PRは不可）④MCPコネクタのレジストリに GitHub の書き込み系コネクタが無い。→ Fine-grained PAT を発行してもチャットに貼る以外に渡す方法が無く、**コンテナはセッション終了で破棄されるので毎回貼り直し＝会話ログに毎回残る**。割に合わない。**PAT方式の再検討は、Cowork側にシークレット登録の仕組みができてからにすること。** **代わりに受け渡しの規約を決めた（行き違いの実際の原因はpush権限ではなく、パッチを送り直したこと）**: (1) **パッチは常に `main` からの累積版1本だけ**にする。区切りごとの単体パッチを別に送らない (2) ファイル名にコミット範囲を入れる（例 `rakuten-kpi_main..786b20e.patch`） (3) **SHA256 と同梱ファイル一覧を必ず添える** (4) 適用前に `git apply --check` を回す手順を毎回書く (5) **「これ以前に送ったパッチは破棄」と毎回明記する** (6) Cowork 側はローカルにブランチを切ってコミットまで作り、`git diff main...HEAD` を出す。オーナーは同名ブランチを切ってパッチを当て、コミット・push する | 2026-08-04 | 運用開始済み。**PATは発行しないこと**（発行済みなら revoke してよい）。**PR #1 で実証済み（2026-08-04）**: パッチをチャット本文に分割して貼り、オーナー側で適用 → PR作成 → マージまで到達。マージ後に `refs/pull/1/head` を取得して突き合わせたところ、**12ファイルすべてが git のブロブハッシュレベルで完全一致**していた（コピペ経由でも欠損しない）。適用時は `git apply --ignore-whitespace` を使うこと（差分に行末スペースの行が含まれ、コピペで落ちるため） |
| **GAP分析画面のキーボードフォーカス順序（2026-08-04 発見・別チケット）**: typeui.sh「clean」導入に伴うキーボード到達性修正（区切り3。LogicTreeのSVGノード・GAPの改善策ボタン・RPPの診断バッジへの`tabIndex`/`onKeyDown`/`aria-label`追加）自体は実画面で正しく機能することを確認済み。**しかし実用上は問題が残る**: Tabキーを押すとまずサイドバー（`Sidebar.tsx`）のナビゲーション項目にフォーカスが移動し、本文（GAP分析の改善ボタン等）に到達するまで何度もTabを押す必要がある。原因はページ全体のDOM順（サイドバー→本文）にあり、区切り3のtabIndex追加では解決できない構造的な問題。一般的な対策は「本文へスキップ」リンク（`<a href="#main-content">本文へスキップ</a>`をページ先頭・視覚的には非表示でフォーカス時のみ表示）をレイアウト共通部分（`App.tsx`または各ページのHeader）に追加すること | 2026-08-04 | **実装済み（2026-08-05・PR #7 `d4207a7`）**。当初は「未実装（優先度: 中）」として記録されていたが、翌日オーナー指摘を受けて対応した。`App.tsx` のルート div 先頭に `本文へスキップ`（`href="#main-content"`）を置き、`<main>` に `id="main-content"` と `tabIndex={-1}` を付与。**全ページ共通のレイアウトに入れたので、この行が指摘していた「全ページへの適用」は満たしている**。実測で本文まで Tab 14回 → Tab 1回＋Enter になった。実装上の注意2点は上の行（二重スクロールバー解消とスキップリンク追加）に記載 |
| **区切り4の残り: GAP分析の2つの表に表の作法を適用（2026-08-04 実装）**: ジャンル別参照テーブル・商品別テーブルへ `tabular-nums`・1px区切り・固定ヘッダー・左端固定を入れた。**この作業で分かった実装上の前提が2つあり、他画面へ展開するときも必ず守ること**: **(1) 固定ヘッダーは「表のラッパー自身が縦スクロールする」ことが前提。** ラッパーが `overflow-x-auto` だけだと縦の計算値も auto になるが高さ無制限で縦スクロールが起きず、`sticky top-0` を書いてもヘッダーは固定されない（ページ側のスクロールで枠ごと画面外に出る）。**`max-h-*` と必ずセットにする**（今回は `max-h-[70vh] overflow-auto`）。**なお区切り2で入れた `ProductKPI.tsx` / `RppAnalysis.tsx` の `sticky top-0` はこの前提を満たしておらず、実際には固定されていない。表示は壊れていないので緊急ではないが、直すなら同じ `max-h` 付与が必要＝表の高さを制限する挙動変更になるため別チケット（2026-08-04）。** **(2) 左端固定の識別列には `min-w` が要る。** 無いと狭い幅で商品名と「母数不足・参考値」バッジが1文字ずつ折り返し、行の高さが数百pxに膨らむ（固定した列が読めないと左端固定の意味がない）。今回は `min-w-[11rem]`。**また浮く左端セルには行と同じ背景を明示的に持たせること**（`bg-inherit` は Tailwind の出力順に依存して崩れるので使わない。`tr` に `group` を付け、セル側は `group-hover:` で追随させる）。ついでに**行背景を「警告 > 選択 > 通常」の1本の三項に統一**した（従来は `bg-blue-50` と `bg-red-50` が別々の三項で、両方立つ行の勝敗が CSS の出力順任せだった）。**単位は見出しに寄せていない**＝セル側の `formatCurrency` / `formatPercent` が `¥` と `%` を出しており、見出しへ移すには `lib/format.ts` への差し替えが要るため（やるなら別作業） | 2026-08-04 | 実装済み（`0d2d5b0` / PR #3・`frontend/src/pages/GapAnalysis.tsx` 単体の変更）。**検証済み**: `npm run build` 型エラー0／ヘッドレスChrome（900×620で横スクロールと `max-h` を発動させた状態）で、商品別テーブル11行・縦横ともスクロール可能な状態にして**スクロール後もヘッダーがラッパー上端・左端列がラッパー左端に残ることを幾何で実測**（`headerStuckToWrapperTop` / `firstColStuckToWrapperLeft` とも true）、警告行の赤背景が固定セルにも追随して透けないこと、`font-variant-numeric: tabular-nums`・行区切り 1px（`tr` の border-top、gray-100）・ヘッダー下境界 1px・`text-transform: none`（uppercase除去）を computed style で確認。週次（`rpp_click`）・月次（`site_uu`）の両軸で列見出しの切替と表の作法を確認、コンソールエラー0。**本番反映確認済み（2026-08-04、PR #3 マージ `14b3865`）**: 本番CSS（`app.ureshiru.com/assets/index-DY8VpgNr.css`）に `.max-h-\[70vh\]{max-height:70vh}` と `.min-w-\[11rem\]{min-width:11rem}` が生成されていることを実物で確認（Tailwindは使用クラスのみ出力するので、これが2つの表への適用の証拠になる）。ローカルで main をビルドしたCSSとハッシュも一致（`index-DY8VpgNr.css`）。**JSのハッシュは一致しないが正常**＝本番ビルドには `VITE_SUPABASE_*` が埋め込まれるため（CSSはenvに依存しないので一致する）。**確認方法の限界**: 本番の認証必要画面（`/api/security-status` も401）はログインなしで開けないため、**ブラウザでログインして目視した確認ではなく、配信されているCSS/JSの実物で担保している**。実際の挙動はローカル（ヘッドレスChrome・55行・900×620と1440×900）で実測済み |
| **区切り5: 請求・プラン／アカウント設定の金額表記（2026-08-04 オーナー判断＝最小で閉じる）**: 着手前の洗い出しで、**対象2画面の数値表示は全部で5箇所しかなく、`format.ts` に寄せられるのは2箇所だけ**と判明した（`AccountSettings` の登録データ「N 行」／`Billing` 診断パネルの金額の桁区切り）。**`Billing` の月額「¥20,000（税抜） / ¥22,000（税込）」は触らないこと。** 理由は3つ: ①**バックエンド由来**＝`billing.py` の `PLAN_AMOUNT_LABEL`（env で上書き可能）が完成した文字列を返しており、フロントで整形しても env を変えられたら意味がない②**法令上の制約がコードに明記されている**＝総額表示義務のため税込を主表記から外さない・税抜と税込は同じ視認性で並列（`billing.py` のコメント）③**契約金額に「万・億」の丸めを当ててはいけない**＝規則の丸めは「カードと軸のみ」で、`¥22,000` を `2.2万円` にするのは請求画面として不適切。**診断パネルも `formatYen`（「円」後置・丸めあり）にしていない**＝同じ画面の主表記が `¥` 前置なので、ここだけ後置にすると画面内で金額の書き方が食い違うため、`¥` は文言として残し桁区切りだけ `formatCount` に寄せた。**別チケットとして切り出した論点**: アプリ全体で金額書式が2系統ある（`formatCurrency`＝`lib/utils.ts`・`¥` 前置・丸めなし・9ファイル／`formatYen`＝`lib/format.ts`・「円」後置・万億で丸め・6ファイル。**`Dashboard.tsx` と `RppAnalysis.tsx` は両方を使っている**）。規則52行「セルの `¥` は消して見出しに（円）」に照らすと `format.ts` 側に寄せるのが筋だが、表示が広範囲に変わるため区切り5より大きい作業になる。**オーナー判断で区切り5は最小で閉じ、統一は別チケット（2026-08-04）** | 2026-08-04 | 実装済み（`d486bbe` / PR #4・`frontend/src/pages/{Billing,AccountSettings}.tsx`）。**これでデザイン展開の区切り0〜5はすべて完了。** **検証済み**: `npm run build` 型エラー0／ヘッドレスChromeで請求・プラン画面の「診断する」を実行し診断パネルの「金額: ¥22,000（税込・内税）」が**表示不変**であることを実画面確認、コンソールエラー0。**アカウント設定はローカル（認証無効）では画面全体が「ローカル開発モードのため利用できません」になり実画面で出せない**ため、vite 経由で `format.ts` を import して `formatCount` の出力が変更前の `toLocaleString` 直呼びと一致することを7ケース（0/1/244/1000/22000/1234567/40000000）で実測（全件一致）。`null`・`undefined`・`NaN` は**変更前は例外になっていたが `—` を返すようになった**（改善）。**本番反映確認済み（2026-08-04、PR #4 マージ `654bec9`）**: 本番JS（`assets/index-A2JKPSQG.js`）に `Name:"text-gray-900 tabular-nums",children:qr(e.total_rows," 行")`（=`formatCount(info.total_rows, ' 行')` の minify 形。変更前なら `e.total_rows.toLocaleString()` が並ぶ）と、Billing 側の `` `${qr(x.price.unit_a…` `` を実物で確認。**ローカルで確認できなかった「登録データ N 行」も、この配信物レベルで反映を確定できた**（本番の認証必要画面はログインなしで開けないため、ブラウザ目視ではなく配信JSでの担保。関数の出力一致は7ケースでローカル実測済み）。**なお本番の `last-modified` は 05:01:05Z のままで PR #4 マージ（05:00:32Z）の33秒後という不自然な値だが、上記のとおり中身は区切り5を含む**（Renderが成果物のタイムスタンプをソース由来にしているためと推測。`last-modified` でデプロイ世代を判定しないこと） |
| **【一時措置・撤去済み】カード登録なしでトライアル開始できるようにした（2026-08-06 導入 → 同日撤去）**: **⚠️ 現在この措置は効いていない（env削除済み）。以下は「なぜ入れたか」と「再び必要になったときの手順」の記録で、いま守るべきルールではない。** **理由 = Stripe側の決済・入金が一時停止中で、Checkout でのカード登録が機能しなかったため。** アイズ社のトライアル運用テスト（2026-08-07予定）を止めないための措置で、**この期間の課金が必要になった場合は手動で請求書を発行する運用**にする（オーナー決定）。 **撤去条件だったもの（＝達成済み）: Stripeの決済・入金が復旧したら env を削除して元に戻す。** コードは残してよいが、フラグが立っている限りカード登録なしで全機能が使える状態が続く。`GET /api/billing/diagnose` がフラグの有効/無効と対象範囲を必ず warn で返すので、撤去し忘れはそこで気づける（**この仕掛けは実際に機能した**）。 **設定（env・既定は無効）**: `TRIAL_WITHOUT_CARD=true` で有効化／`TRIAL_WITHOUT_CARD_DOMAINS=example.co.jp`（カンマ区切り・任意）で対象メールのドメインを限定。**DOMAINS を指定しないとサインアップした全員が対象**になる＝`app.ureshiru.com` は self-serve で登録できるため、期間中は誰でも無料で全機能を使える。**絞れるなら必ず DOMAINS を設定すること。** **実装**: カード必須にしていたのは実質 `routers/billing.py` の `create_checkout()` 1か所だけ（Stripe Checkout のURLを返す）。既存の `EXEMPT_TEST_EMAILS` と**同じコードパス**（Checkoutを通さず `trialing` をDBに直接作成）を新フラグでも開く形にした。判定は `billing.trial_without_card_for()` で、**必ずJWT検証済みの `user.email`** を渡す（入力値で判定すると誰でも回避できる）。 **⚠️ この経路の契約は Stripe 側に存在しない**（顧客もサブスクも作らない）＝自動課金されないかわりに **Webhookが来ないのでトライアル期限が過ぎても自動で止まらない**（`status` は `trialing` のまま）。フラグを戻したあと、該当ユーザーの `Subscription` 行をどうするか（手動で status を変える／改めて契約してもらう）は運用で決めること。**誰にカードなしで開始したかは Render のログに残る**（`カード登録なしでトライアルを開始しました（TRIAL_WITHOUT_CARD）: user_id=… email=…`）。Stripeに記録が無いので、手動請求のときはこのログが唯一の手掛かりになる。 **画面側**: (a) `/api/billing/plans` に `card_required` を追加し、カード不要のときは「決済は Stripe の安全な画面で行われます」を出さない（実際にはStripeへ飛ばないため）。(b) `/api/billing/status` に `stripe_linked` を追加し、**Stripe顧客が無い契約では「お支払い方法の変更」ボタンを出さない**（押すと `契約情報が見つかりません` になり利用者を混乱させる。通しの動作確認で実際に踏んだ）。**この2つはフラグを戻せば自動的に元の表示に戻る**（実Stripe契約は `card_required=true` / `stripe_linked=true` になるため）。 | 2026-08-06 | **撤去完了（2026-08-06）。コードは残存・envは未設定＝既定の無効状態。** Render の `TRIAL_WITHOUT_CARD` を削除して再デプロイ済み。**本番で撤去を実測確認済み（2026-08-06）**: ①`/api/billing/diagnose` が `trial_without_card: false` / `trial_without_card_domains: []` / `ok: true` を返し、**TRIAL_WITHOUT_CARD の warn が0件**（`_TRIAL_WITHOUT_CARD` はモジュール読み込み時評価の定数なので、false＝プロセス再起動済みの証拠）②EXEMPT_TEST_EMAILS 対象外・未契約のアカウントで `/api/billing/plans` が `card_required: true` を返し、実画面のボタン下注記が「**決済は Stripe の安全な画面で行われます。**」に復帰（「ただいまカード登録なしで〜」は非表示）③**実クリックで通し**: 「14日間の無料トライアルを始める」→ `checkout.stripe.com/c/pay/`**`cs_live_`**…へ遷移し、Checkout画面に「14 日間無料／その後 ¥22,000/月・2026年8月20日以降／小計 ¥22,000／**消費税 (10% を含む) ¥2,000**／今日期日の合計 ¥0」を表示。**＝Stripe側の決済停止も復旧しており、`checkout.Session.create` が本番モードで成功する**（診断は `Price.retrieve` 等の読み取り系しか叩かないので、**診断ok だけでは復旧の証明にならない**点に注意）。カード入力・「申し込む」は実行せず離脱、確認後の `/api/billing/status` は `is_active: false` で未契約のまま。 **⚠️ 未対応の宿題（この措置の後始末）**: フラグ経由で作られた契約は Stripe に存在せず **Webhookが来ないので期限が過ぎても自動で止まらない**（`status` は `trialing` のまま）。**該当者がいたか・どう処理するかは未確認・未決定。** 洗い出しは Render のログ（`カード登録なしでトライアルを開始しました（TRIAL_WITHOUT_CARD）: user_id=… email=…`）が唯一の手掛かり。なお **demo@ureshiru.com も `trialing`／Stripe ID が NULL** だが、これは `EXEMPT_TEST_EMAILS` 由来（trial_end 2026-08-13 ＝ 2026-07-30 起点）でこの一時措置とは別件。EXEMPT_TEST_EMAILS は撤去対象ではないので**そのままで正しい**。 **⚠️ 再び有効化するときの注意**: Render に `TRIAL_WITHOUT_CARD=true` を設定するだけで即座に効く（**DOMAINS 未指定だとサインアップした全員が対象**）。`EXEMPT_TEST_EMAILS` 対象のアカウント（demo等）は**フラグと無関係に常に Checkout を通らない**ため、**フラグの有効/無効の検証には使えない**（2026-08-06 に実際にこれで検証できず、別アカウントでの再ログインが必要になった）。 **導入時の検証（2026-08-06、PR #10 `75d4693`・`backend/billing.py` / `routers/billing.py` / `.env.example` / `Billing.tsx` / `types/index.ts`）**: `npm run build` 型エラー0 / `from main import app` OK / コンソールエラー0（Viteの HMR WebSocket のみ）。フラグ判定を5パターン×5メール（env未設定・フラグのみ・ドメイン限定・@付き記法・フラグfalse）で実測。HTTPで**フラグOFF時は従来どおりStripe経路に入る**ことを確認（契約は作られない）。フラグONで `/api/billing/checkout` が Stripe を呼ばずに `trialing` を作成し、`stripe_customer_id`/`stripe_subscription_id` とも NULL（=自動課金なし）、主要API 200、診断に warn、ログ出力を確認。**実画面（実クリック）で通し**: 「14日間の無料トライアルを始める」→ カード画面を経ずに「契約中／トライアル中／トライアル終了 2026/8/20」へ到達、注記が「カード登録なしで開始できます」に切替、支払方法ボタン非表示。**Stripe顧客ありの契約ではボタンと従来文言が出るリグレッションなしも実測。** |
| **Stripe の Product 名を「ウレシル 通常プラン」→「ウレシル 月額プラン」に変更（表記統一・2026-08-06）**: **なぜ気づいたか**: TRIAL_WITHOUT_CARD 撤去の検証で本番の Checkout 画面を実際に開いたところ、**Stripe側だけ「ウレシル 通常プラン」**で、アプリ（`billing.py` の `PLAN_LABELS`）とLP（料金セクション／JSON-LD の `Offer.name`）の「ウレシル 月額プラン」と食い違っていた。**顧客が見るのは Checkout 画面と請求書＝Stripe の Product 名**なので、アプリ側のラベルだけ合っていても意味がない（アプリのラベルが出るのは契約後の `/billing` だけ）。**⚠️ 直す場所は price ではなく Product 側**（`prod_Uy8KgcvxLY3gkx`）。**Product 名の変更は price を作り直さない＝既存契約はそのまま**で、税率変更のように「envを直しても既存契約に遡らない」類の落とし穴は無い。**⚠️ ただし発行済みの請求書に遡って反映されるかは未確認**（Stripeは請求書明細を発行時点でスナップショットするため、過去分は旧名のまま残る可能性がある。決済停止期間中で実請求がほぼ無いはずなので実害は小さい）。**今後プラン名を変えるときは3箇所（アプリ・LP・Stripe Product）を同時に直すこと**（本文の価格・プラン名の節に追記済み） | 2026-08-06 | 実施済み（Stripeダッシュボード側の変更。**コード変更なし**＝`PLAN_LABELS` は元から「ウレシル 月額プラン」で正しかった）。**本番実測で3箇所一致を確認（2026-08-06）**: ①`GET /api/billing/diagnose` の `price.product_name` = **`ウレシル 月額プラン`**（`product_id: prod_Uy8KgcvxLY3gkx`）②`GET /api/billing/plans` のアプリ側ラベル = `ウレシル 月額プラン`③LP `https://ureshiru.com/` の料金表示と JSON-LD `Offer.name` = `ウレシル 月額プラン`。**`price_id` は `price_1TyC3uBs3Mtc3ArJyhipDuLo` のまま不変**（＝Product のリネームであって Price の作り直しではないので、既存契約の再作成は不要）。診断は `ok: true`、価格まわりの警告なし（残る warn は「DBに subscription ID がありません」1件のみ＝Checkout を離脱した検証アカウントの状態を正しく表しているだけ） |
| **⚠️【蒸し返し禁止】Checkout・カード明細に出る事業者名「JuneOne」は変更しない（2026-08-06 オーナー決定）**: **決定 = 変更しない。** 理由 = **Stripeアカウントの屋号設定上、明細書表記を分離できない**（屋号 JuneOne がメインでかかる仕様）。**「顧客はLP・アプリで『ウレシル』しか見ていないので、ウレシルに揃えたほうが親切では」という提案は検討済みで、技術的にできないという結論。再提案しないこと。** **どこに出るか（2026-08-06 に本番 Checkout `cs_live_…` で実測）**: ①Checkout画面ヘッダーのブランド名 ②申込ボタン下の同意文「解約されるまで、利用規約に基づき**JuneOne**が請求を行うことに同意した上で、申し込んでください。」 ③Link の保存説明文「**JuneOne** をはじめ、Link に対応している場所であればどこでも〜」 ④**顧客のクレジットカード明細**。**⚠️ この決定は「表記の不一致を放置してよい」という意味ではない**＝顧客から見れば別会社への請求に見えるリスクは残るので、**告知で埋める**方針にした（次行の別チケット参照）。**プラン名（ウレシル 月額プラン）の3箇所統一とは別問題**なので混同しないこと。あちらは変更可能で実際に統一済み | 2026-08-06 | 決定のみ（**コード変更なし・Stripe設定変更なし＝現状維持が正**）。この行は「なぜ直っていないのか」を次のセッションに伝えるための記録で、作業は発生しない |
| **⚠️ 別チケット（未着手）: カード明細が「JuneOne」名義になることをLP側の法的文書で告知する（2026-08-06 記録）**: **やること** = 利用規約 **または** 特定商取引法に基づく表記に「クレジットカードのご利用明細には **JuneOne** の名称で記載されます」といった一文を追加する。**なぜ** = 上の行のとおり明細表記は変更できないため、顧客が見慣れない名前を見て「身に覚えのない請求」と誤認するのを防ぐ（問い合わせ・チャージバックの予防）。購入前に読める場所に書いてあることが重要。 **⚠️ 着手時の必須事項**: **必ずLP側（別リポジトリ）に書くこと。アプリ内に法的ページを作らない**（本文の「法的文書はこのアプリ内に持たない」の節を参照。過去にアプリ側にも同じページを作り、価格改定でLPと食い違った事故がある）。**どちらのページに入れるかは着手時の判断**（特商法なら「販売価格・支払方法」まわり、利用規約なら利用料金の条が候補）。**この決定に依存する実装は無い**ので、優先度は低くてよいが、公開後に問い合わせが出たら即着手すること | 2026-08-06 | **未着手（記録のみ。実装は今回スコープ外とオーナー決定）** |
| **RPP CSV取込時の週始まり（日曜）正規化（2026-08-04 記録 → 2026-08-05 実装済み）**: **RPP CSV を取り込むとき `RppWeekly.week_start` を日曜へ正規化していない。** 現状は `backend/routers/import_csv.py` の `parse_rpp_real_file` / `parse_rpp_df` が CSV の集計期間の開始日を**そのまま**保存している（`week_date = date.fromisoformat(date_from)`）。一方**読み出し側は日曜キーで突き合わせる**＝`backend/routers/dashboard.py` の `get_week_start()` が `d.isoweekday() % 7` で日曜起点に丸めており、PeriodSelector の週次ピッカーも日曜起点週に丸めている。**そのため CSV の集計期間が日曜始まり以外（例: 月曜〜日曜）でDLされた場合、取込は成功するのにダッシュボード・GAP分析の週次に何も出ない「無言の不具合」になる。** **① 未実装であること**: 上記のとおり正規化コードは main に入っていない。**② 未確認であること**: **「RMSのRPP CSVが常に日曜始まりか」は確認が取れていない。** 常に日曜始まりなら実害はなく、この修正自体が不要になる。**③ 着手前にやること（必須）**: **まず RMS 側で実際にRPP CSVをダウンロードし、5行目の「集計期間: … YYYY-MM-DD - YYYY-MM-DD」の開始日が何曜日かを確認する。** 曜日が日曜以外になり得ることが分かってから実装に入る（そうでなければ着手しない）。**参考実装のパッチ**: `docs/patches/week_start_sunday_fix_2026-08-04.patch`（115行・3ファイル）。旧worktree `claude/focused-swanson-f04f99`（先端 `ba77336`・2026-06-08）に未コミットで残っていた作業を、worktree 撤去前に保存したもの。中身は (a) `import_csv.py` に `_week_start_sunday()` を追加して2箇所の `week_date` を正規化（修正本体）(b) `DataImport.tsx` / `types/index.ts` に `weeks?: string[]` を足して**取込結果に「取込週」＝正規化後の実際の週キーを表示する**（正規化できたかを画面で確認できるようにする補助）。**⚠️ このパッチは現在の main にそのままでは当たらない**（`git apply --check` で `patch does not apply` を実測。2026-06 以降 `import_csv.py` が大きく変わっているため）。**当てようとせず、上記(a)(b)の意図を読んで手で移植すること** | 2026-08-04 | **実装済み（2026-08-05・PR #9 `8bfa80b`）**。**③のRMS確認は実施済みで、「常に日曜始まり」ではないことが確定した**（ダウンロードフォルダの実ファイルを読み取りのみで検査。`2026-07-01(水)〜07-22(水) 22日間` / `2026-08-01(土)〜08-05(水) 5日間` / `2026-07-19(日)〜07-25(土) 7日間` が混在）。**あわせて「7日間ですらない期間が実在する」ことも判明し、これが実装方針を変えた。** **パッチをそのまま移植しなかった点が2つあり、戻さないこと**: **(1) 月次レポート由来の行は正規化しない。** パッチは `period_type` に関係なく丸めていたが、`RppWeekly` の月合計行は `week_start` が月初であること自体が識別子で、`detect_rpp_double_count()` が「`week_start` == 月次レポートの `date_from`」で二重計上を検出している。月初が日曜以外のとき（例 2026-04-01 水 → 03-29）月合計行が前月へ飛び、二重計上検出も壊れる。 **(2) 7日間ちょうどの期間だけ正規化する（`_is_seven_days()`）。** 22日間を丸めると `2026-06-28` になり、7月のデータ22日分が6月の月次集計へ移動したうえ既存の6/28週の行を上書きする（`week_start`+`product_url` の upsert）。5日間（8/1〜8/5）も丸めると7月へ移動して8月の月次から抜ける。**7日間以外は従来と完全に同一の挙動**（開始日のまま保存）にし、注記で「週次画面に出ない場合がある・日〜土の7日間でDLしてほしい」と伝えるだけにした。 **`RppSales`（生データ保管）は一切変更していない**（`date_from` はCSVのまま）。取込結果に `weeks`（正規化後の日曜〜土曜）を返し、画面に「取込週」バッジを表示。丸めた場合はメッセージに明示する（無言で日付をズラさない）。 **⚠️ `docs/patches/week_start_sunday_fix_2026-08-04.patch` は上記(1)の欠陥を含むため適用しないこと。** 参考として残してあるだけで、main の実装が正。 **検証済み**: `npm run build` 型エラー0 / `from main import app` OK / コンソールエラー0 ／パース単体5ケース（日曜始まり7日=不変・月曜始まり7日→日曜・22日間=不変・5日間=不変・月次=不変）／**既存データ不変をSHAで実測**（サンプル88行が取込前後で `4c4c7f3d29216d18` 一致）／E2E（月曜始まりCSV取込で `/api/dashboard?period=weekly&date=2026-08-09` が `kpis: null` → `gross 195000`、月次2026-08も 742,402+195,000=937,402 で整合、`/api/import/integrity` ok:true）／実画面（2026-06-08(月)のCSV取込 →「取込週: 2026-06-07〜2026-06-13」バッジ → ダッシュボード週次で 11.1万円・アクセス777・CVR 2.70%・客単価5,286円 を表示）。旧worktree 2本（`access-definition-unification-0bbc91` / `focused-swanson-f04f99`）は撤去済み。前者は新規作成していた `access_definitions.py`・`ReliabilityNote.tsx` とも**main に取り込み済みかつ main のほうが進化していた**（worktree版38行=閾値100のみ／main版55行=`MIN_ACCESS_SAMPLE_MONTHLY=430`・`MIN_ACCESS_SAMPLE_YEARLY=5220`・`min_access_for()` 追加）ため、パッチを残さず破棄した |
| **GAP分析の二重スクロールバー解消とスキップリンク追加（2026-08-05 実画面指摘・実装済み）**: **(1) 縦スクロールは本文側の1本に統一した。`GapAnalysis.tsx` の `TABLE_SCROLL` に `max-h-*` を戻さないこと。** 区切り4（PR #3）で固定ヘッダーのために入れた `max-h-[70vh] overflow-auto` は、既に縦スクロール領域である本文（`flex-1 overflow-y-auto`）の内側に2つ目の縦スクロール領域を作っていた（実測: 本文側のバーが x=1250〜1265、表のバーが x=1214〜1229 で右端に並ぶ）。`overflow-x-auto` だけにして解消。**トレードオフとして固定ヘッダー（`sticky top-0`）は成立しなくなるため `top-0` も外した**（残すと効かない指定がコードに残る）。**左端の識別列の固定（`sticky left-0`）は横スクロールに対して働くのでそのまま機能する**＝広い表で行を見失う問題は解消したまま（実測: 横 346px スクロール時も1列目がラッパー左端に残り、背景も不透明）。**「固定ヘッダーを戻したい」となったら、表だけを縦スクロールさせる以外に方法が無い＝二重スクロールバーが必ず復活する**ので、その前提で判断すること。 **(2) ページ自体のスクロールバー（3本目）も消した。`App.tsx` のルート div の `relative` を外さないこと。** Tailwind の `sr-only` は `position:absolute` なので、位置指定の祖先が無いと包含ブロックが ICB になり、ルートの `overflow-hidden` に切り取られない。GAP分析では `components/kpi/Delta.tsx` の読み上げ用スパン（「減少」）が本文の奥にあるため静的位置が y=1329 まで下がり、`h-screen` なのに 530px ぶんドキュメントがスクロールしていた（`documentElement.scrollHeight` 1330 / `clientHeight` 800 を実測）。ルートに `relative` を付けて包含ブロックを固定し解消。**個別コンポーネント側ではなくアプリ枠で塞いでいる**のは、同じことが他画面の `sr-only` でも起こり得るため。 **(3) スキップリンク**: ルート div の先頭（＝DOM上の最初のフォーカス可能要素）に `本文へスキップ`（`href="#main-content"`）を置き、`<main>` に `id="main-content"` と `tabIndex={-1}` を付けた。**`tabIndex={-1}` は必須**（無いとスクロールするだけでフォーカスが移らず、次の Tab がサイドバーの続きに戻る）。**表示は `focus:absolute` ではなく `focus:fixed` を使うこと**（ルートの `overflow-hidden` に切り取られ得るため）。 | 2026-08-05 | 実装済み（`frontend/src/App.tsx` / `frontend/src/pages/GapAnalysis.tsx` の2ファイル）。**検証済み（ローカル実画面・ヘッドレスChrome 1280x800 / 900x700 / 1440x900）**: `npm run build` 型エラー0／GAP STEP3で縦スクロール領域が本文側1つだけ・`window.innerWidth - documentElement.clientWidth = 0`（ページバー無し）・`documentElement` がスクロール不可であることを実測／左端固定列の追随を幾何で実測／他7画面（`/` `/products` `/rpp` `/master` `/targets` `/import` `/reports`）でも縦スクロール領域が本文側1つ・ページバー0のリグレッション無しを確認／ActionPanel を開いた状態は本文とパネルで別々の縦スクロール（右端が x=1120 と x=1440 で並ばない＝仕様どおりの2ペイン）／**実キー入力**で Tab 1回→スキップリンクにフォーカス（`position:fixed`・(16,16) 114x36・クリップ無し・中心の最前面要素が当該リンク）、Enter（`isTrusted:true`）→ `<main id="main-content">` にフォーカス移動、さらに Tab →本文の最初のコントロール（廃盤を除外チェックボックス）に到達。**スキップリンク導入前は本文まで14回の Tab が必要だった**（フォーカス可能要素の実測）。コンソールエラー0 |
| **⚠️ 別チケット（未着手）: GAP分析の商品テーブルの表示列を絞り、横スクロールごと解消する（2026-08-05 検討・見送り）**: 上の行で固定ヘッダーを外したが、**両立させる方法は検討済みで、実装しないと決めたのはコストとリスクの判断であって「不可能だから」ではない**。同じ議論を次のセッションで一からやり直さないための記録。 **前提となる実測（ヘッドレスChromeで検証済み・2026-08-05）**: `position: sticky` は最も近いスクロールコンテナに対して効く。本文スクローラを3段階スクロールさせて要素の top が動かないかを測ったところ、①overflow の祖先なし=張り付く ②`overflow: hidden` の内側=**張り付かない**（＝カードの角丸用 `overflow-hidden` だけで sticky は死ぬ。ヘッダーを別要素にしても無意味） ③`overflow-x: auto` の内側=張り付かない ④**`overflow: clip` / `overflow-x: clip` の内側=張り付く**（clip は scroll container を作らないため。3000px幅の子を切り取りつつ sticky は外側に効いた）。 **検討した4案と結論**: **案1 ヘッダー分離＋横スクロール同期** … カードを `overflow-x: clip` にし、`sticky top-0` のヘッダー帯（内部は clip＋`transform: translateX(-scrollLeft)`）と `overflow-x: auto` の本体に分ける。技術的に成立するが、(a) table が2つに割れるので `table-layout: fixed`＋`colgroup` か ResizeObserver での列幅実測同期が要る（再測定トリガーはデータ再取得・列ソート・期間切替・軸切替・廃盤除外・サイドバー折りたたみ・ウィンドウリサイズ）(b) ヘッダー帯は clip なので `scrollLeft` を持てず transform＋rAFスロットルになる (c) ヘッダー側の `sticky left-0` は clip 下では本文スクローラ基準になり効かないので translateX の打ち消しが要る。新規共通コンポーネント150〜250行相当。**崩れ方が「数pxずれる」で気づきにくく、この画面は状態が多いため負債になると判断して見送り（オーナー決定）**。 **案2 本文スクローラに横スクロールさせる** … 表のラッパーから overflow を全部外すだけ（JSゼロ）。実測でヘッダー固定・左端列とも成立したが、**横スクロールで本文全体が流れる**（ロジックツリー見出しの left が 265→65 に移動するのを実測）。UX劣化のため見送り。 **案4 収まるときだけ sticky** … ResizeObserver で収まっていれば `overflow: visible`、はみ出していれば `overflow-x: auto` に切り替える（20行程度）。window幅やサイドバー開閉で挙動が変わり「効いたり効かなかったりする」ため見送り。 **将来やるなら案3**: **商品テーブルの表示列（現在14列）を絞る。** 実測では **1280px幅では横スクロールが発生していない**（900px幅で初めて `scrollWidth 965 / clientWidth 619`）＝`overflow-x-auto` は狭いwindowのための保険でしかない。常に収まる列数にすればラッパーが scroll container でなくなり、**`sticky top-0` 一行だけで固定ヘッダーが成立する**（上記①のケース）＝根本解決。 **⚠️ 着手前に必要なのはコードの判断ではなくプロダクト判断**: 「14列のうち、GAP分析の商品ドリルダウンで常時見る必要がある列はどれか」。ここが決まらないと着手しないこと。 | 2026-08-05 | **未着手**（今回のスコープ外とオーナー決定）。案1・2・4は**再検討しないと決めた**ので、蒸し返さないこと。案3に着手するときは上記の列選定から |
| **LPの虚偽表示3件を修正（2026-08-07・LP全面リニューアルの前段として緊急対応）**: リニューアル前の突き合わせで、LPが「実装されていない機能」を書いている箇所が見つかったため、リニューアル本体と切り離して先に直した。**(1)「メール・チャットサポート」→「メール・アプリ内フォームでのサポート」**: チャットサポートの実装がリポジトリ内に一切ない（Intercom/Crisp/Zendesk等なし）。実際の窓口はメールとサイドバー「不具合・要望を送る」の2つ。**(2)「週次アラート通知」→「週次・月次の改善アラート表示」**: `notifications.py` は `NOTIFY_EMAIL` 宛（＝自社宛）の問い合わせ・フィードバック通知しか持たず、cron/スケジューラも無い＝**顧客に通知を送る仕組みは存在しない**。アラートは `GET /api/dashboard/alerts` の画面内表示で、対応期間は weekly/monthly のみ（yearly は診断系を出さない方針どおり非対応）。**「通知」という語は、実際に送信する仕組みができるまで使わないこと。** **(3) `lp/help.html` の「『請求・プラン』画面からいつでも解約できます」を問い合わせ経由に修正**: 2026-07-30 に Stripeポータルの自己解約を意図的に無効化した際、`tokushoho.html` と `terms.html` は直したが **help.html だけ取り残され、自分の法的文書と食い違っていた**。**解約文言は index.html(FAQ) / help.html / terms.html(第5条) / tokushoho.html(返品・キャンセル) の4ファイルに存在する。1つ直すときは必ず4つとも確認すること**（各ファイルに相互参照コメントを入れた）。あわせて help.html の画面ガイド「解約手続き」→「解約のご連絡（お問い合わせフォーム経由）」、最終更新日を 2026-08-07 に更新。 **⚠️ 調査で判明した別件**: `docs/LP_hallmark_audit_2026-07-30.md` と `docs/LP_lowya_dna_2026-07-30.md` は**どちらも存在しない**（作業ツリー・全ブランチ全履歴を検索して不在を確認）。にもかかわらず `lp/index.html` のデザイントークンのコメントが後者を「参照」と書いている。**Hallmark監査のcritical 3件（テンプレ骨格・AIナビ・3カラムグリッド×2）は原文が失われており照合不能**。監査から実施されたのはフォーカスリング修正（`955f95f`）のみで、**発見時点では**構造3件が未解消だった（`.cards` の3カラムが2箇所＋ヒーローモックに1箇所、ナビは640px以下で全消滅）。**この3件は同日のLP全面リニューアル（`f86a9b8`）で構造レベルから解消済み**（下記参照）。 | 2026-08-07 | 実装済み（`lp/index.html` / `lp/help.html`）。**検証済み**: 虚偽claim 3語の残存0をgrepで確認、解約文言の「2〜3営業日以内」「問い合わせフォーム経由」が4ファイルとも揃うことを確認、ヘッドレスChromeで両ページをレンダリングし `details` 要素数（index 4 / help 8）とマークアップ非破壊・文言の出し分け9項目をDOMで実測（全PASS）。**後続ステップも同日中に完了した**（当初「未対応」と書いていたが、いずれも 2026-08-07 に完了）: ①LP新構成の設計 → `docs/LP_redesign_sekkei_2026-08-07.md`（決定事項・掲載ルール・撮影仕様を記録）②Hallmark critical 3件込みの全面リニューアル → **`f86a9b8`**。骨格を Narrative Workflow（工程スパイン1.0〜5.0）へ入れ替え、ナビを N9 Edge-aligned minimal、フッターを Ft1 Mast-headed に。**C案トーン（配色・フォント・トークン）は維持**＝critical 3件はいずれも構造の指摘で色の指摘ではないため。3カラムグリッドは「禁止」ではなく「使う理由をなくす」形で消し、CSSから `repeat(3, 1fr)` と `repeat(auto-fit` が消えた。ヒーローの自作UIモック（偽ダッシュボード）も実スクリーンショットに置換。**モバイルでナビ項目が全消滅するバグも N9 の採用で構造的に解消**③help.htmlの内容更新 → 9章→11章（「期間の使い分け（週次・月次・年次）」「目標の設定（3つの階層）」を新設。改善アラートに「メール等での通知は行っていない」を明記）④再発防止ルール4〜6の追加 → **`2a6d58d`**。 **⚠️ 残タスク2件（着手前に必ず `docs/LP_redesign_sekkei_2026-08-07.md` の §8〜§10 を読むこと）**: **(1) `shot-5-learn.jpg` の撮り直しと工程5.0への差し込み** … サンプルデータでは施策の実施月＝当月のため比較対象の翌月実績が存在せず、「実施した施策のその後」が全件「測定待ち」になる（構造上の制約であって不具合ではない）。載せると機能が動いていないように見えるため今回は画像なしで公開した。`backend/sample_data.py` に**過去月の施策＋翌月実績**を仕込んでから撮り直す。差し込み位置は `lp/index.html` のHTMLコメントで確保済み。**(2) アイズコーポレーションの実績追加** … 2026-08-07時点で許諾未取得のため掲載していない（書けば捏造になる）。許諾が取れたら「お客様の声」を追加する。位置は `lp/index.html` のコメントで確保済み。**掲載時のルールは設計書 §9 を厳守**＝開示範囲を広げない・「最大◯%」「平均◯%」に言い換えない（母数1件で平均は成立しない）・事例が2件以上になるまで stat strip 型（数値を3〜5個並べる形）にしない。免責は主張の直下・同じカード内に置き、小さい灰色文字に落とさない（景表法の打消し表示の指針）|
| **LPに「私たちについて」ページ（`lp/about.html`）を新規追加（2026-08-09）**: EC通販業界10年以上の経歴・ウレシルを作った動機・価格設定の考え方を3段落で書いた読み物ページ。**文言はオーナー確定・そのまま使用**（実装側で書き換えないこと）。**①匿名性**: **顔写真・実名・会社名を載せない**（オーナー決定）。`index.html` にある JSON-LD の `Person`（実名）も**意図的に入れていない**ため、SEO目的で足さないこと。※実名そのものは特商法・プライバシーポリシー・`index.html` の JSON-LD で開示済み（法令上の要請）＝**匿名なのは about.html の中だけ**で、サイト全体が匿名なわけではない。**②「私たち」表記の判断**: 実態は個人事業（特商法の事業者は個人名義）で、見出しは複数形・本文は一人称単数のトーンという不整合がある。**問題なしと判断してこのまま採用した**（景表法の優良誤認は商品の内容・取引条件が対象で、事業者の呼称は対象外。個人事業が「私たち」を使うのは商慣行上ふつう）。**「一人称に揃えるべきでは」と後から書き換えないこと。** **③デザイン**: `style.css` + C案トーンの上書きという **help.html と同じ作り**。**index.html の `:root` をコピーしない**（トークンが2箇所に増えるとズレるため）。ナビは `style.css` 側が既に N9（Edge-aligned minimal）なのでそのまま使用。末尾に「料金を見る」（`index.html#pricing`）を1つだけ置き、本文に価格を書いていない＝**価格改定時にこのページは影響を受けない**。**④フッター導線**: `index.html`（Ft1）と `help.html` のフッターに「私たちについて」を追加。**法的3ページ（特商法・プライバシー・利用規約）のフッターは法的リンクのみの構成を保つため追加していない**（意図的）。**⑤Hallmark基準（捏造しない）の照合結果**: 数値の主張ゼロ（「導入◯社」「平均◯%改善」の類が無く、打消し表示の論点が発生しない）／第三者の名前・実績を借りていない（アイズコーポレーションにも触れていない）／経歴部分は**事業者本人の申告でリポジトリ側では検証不能**＝事実性はオーナー確認に依存する、と明記して採用／「AIを使いこなす知識が無くても」は利用者側に知識が要らないという文脈で、**製品がAI搭載だとは書いていない**（backend に LLM 呼び出しは実際に0件。grep確認済み）。**「AIが提案します」と断定的に書き換えないこと**（VISION.md の最終形に向かう途中であり、現時点では虚偽になる） | 2026-08-09 | 実装済み（`lp/about.html` 新規 / `lp/index.html`・`lp/help.html` のフッター / `lp/README.md` の構成一覧を実態に更新＝help.html が抜けており画像も旧名 `screenshot-*.jpg` のままだった）。**検証済み（ヘッドレスChrome・実ファイルをローカル配信して実測）**: LPはビルド工程が無い静的HTMLのため配信物で確認。確定文言3段落を1文字ずつ照合し全一致（243/123/74文字・全角ダッシュ「——」含む）／C案トーン適用（body `#fdfcf9`・文字 `#383731`・Zen Kaku Gothic New / h1 Zen Maru Gothic・罫 `#78927b`）／index.html と help.html の両方から**実クリック**で `/about.html` に到達（HTTP 200）／375px幅で横スクロール0・ボタン全幅・タップ領域55px、1280px幅でも横溢れ0・本文1行 544px（34em）／**ページHTML全体を検索して実名・「JuneOne」の出現0件・画像0枚**／コンソールエラー0|
| **セキュリティ残タスク3件の対応（npm audit更新／CSPヘッダー追加／consulting.py文字数上限）**: `security/security_check_2026-08-17.md` の継続指摘への対応。①npm audit（frontend）: nanoid/postcss/vite（high 3件）は `npm audit fix`（--forceなし）で解消。react-router/react-router-dom（moderate 2件）は脆弱性range `>=6.0.0 <7.18.0` でv6系にパッチが無く、修正には7.18.0以降へのメジャー移行が必須と判明したため**今回は見送り**（SSR不使用・遷移先は全て固定内部パスで悪用経路が薄いと判断。v7移行は別チケット）。②`backend/main.py` の `security_headers` にCSPヘッダーを追加。外部依存を棚卸しした結果、フロントが実際に外部と通信するのはGoogle Fonts（style-src/font-src）とSupabase（connect-src、認証API・Realtime）のみ（Stripeは `window.location.href` でのリダイレクトのみでscript-src不要）と確認。`script-src 'self'` のみ（unsafe-inline/unsafe-evalなし）、`connect-src` は `SUPABASE_URL`/`VITE_SUPABASE_URL` から実行時に導出（ハードコードしない。未設定のローカル開発は `'self'` のみで壊れない）、想定外の追加接続先用に env `CSP_CONNECT_SRC` を用意。`ENABLE_DOCS=1` 時の `/docs`/`/redoc`/`/openapi.json` はCDN読み込み前提のSwagger UI/ReDocのためCSP適用除外。③`backend/routers/consulting.py` の `InquiryPayload` に文字数上限を追加（`feedback.py` と同じサーバ側手動チェック方式に統一。name/company_name=200、scale_hint/contact_phone=100、message=5000）。フロント `ConsultingInquiryForm.tsx` にも同じ上限を `maxLength` で追加。**⚠️ 発見した問題**: 指示書が参照していた `security/security_check_2026-08-17.md` が本リポジトリの全ブランチ・全履歴に存在しなかった（`security/` ディレクトリ自体が本セッションで新規作成）。指摘内容は実コードと一致していたためチェック自体は実施されていたと考えられるが、成果物がコミットされないまま失われたパターン（本表ルール4の再発）。`security/index.md` に索引として記録した | 2026-08-18 | 実装済み（`backend/routers/consulting.py` / `frontend/src/components/ConsultingInquiryForm.tsx` / `frontend/package-lock.json` / `backend/main.py` / `security/index.md` 新規。3コミットに分割: `c5e38bc` consulting.py／`1a6fc7c` npm audit／`c9b3809` CSP）。**検証**: `npm run build` 型エラー0、`from main import app` OK、npm audit 5件→2件（残るreact-router系のみ）、ローカルuvicornでconsulting.pyの文字数超過→400・正常値→200を確認、ローカルで本番同様構成（backend単体配信＋frontend/dist）にしてヘッドレスChromeでダッシュボード/targets/import/billing/products/rppを巡回しCSP violation 0件・`document.fonts` でGoogle Fontsが `status: loaded` であることを確認、コンソールエラー0。**本番デプロイ確認済み（2026-08-18、[PR #16](https://github.com/Shoichiro12/rakuten-kpi-app/pull/16) マージ `61346e3`）**: `curl -i https://app.ureshiru.com/api/health` でCSPヘッダーの `connect-src` に実際のSupabaseオリジン（`https://yxkylmoqibmcsqtgdxkf.supabase.co` / `wss://…`）が入っていることを確認。ヘッドレスChromeで未ログイン状態のログイン画面・`/billing`アクセスでコンソールエラー・CSP violationとも0件、`document.fonts` でGoogle Fontsが `loaded`、Supabaseの公開エンドポイント（資格情報なし）へのfetchが `401`（＝CSPでブロックされずサーバーまで到達）を確認。**ログイン後の画面は資格情報入力を避けたため未確認**（オーナー自身の確認を推奨）。react-router v7移行の判断は別途必要（`security/index.md` に記録） |
| **react-router / react-router-domをv7.18.2へメジャー移行**: 上記npm auditの見送り分（react-router系）を解消。①事前調査でnpm audit検出が指示書想定の2件→着手時3件に増えていた（`GHSA-jjmj-jmhj-qwj2`が新規。react-router-dom単体のオープンリダイレクトXSS、range `>=6.30.2 <=6.30.4`）。v6系の新パッチは出ておらず、3件ともv7.18.0以降への移行でのみ解消可能と確認。②段階1: v6のままBrowserRouterに`future={{v7_startTransition, v7_relativeSplatPath}}`を追加し挙動不変を確認してからコミット。③段階2: `react-router-dom@7.18.2`へ更新（`react-router`も連動して7.18.2）。v7では`future` propが型エラーになるため削除。importは`react-router-dom`のまま維持（`react-router`への一括書き換えはスコープ外）。使用APIが`BrowserRouter`/`Routes`/`Route`/`useLocation`/`useNavigate`/`Link`/`NavLink`のみでデータルーターAPI（`loader`/`action`等）・`useParams`・`Navigate`は不使用だったため挙動変更なし | 2026-08-18 | 実装済み（`frontend/src/App.tsx` / `frontend/package.json` / `frontend/package-lock.json`。2コミットに分割: `61654d5` future flag／`f5ca258` v7更新）。**検証**: 前提条件（React 18.3.1が`>=18`要件、Dockerfileの`node:20-alpine`が`>=20.0.0`要件を満たす。CIはLP用のみでfrontend/ビルドと無関係）を確認、`npm run build`型エラー0、npm audit 3件→0件（フロント依存の残件すべて解消）、ローカル本番同様構成でヘッドレスChromeにより主要9画面のSPAナビゲーション・ブラウザ戻る/進む・直リンク（SPAフォールバック）・サンプルデータのダッシュボード表示・コンソールエラー/警告0を確認。**本番デプロイ確認済み（2026-08-18、[PR #18](https://github.com/Shoichiro12/rakuten-kpi-app/pull/18) マージ `f22a854`）**: 新JSバンドル配信（ハッシュ変化）を確認後、ログイン画面・パスワードリセット画面の表示、未ログイン状態での`/billing`・`/import`直リンク（ログイン画面へのフォールバック）、コンソールエラー0を確認。**ログイン後の画面巡回は資格情報入力を避けたため未確認**（オーナー自身の確認を推奨）。**⚠️ このリポジトリ側 `security/index.md` は、プロジェクトdocs側（リポジトリ外）に同名の索引がマスターとして既に存在することが2026-08-18判明した。次回セキュリティチェック時にプロジェクトdocs側と同期するか削除するかが判断される予定（`security/index.md` 冒頭に記録）** |

| **外部デザインレビュー2本（アプリUI・LP）への対応（計画書 `docs/jisso_keikaku_design_review_2026-08-20.md`）**: オーナー決定6点。**①GAP分析は「選択KPI基準」へ変更＝2026-08-04の「並び順は売上前期比で固定（蒸し返し禁止）」を正式に上書き。** 理由=顧客の実利用フィードバックで選択KPI基準のほうが良いと確認されたため（単なる蒸し返しではなく顧客検証を経た再決定）。KPI未選択時は従来どおり売上基準。**KpiActionHint（3つ目の打ち手表示先）は今回も作らない**（8/4の③は維持）②UIレビューは顧客体験が良くなるものを採用: ダッシュボード初期表示圧縮（アラート3件＋展開・グラフ/計画パネル折りたたみ）・商品別KPI一覧を判断用5列に圧縮（全指標は詳細パネルへ）・RPPは表示基準720h/12hの選択式（既定720h・集計の正は720hのまま不変）・データ取込みの個別取込/履歴削除/サンプルを折りたたみ・目標設定の試算をKGIカード直下へ・`text-[10px]/[11px]`→`text-xs`一括引き上げ。**アカウント設定の削除セクションは不採用**（既に赤枠区別済み・指摘が現状と不一致）③**LP見出し書体は角ゴシック主役へ＝2026-08-07「C案トーン維持」のフォント部分を上書き**（配色は維持・ワードマークのみ丸ゴシック残す）④LP導入事例の運用ストーリーは**保留**（許諾済み事実のオーナー提供待ち。位置はHTMLコメントで確保。捏造禁止）⑤shot-hero.jpg はサンプルデータで再撮影（KGIヒーロー＋今日やること主役）⑥LP5工程は骨格維持で強弱（2.0/4.0=画像つき主役、1.0/3.0/5.0=画像なし補助）。CTA主文言は「14日間無料で試す」に統一、`lp/style.css`（下層）はC案トーンへ | 2026-08-20 | 実装済み（本計画の同一ブランチ。検証結果は `docs/sagyou_houkoku_design_review_2026-08-20.md`） |

| **サンプルデータの分離（is_sampleフラグ）・アイテム別目標の削除UI・商品マスタ高速化（報告書 `docs/sagyou_houkoku_sample_data_safety_2026-08-20.md`）**: オーナー報告3件への対応。①**サンプルは実データと分離**: データ系10テーブルに `is_sample` 列を追加（migrations._EXTRA_COLUMNSで既存DBにも冪等追加）。`generate_sample_data()` の冒頭全削除を廃止し「サンプル分だけ入れ替え」に変更。**無条件の全削除に戻さないこと**（実データが消える事故報告が発端）。`DELETE /api/sample-data` 新設、`/api/reset-data` はサンプル残骸（目標・アイテム別目標・マスタ）も一掃（実データの設定は保持）。実データと管理番号が衝突する商品は生成スキップ（実データ優先）。**サンプル生成に新テーブルを足すときは必ず is_sample=True を付け、delete_sample_data() の対象にも加えること**②アイテム別目標の削除ボタンをUIに追加（DELETE APIは既存・未接続だった）③**`masters.get_review_queue()` は固定6クエリのプリフェッチ方式。商品ループ内で db.query を呼ばないこと**（旧実装はN+1で数千SKUだと表示に約1分。3000商品で107ms・提案内容は旧実装と全件一致を実測）。フロントは提案を一覧と切り離して並行取得＋「計算中」プレースホルダ。商品マスタ一覧に検索＋50件/ページのページング。④**旧サンプル（フラグ無し）は起動時マイグレーション `_mark_legacy_sample_rows()` が遡及認定する**（管理番号＋商品名/URLのカタログ完全一致のみ・冪等。targets と product_categories は誤認定リスクがあるため対象外＝無害な残置）。**判定基準を緩めないこと**（実データを巻き込む） | 2026-08-20 | 実装済み（backend: models/migrations/sample_data/main/masters、frontend: DataImport/TargetSetting/MasterSettings/api/types。検証は報告書参照） |

| **ダッシュボードのドリルダウン再設計（計画書 `docs/jisso_keikaku_dashboard_drilldown_2026-08-22.md`）**: ダッシュボード＝ドリルダウンの入口にする再設計。開いた瞬間に見えるのは「予算 vs 売上」だけ（段1 HeroKgi）。未達のときだけ「詳しく見る」で 段2要因（KpiTriage）→ 段3ジャンル（GenreDrill・粒度切替）→ 段4商品（ProductDrill）→ 段5アクション（ActionRx）に掘れる。判定・並び順・打ち手のロジックは既存GAP分析資産（`components/gap/kpiGap.ts` / `actionLibrary.ts`）をそのまま流用し、バックエンド改修なし。確認事項Q1〜Q5はオーナー承認済み（Q1=GAP分析ページは当面残す／Q2=「今日やるべきこと」は撤去し件数バッジのみ段1に残す・推移グラフ/計画パネルは折りたたみのまま維持／Q3=達成時も「達成していますが内訳を見る」の薄いリンクを残す／Q4=ワードマードもBIZ UDPGothicに統一／Q5=週次は「目標（週按分）」と表示ラベルに明記）。デザイントークン表はCLAUDE.md「デザイントークン」節参照。**HeroKgiのバーはBulletChart（弾丸グラフ）を使わず、按分目標=100%の単純な進捗バー**（本番目視でBulletChart流用時にインラインラベル衝突・軸の誤読が発覚し作り直した。BulletChart.tsx自体は削除せず残置）。**週次目標按分は `backend/period_utils.py` の `prorate_weekly_target_field()` が単一の真実**（KPI評価マトリクス `routers/evaluation.py` の `/matrix` `/access-plan` と `routers/dashboard.py` の週次 `target_sales` が同じ関数を呼ぶ。月をまたぐ週は各日が属する月の目標を日割り合算。**新しく週次目標を出す場所を足すときは必ずこの関数を呼び、独自に按分計算しないこと**＝フロントとバックエンドで計算式が食い違い同一画面で達成率が2種類出た実例あり）。月次・年次はフロント（`Dashboard.tsx`）で経過割合按分（現状維持）。**Dashboard.tsx の `load()` は period/dateValue を ref で比較するstale-response guardを持つ**（`usePeriodState` がマウント後にdateValueを自動更新するため、初回表示時に旧date・新dateの2回loadが競合し、ネットワークタイミング次第で旧dateの結果が新dateを上書きするレースコンディションが実際に発生した。**新しいAPI呼び出しをload()に足すときもこのガードの外に出さないこと**） | 2026-08-22 | **区切り0〜5＋追加修正3件すべて本番反映・オーナー目視完了**。区切り0〜5: `frontend/tailwind.config.js` トークン追加・`components/diagnosis/{HeroKgi,DrillDown,KpiTriage,GenreDrill,ProductDrill,ActionRx,Breadcrumbs}.tsx` 新規・`pages/Dashboard.tsx` 置き換え・`components/dashboard/TodayActions.tsx` 削除・商品別KPI/RPP/データ取込み/商品マスタ/目標設定のgray/blue系トークン移行（PR #22〜#25）。追加修正: ①HeroKgiのグラフ可読性（弾丸グラフ→進捗バー、PR #26）②週次按分の統一（PR #27）③期間表示のレースコンディション（PR #28）。**本番目視（オーナー+Claude、2026-08-22）で最終確認済み**: 週次（達成率69.7%=KPI評価マトリクス69.7%で完全一致・目標¥1,129,032）・月次（48.2%・月按分354.8万円・軸出し分け=アクセス人数UU正常）・年次（74.8%・回帰なし）。**別チケット（未着手・オーナー確認済みで急ぎではない）**: 月次画面に売上の達成率が3種類並ぶ（ヒーロー48.2%=月按分／KPI評価マトリクス34.2%=フル月目標比／アラート45.7%）。どれも按分基準ラベルが付いており誤りではないが、週次で統一したのと同種の混乱の種。統一するなら「画面内の売上%は按分基準に統一」という複数エンドポイントにまたがる設計判断が必要（週次のような単純な統一先が無い）。着手するときはこの3箇所（`HeroKgi.tsx`のproratedTarget・`routers/evaluation.py`の`/matrix`・`routers/dashboard.py`の`/alerts`）がそれぞれ何を分母にしているかの整理から |

| **マスタ設計の統一（目標・商品・カテゴリ）＋ツールチップ修正（計画書 `docs/jisso_keikaku_input_ia_seiri_2026-08-22.md`）**: 3つのマスタ（目標マスタ=targets／商品マスタ=products／カテゴリマスタ=product_categories新設）をDB構造ベースで統一設計。区切り構成は①ⓘツールチップのPortal化②バックエンドのマスタCRUD規約整備（archived_atソフトデリート・CSV export/import全マスタ整備、規約は本ファイル「マスタCRUD規約」節参照）③EditableGrid共通フック（`components/grid/useEditableGrid.ts`）をアイテム別目標の一括編集に実戦投入④カテゴリマスタ独立ページ新設⑤商品マスタの4タブ化（商品／アイテム別目標／ベンチマーク／未確認の提案）＋商品削除UI⑥目標マスタの12ヶ月グリッド化＋計画パネルのダッシュボードからの統合⑦導線・文言QA。各区切りでオーナー承認済み（Q1=削除は1件ずつソフトデリート・全削除のみConfirmDeleteModal必須／Q2=アイテム別目標は現行機能のまま商品マスタへ移設／Q4=カテゴリマスタはサイドバー独立／Q5=計画パネルはダッシュボードから撤去し目標設定へ統合／Q6=グリッドは自作EditableGridで十分・Excelコピペは第2段階／Q7=商品の状態はUIには「販売中/廃盤」の2値のみ、削除は内部archived_atで実装しUIに「アーカイブ」の語を出さない） | 2026-08-22 | **区切り1〜7すべて本番反映済み**（PR #30〜#36）。区切り1=`KPIHelp.tsx`をcreatePortal化しクリップ解消。区切り2=`products`/`product_categories`/`targets`にarchived_at追加、`inactive_management_nos()`へ集約、Target参照10箇所以上に個別archived_at条件追加、`DELETE /api/master/products/{no}`新設、カテゴリ・目標マスタのCSV export/import新設（`routers/masters.py`・`routers/targets.py`）。区切り3=`useEditableGrid`（pending/isDirty/dirtyRows/bulkSave）を新設しTargetSetting.tsxのアイテム別目標編集を置換、ヘッドレスChromeで動作等価を実測。区切り4=`pages/CategoryMaster.tsx`新設（`/master/categories`）、商品マスタからカテゴリ管理UI撤去。区切り5=`MasterSettings.tsx`を4タブ化しアイテム別目標セクションをTargetSetting.tsxから丸ごと移設（API無変更）、商品削除ボタン＋ConfirmDeleteModalを追加しNEW-001サンプル残骸を実際にUI経由で削除できることを確認。区切り6=TargetSetting.tsxの単月フォームを12ヶ月グリッド（`plan.months`の予算年度をそのまま流用・フロントで年度計算を複製しない）に置換、`RevenuePlanPanel`/`AccessPlanner`を`components/dashboard/`から`components/targets/`へ移設しDashboard.tsxから計画パネルを撤去。区切り7=`lp/help.html`の画面ガイド・期間の使い分け・目標の設定セクションを新IAに合わせて更新（「目標設定の3階層」→「目標の設定」、カテゴリマスタ行追加、計画パネル関連のダッシュボード言及を削除）、全10画面のヘッドレスChrome回帰でエラーボーダリー・コンソールエラー0を確認。**target_sales（KGI・目標マスタ）と target_sales_budget（年間予算按分の手動補正・`POST /api/revenue-plan/override`専用）は引き続き別軸**＝区切り6のグリッド化でもこの分離規約は変更していない。 |

| **LPをエッセイ型に全面刷新（計画書 `docs/jisso_keikaku_lp_essay_2026-08-23.md`／正モック `docs/mockups/ureshiru_lp_proposal_v2_2026-08-22.html`）**: チャットでのデザイン検討（v1→v2）を経てオーナー承認済みの内容。620px一本コラムで「月曜の朝」を追体験させる構成に全面刷新し、旧「5つの工程」セクションは廃止（内容はエッセイ本文に吸収）。ヒーローは静止画スクショではなく、新ダッシュボードの予算vs売上→KPI分解→商品アクションの2段ドリルをvanilla JSで再現した「触れるライブデモ」。書体をアプリと統一（本文BIZ UDPGothic／数字Inter。Zen Maru Gothic・Zen Kaku Gothic Newはワードマーク含め`index.html`で廃止。**`lp/style.css`＝下層ページ（about/help/privacy/terms/tokushoho）は今回スコープ外のため未変更**、旧書体のまま）。配色はC案トークン維持。「安い」の独立セクションは置かず、料金ブロック下に1行のみ（1日¥733＝RPP広告クリック約7回分）。デモ・本文の打ち手例は本番アプリの実出力に準拠（架空のアクション文言は使用禁止）。SEO/AI検索対応としてJSON-LDをFAQPage込みの2本に拡張し、FAQPageスキーマとページ上のFAQ（5問）は一言一句完全一致させる規約。`llms.txt`をサイトルート新設（JSON-LDのdescription・featureList・FAQ・料金を平文化。新情報の創作禁止）。**現行LPから流用・維持したもの**: 導入事例（実際に開示許可済みの実績値。中身は現行のまま）・フッター（法的ページへのリンク一式）・CTA遷移先（`https://app.ureshiru.com/billing?signup=1`）・GA計測タグ。**モックにも計画書にも明記が無く実装判断で追加/削除したもの**: ①アクセシビリティのスキップリンク（`本文へスキップ`）は現行機能を維持のまま追加②og:imageは新ヒーロー用画像が無いため暫定で旧ヒーロー画像(`shot-hero.jpg`)を流用しTODOコメントを残した（1200×630での撮り直しは別タスク）③「ECコンサルティング」upsellブロックと「そのほかの画面」の仕様表はモック・流用リストいずれにも無かったため削除（エッセイ構成への一本化を優先）。 | 2026-08-24 | 実装済み・本番反映済み（[PR #37](https://github.com/Shoichiro12/rakuten-kpi-app/pull/37)、mainマージ`a8a4d02`）。**検証済み**: JSON-LD 2本の構文OK・FAQPage文言とページ上FAQ5問の完全一致をスクリプトで実測、主要タグ（div/a/header/main/footer/details）の開閉数一致、ヘッドレスブラウザでライブデモの2段展開（「詳しく見る」→KPI行クリック→打ち手表示）と「最初から見る」リセットの動作確認、375px幅・1280px幅とも横オーバーフロー0、BIZ UDPGothic(400/700)・Inter(600/700)が実際に読み込まれ描画に使用されていることを確認、コンソールエラー0。**⚠️ 一度、実装が本番未反映のまま「区切り1が飛ばされた」と誤認される事故が発生した（2026-08-24）**——原因は実装漏れではなく、作業ツリー内の変更をコミット・push・マージするまでの承認待ちだった段階でオーナーが本番URLを直接検証したこと。**教訓: 実装完了の報告と本番反映（コミット・push・マージ）は別のマイルストーンとして明示的に区別して伝えること。** その後コミット・push・PR作成・mainマージまで完了。マージ後の本番目視（オーナー確認）は別途実施予定——`/llms.txt`が実ファイルとして`text/plain`で配信されるか、Cloudflare PagesのSPAフォールバックでHTMLが200返却されないかを含めて確認すること。 |

| **「ウレシル社」サブエージェント体制の導入（`filesv4.zip` 経由でオーナーが提供・README `.claude/README.md` の組織図に従い導入）**: 経営企画室(pmo)・設計(planner)・実装(developer)・レビュー(reviewer)・品質保証(qa)・セキュリティ(security)・デザイン(designer)・データ分析(kpi-analyst)・法務経理(legal-finance)・カスタマーサクセス(customer-success)・インフラ運用(infra-ops) の11サブエージェント（`.claude/agents/`）と、`/kickoff` `/jisso-keikaku` `/release-check` `/security-check` `/price-check` の5定例スキル（`.claude/skills/`）を追加。**導入時に判明した点**: 旧CLAUDE.mdが参照していた `backend-engineer`/`frontend-engineer`/`data-analyst`/`qa-debugger` の4エージェントはこのリポジトリのどのブランチ・worktreeにも実体が無い「幽霊参照」だった（申し送りルール6が警告する事故の実例。該当箇所は本ファイル「`.claude/agents/`」節に訂正済み）。**`.claude/skills/` はリポジトリの `.gitignore`（18-19行目）で追跡除外**なので、新設した5スキルの `SKILL.md` はこのマシン上のみ有効でコミットされない（`.claude/agents/` は無視対象外なのでコミットされる）。**別環境でこの体制を使うには `.claude/skills/{kickoff,jisso-keikaku,release-check,security-check,price-check}/` を都度コピーする必要がある**（design-systemスキルと同様の運用）。 | 2026-08-24 | 実装済み（`.claude/agents/*.md` 11ファイル・`.claude/skills/{5スキル}/SKILL.md`・`.claude/README.md`・本ファイルへの追記。動作確認はセッション開始時のスキル一覧に5スキルが認識されたことのみ実測、11エージェントの実際の呼び出し動作は未検証） |

| **週次セキュリティチェック（2026-08-24）の新規指摘2件（バックログ・優先度: 中/低）**: ①CSVエクスポート（`backend/routers/masters.py` の商品・カテゴリexport、`backend/routers/item_targets.py` のexport、既存 `backend/routers/export.py`）にCSVインジェクション（数式注入）対策が無い。既存 `export.py` の未対策パターンが今回のマスタ設計統一（区切り2〜5）で3ルーター分に広がった。重大度「中」。**テナント分離（`UserScopedMixin`）により影響は「自分自身がエクスポートしたCSVを自分で開いたとき」に限定される**ため緊急対応は不要と判断（`security/security_check_2026-08-24.md` の結論）。対応するときは共通ヘルパー（`csv_safe_cell()`等）化を推奨——セル値が `=`/`+`/`-`/`@` で始まる場合に先頭へシングルクォート等の無害化を挟む方式。②新設CSVインポート3本（カテゴリ・目標・アイテム別目標）にファイルサイズ上限が無い（重大度「低」・既存の商品マスタCSVインポート等と同じ既存パターンの踏襲、`_paid`ガード配下のため悪用インセンティブは低いと判断）。**出典**: `security/security_check_2026-08-24.md`（結論サマリ・詳細）、`security/index.md`（未解決の指摘事項テーブル・実施記録） | 2026-08-24 | 未実装（バックログ。詳細は `security/index.md`・`security/security_check_2026-08-24.md` 参照） |

| 社内マップ `docs/office_map.html` を導入、作業報告時の更新をルール化 | 2026-08-24 | 実装済み（`docs/office_map.html`。家臣団＝サブエージェント体制と残作業＝軍令帳を可視化する自己完結HTML。ブラウザで家臣札クリック→評定の間に肖像・口上・知略武勇バー・采配コマンドが表示されることを実機確認済み。更新ルールは本ファイル「🏢 社内体制」節に追加） |

| **render.yaml削除（実在しない旧サービスの定義）、Render目視結果を記録**: オーナーがRenderダッシュボードを目視確認した結果、①稼働中のサービスは`rakuten-kpi-app-sg`（Singapore）1件のみで、旧Oregonサービス`rakuten-kpi-app`は存在しない②`rakuten-kpi-app-sg`のプランはStarter（画面バッジで確認）。**CLAUDE.mdインフラ申し送りの記載（Starter・Singapore）が正しく、`render.yaml`（`plan: free`・サービス名`rakuten-kpi-app`）の方が実態と食い違った古い定義だった**と確定。Blueprint運用はしていない（サービス名が食い違っていたことからも、手動作成されたサービスと判断）ため、`render.yaml`は削除。削除前に参照箇所をgrepし、`docs/office_map.html`（今回あわせて更新）と`DEPLOY.md`のステップ3（Blueprint前提の手順）の2件を確認。`DEPLOY.md`は「New → Blueprint」を「New → Web Service」に書き換え、手動作成の実態に合わせた注記を追加 | 2026-08-24 | 実装済み（`render.yaml`削除・`DEPLOY.md`修正・`docs/office_map.html`更新・本ファイルへの追記）。**確認事項は解消済み（2026-08-24）**: PR #39マージ後のRender Auto-Deployが緑チェックで正常完了したことをオーナーがダッシュボードで確認。`render.yaml`削除はデプロイ設定に影響しなかった |

| **旧Vercelプロジェクト削除を記録、軍令帳を更新**: Vercelの旧プロジェクト2件（`ureshiru` / `ureshiru-lp`）をオーナーが削除。`ureshiru.com`・`app.ureshiru.com`の稼働に影響なしを確認。Stripeダッシュボードに警告・追加情報要求等の表示なしもあわせて確認（オーナー目視。Claude Codeはダッシュボード類にアクセス権限が無く未確認）。`docs/office_map.html`の軍令帳から「PR #39御裁可のこと」「旧Vercel廃城のこと」の2枚を除去し、STATUSの「評定」を`議案なし`に更新（残り: 急務1枚=CSVインジェクション対策、後日2枚=四万SKU実測・専門家確認） | 2026-08-24 | 実装済み（本ファイル「旧Render(Oregon)・旧Vercelプロジェクトの削除」行を完了に更新・`docs/office_map.html`更新）。ブラウザ実機で軍令帳3枚・評定「議案なし」・コンソールエラー0を確認 |

| **定期実行の仕組み（夜勤体制）を導入**: Claude Code Routines（無人・定期実行のクラウドエージェント）で①週次security-check（月曜朝）②夜勤（平日夜・軍令帳の急務を1件だけ進めてPRを作る）の2本を運用する方針を決定。CLAUDE.mdに「🌙 夜勤の掟」節を新設（mainへ直接pushしない・外部ダッシュボードに触れない前提を明記・オーナー判断が要る事項はPR本文「評定待ち」節に列挙して止まる・1回で急務1件まで・成果物に作業報告と軍令帳更新を含める）。夜勤の実行プロンプトは`.claude/commands/yakin.md`（**初回運用は「調査・ドキュメントのみ、コード変更禁止」から開始し、PRの質を見てから段階的に緩める**とオーナー承認済み）。**導入作業中に重要な発見**: `RemoteTrigger`で既存routineを確認したところ、2026-07-29から**Coworkベースの週次セキュリティチェックroutineが既に稼働中**だった（8/3・8/10・8/17・8/24と4回実行済み）。GitHubリポジトリを読み取り専用でcloneし、成果を**このリポジトリではなく「sellerhub」という別Claude.aiプロジェクトのドキュメント機能**（`project_write`）に保存する設計で、PR作成・コミットは一切行わない。**これが`security/index.md`冒頭に長らく記録されていた「プロジェクトdocs側に同名のマスター文書が別途存在する（2026-08-18判明・正体不明のまま放置）」の正体だったと確定した。** リポジトリ側の運用（このセッションで手動運用してきた`security/`ディレクトリ）と役割が重複するため、オーナー確認のうえ`RemoteTrigger`の`update`で`enabled:false`にして無効化した（削除はUI操作が必要なため未実施） | 2026-08-24 | 実装済み（CLAUDE.md「🌙 夜勤の掟」節・`.claude/commands/yakin.md`）。**動作確認は確認済み（2026-08-24）**: `RemoteTrigger`で2本を作成し`Run now`でテスト実行。1回目は①`.claude/commands/yakin.md`がmain未マージのため夜勤が`Unknown command`で即終了②GitHub App連携が組織側で外れておりsecurity-checkがコミットまで完了してもpush・PR作成に403で失敗、という2つの問題を検出（テストの目的どおり）。オーナーがGitHub App再連携・PR #40/#41マージの対応後、再実行して両方とも成功を確認: 夜勤は初週スコープ（コード変更禁止）を守り`docs/`配下のみで[PR #42](https://github.com/Shoichiro12/rakuten-kpi-app/pull/42)、security-checkは高0件で[PR #43](https://github.com/Shoichiro12/rakuten-kpi-app/pull/43)（CI green・mergeable）。**⚠️ 手順上の反省（2026-08-24、下記行も参照）**: 既存Coworkルーチンの無効化を、蓄積された4週分の成果物の中身を確認する前に実行してしまった。AskUserQuestionでの確認自体は経ていたが、オーナーから「稼働中の仕組みを止める判断は、中身を見てからにすべきだった」「『確認させてください』と言ったら実行前に本当に止まること」と指摘を受けた。無効化の結果自体はオーナーも妥当と判断したが、プロセスの順序を誤った事例として記録する。**⚠️ 新たに観測された挙動→対応済み（2026-08-24）**: 両ルーチンともPR作成後、自発的に「1時間後にPRの状態を再確認する」`send_later`タスクを追加作成していた（PR #42用`trig_01WXKNkG1238XepQqkPdCHhK`／PR #43用`trig_01NefPWSXhoE82o3ao1sCrJY`）。プロンプトが「問題なければ黙って再アーム」としており、マージされるまで無期限に1時間おきで繰り返される設計だった。オーナー判断で両方`enabled:false`にして停止（詳細・再発防止は下記行参照） |

| **ルーチンの自己増殖的タスク作成を禁止する再発防止を実施**: 夜勤・security-checkの初回テストで見つかった「PR作成後に自分で1時間おきのフォローアップタスクを作成する」挙動（`trig_01WXKNkG1238XepQqkPdCHhK`／`trig_01NefPWSXhoE82o3ao1sCrJY`）は、オーナー判断で両方`enabled:false`に停止した。理由: ①掟の思想（決めた範囲でだけ動く）に反する②PRマージ待ちの監視主体はオーナーであり無人ルーチンが1時間おきに見てもできることがない・実行回数のプラン上限を無駄に消費するだけ③「問題なければ黙って再アーム」という設計は無期限に静かに走り続け、7項目目の「7/29から気づかれず動いていたCoworkルーチン」と同じ構図を自ら再生産しかけていた。再発防止として**CLAUDE.md「🌙 夜勤の掟」に8番目の項目を新設**（ルーチンは新しいスケジュール・フォローアップタスクを自分で作らない、続きが必要ならPR本文に書いて止まる）、**`.claude/commands/yakin.md`の手順11に同趣旨を追記**、**Routines側の週次security-checkプロンプトにも`RemoteTrigger`の`update`で同じ1文を追記**（cron・次回実行日時は変更なし） | 2026-08-24 | 実装済み。フォローアップタスク2本の無効化を実施・レスポンスで`enabled:false`を確認。CLAUDE.md・yakin.md・Routinesプロンプトの3箇所とも追記済み。**次回検証**: 来週月曜（2026-08-31）のsecurity-check自動発火時に、フォローアップタスクを作らずに1回で完結することを確認する |

| **夜勤に「巡回」（発見専用フェーズ）を追加**: 夜勤を前段=普請（従来の急務対応）・後段=巡回（発見のみ）の2段構成に改訂。巡回は普請の有無にかかわらず毎晩実施し、コード・ドキュメント・台帳を見回って問題を発見するが**修正はしない**。ルール: ①発見のみ・修正しない②1晩3件まで③各候補に「何が・どこで（ファイルと行）・なぜ問題か・放置するとどうなるか」を必須記載④既存の軍令（QUESTS）・候補一覧・却下済み・台帳記載と重複するものは挙げない⑤候補は`docs/office_map.html`のQUESTSに新設した`stamp:"kouho"`（候補・灰色系の判子、急務=朱色・後日=藍色とは別に視覚区別）として積み、詳細は新設`docs/gunrei_kouho.md`（候補一覧＋却下済みの2表）に書く⑥候補の昇格・却下はオーナーのみ、夜勤はkouho札に手を出さない。CLAUDE.md「🌙 夜勤の掟」に9番目の項目として明文化 | 2026-08-25 | 実装済み（`docs/office_map.html`のCSS/JS・`docs/gunrei_kouho.md`新規・`.claude/commands/yakin.md`の2段構成化・CLAUDE.md掟9番目）。**検証済み**: kouho札の判子ラベル分岐をNode.jsで実測、ブラウザ実機で急務(朱)・後日(藍)・候補(灰)の3色が視覚的に区別されることを確認。**未検証**: 実際の夜勤ルーチンによる巡回の初回実行（次回発火は平日23時JST） |

| **LPの「1店舗・1アカウント」表記と利用規約の整理（ウレシルLP修正指示書2026-08-25対応）**: 実際のアプリは店舗1つ・アカウント1つの構成のため、`lp/index.html`と`lp/llms.txt`から事実と異なる「ユーザー数無制限」表記を計6箇所（指示書記載2箇所＋JSON-LD 2箇所＋llms.txt 2箇所、legal-financeの全件grepで発見）削除し「1店舗・1アカウントでご利用いただけます」に置換（PR #46・本番反映済み）。この新表記が`lp/terms.html`第2条2項「アカウントの管理責任は利用者が負うものとし、第三者への貸与・共有はできません（**同一店舗内の複数ユーザー利用は可能です**）」と字面上重なる懸念が最終照合で浮上した。**最終結論（2026-08-25・オーナー判断で確定）: 共有利用は規約上可能だが、LPでは訴求も明示もしない方針。** 「1店舗・1アカウント」＝発行アカウント数の話、「同一店舗内の複数ユーザー利用」＝その1アカウントを店舗内で共有してよい許諾の話で意味のレイヤーが違い矛盾ではないため、**規約(`terms.html`)・LP表記ともこのままで変更不要**。LP側に「（同一店舗内であれば複数のスタッフでの共有利用が可能です）」を明示する補足は**入れないことで確定**（規約上可能なことをわざわざLPで訴求する必要はないというオーナー判断）。**経緯の記録（重要）**: 一度はこの補足を追加する方針でPR #47を作成・**マージまで完了させてしまった**が、直後にオーナーが「書かない方針」へ再判断し、PR #47の内容（6箇所の補足文言）を別PRで打ち消した。**教訓: 「マージ前に確認できた」という前提は、実際にマージ済みだったため誤りだった。** 判断が固まるまでは、たとえ軽微なLP文言でもマージ前に一呼吸置いて確認する重要性を示す事例として残す | 2026-08-25 | **最終状態は実装済み**: `lp/index.html`・`lp/llms.txt`とも「ユーザー数無制限」削除＋在庫アラート追記（PR #46）のみが本番反映され、共有利用の補足（PR #47の内容）は別PRでrevertし本番には出ていない。**参考記録**: legal-financeから語彙を`terms.html`側（「ユーザー」「複数ユーザー利用」）に寄せる代替案（例:「同一店舗内であれば複数のユーザーでご利用いただけます」）も提示されたが、そもそも補足自体を入れない方針が確定したため不採用。**将来の巡回対策**: この件を`docs/gunrei_kouho.md`の却下済み節に記録し、夜勤の巡回が同じ「LPと規約の表記の重なり」を候補として再提案しないようにした |

| **Coworkルーチン4週分（2026-08-03〜08-24）の突き合わせで判明した記録漏れ**: 無効化前にオーナー指示で、無効化したCoworkルーチンが「sellerhub」プロジェクト側に残していた4週分のセキュリティレポート（`RemoteTrigger`の`get_run_log`で全件取得）と、リポジトリ側`security/`・本ファイルを突き合わせた。**① 最重要・記録漏れ**: 2026-08-03にこのCoworkルーチンが重要度「高」の脆弱性（`backend/main.py`のSPA配信フォールバックが未認証パストラバーサルに脆弱。`%2e%2e`エンコードで`/etc/passwd`を読み取れることを実機確認・オーナーへプッシュ通知）を発見し、同日中に別セッションが`c4ea5b7`（2026-08-03 11:39 JST、コミットメッセージに経緯の記載あり）で修正済みだった。**この発見〜修正の経緯が本ファイルの申し送り台帳に一度も記録されていなかった**（実装は残ったが根拠が消えていた、申し送りルール4が警告する事故そのもの）。現在の対策コード（`backend/main.py`の`_serve_spa`・`realpath`によるパス封じ込め）は健在で退行なしを確認済み。**② 実害あり・現在進行形の不整合として発見**: 同じ`c4ea5b7`で`EXEMPT_TEST_EMAILS`の既定値を`test@gmail.com`→空文字に変更していたが、本ファイル53行目は3週間「既定 test@gmail.com」のまま誤った記述が残っていた（上記の行で訂正済み）。**③ 軽微**: 同コミットの「中」（`global_exception_handler`の`EXPOSE_ERROR_DETAIL`対応）・「低」（`supabase_rls_fix.sql`のプロジェクトref削除）も本ファイルへの記録は無かったが、実装は健在・実害小さいため訂正のみで対応済みとする。**それ以外の週（7/29・8/10・8/17・8/24）の指摘は、npm audit・CSPヘッダー・コンサル文字数上限・CSVインジェクション等いずれもリポジトリ側`security/`・本ファイルの記録と一致しており、差分なし。** 7/29初回チェックの「高」1件（Stripe Webhook署名検証未設定）は本ファイル既存行（2026-07-30確認済み）と一致 | 2026-08-24 | 突き合わせ完了・記録漏れ2件を本ファイルへ反映（本行・EXEMPT_TEST_EMAILS行）。**`security/index.md`側への遡及反映は不要と決定（2026-08-24）**: 理由は`docs/security_taiou_2026-08-03.md`（作業報告。`security/`配下のチェック報告書ではない）に同じパストラバーサル脆弱性（発見・実機再現・`c4ea5b7`修正・再検証）が既に独立記録されており、欠けていたのは「Coworkルーチンが先に発見・通知していた」という発見経路の情報のみで、それは本ファイルへの記録で十分なため。`security/index.md`は「git側の週次チェック実施記録」という役割を保ち、別系統の履歴を混ぜない。**⚠️ 2026-08-26訂正: 上記で`security_check_2026-08-03.md`と書いたのは誤り。** この名前のファイルは全ブランチ・全履歴に一度も存在しない（`git log --all --diff-filter=A --name-only`で確認）。実在するのは`docs/security_taiou_2026-08-03.md`のみで、当時この2つを混同したまま記録していた。詳細・発覚の経緯は下記「Cowork納品パッチ適用前の事実確認」行を参照 |

| **【宿題】sellerhub側に残る旧「security/index.md」マスター文書の扱い**: 無効化したCoworkルーチンが`project_write`で書き続けていたsellerhub側のドキュメント（`security/index.md`・`security/security_check_YYYY-MM-DD.md`×4）は、ルーチン停止後も**削除・注記なしのまま残存**している。将来別のセッションがこれを見つけると、また「これは何だ」の調査が発生するおそれがある。**理想は文書冒頭に「Coworkルーチン時代の遺物・正はgit側`security/index.md`・参照不要」と注記を入れること**だが、sellerhub側のドキュメント編集（`project_write`/`doc_edit`相当）は今回のClaude Codeセッションのツールセットに無く実施できなかった。次にCowork側のセッション（`project_write`が使える環境）が動く機会があれば、その冒頭注記の追加を検討すること | 2026-08-24 | **未実施（宿題として記録のみ）**。対応するツール・セッションが無いため、今回は本ファイルへの記録で代替 |

| **CSVインジェクション対策（`security_check_2026-08-24.md`の中優先度指摘）の修正方針を下書き**: 夜勤（無人・定期実行）の初回タスク消化。現行スコープ（コード変更禁止）のため実装はせず、影響範囲の実地調査（`masters.py`商品名/カテゴリ名・`item_targets.py`商品名の3箇所が未サニタイズ。`export.py`の共通ヘルパー`_csv_response()`は他3ファイルと共有されていないことを確認）と、共通ヘルパー`backend/csv_utils.py`の`csv_safe_cell()`による対策方針を計画書化した | 2026-08-25 | **計画書のみ・未実装 → 評定確定済み（次行参照）**。当時のオーナー確認事項3点（targets.pyを対象に含めるか／マスタCRUD規約への追記文言／既存データへの遡及対応が不要か）は`docs/jisso_keikaku_csv_injection_fix_2026-08-25.md`「評定結果」節を参照 |

| **CSVインジェクション対策の評定確定、実装フェーズへ**: 上記計画書のオーナー確認事項3点、**3点とも夜勤の推奨どおりで確定**。判断理由: ①`targets.py`は対象から外す——数値列のみのエクスポートに変換を通すのは無意味な処理追加で、「一律で入れる」は後で「なぜここに入っているのか」が分からなくなる種になる。実装時は該当箇所に「自由入力列を追加する場合は`csv_safe_cell()`を通すこと」とコメントを残す②マスタCRUD規約への追記文言を確定: 「CSVエクスポートで文字列セルを出力するときは必ず`csv_safe_cell()`を通す。対象は`=` `+` `-` `@` タブ・CR で始まる値の無害化。数値のみの列は対象外だが、自由入力列を追加した時点で必須になる。」——「なぜやるか」と「いつから必須になるか」を1文に入れることで将来の実装者が迷わないようにした。**実装と同じPRで追記する**（規約と実装が別PRだと片方だけマージされる事故があり得るため）③遡及対応は不要——対策の主戦場はエクスポート時の変換であり、DBの値は単なる文字列。DB側を書き換えると「ユーザーが入力した値と表示が違う」という別の問題を生む。既存エクスポート済みファイルはユーザーの手元にあるものなので触れる対象ではない | 2026-08-26 | **評定確定・実装可**（`docs/jisso_keikaku_csv_injection_fix_2026-08-25.md`「評定結果（確定・2026-08-26）」節に反映済み、`docs/office_map.html` QUESTSの表示も「評定待ち」→「評定確定・実装可」に更新）。**実装は次回の夜勤（普請）が計画書どおりに行う**。マスタCRUD規約への1文追記はこの評定記録では行わず、実装PRで同時に追記する（上記の事故防止の判断どおり）。**→ 2026-08-27夜勤で実装完了（次行参照）** |
| **CSVインジェクション対策の実装完了**: 上記評定確定を受け、夜勤（普請）が計画書どおりに実装した。新規 `backend/csv_utils.py`（`csv_safe_cell()`）を追加し、自由入力の文字列セル（商品名・ジャンル名）を書き出す4箇所に適用: `routers/masters.py`（カテゴリマスタexportの`genre_u1/u2/u3`、商品マスタexportの`product_name`・カテゴリ3列）／`routers/item_targets.py`（アイテム別目標exportの`product_name`）／`routers/export.py`（`export_products`の`product_name`・`genre`）。`routers/targets.py`の`export_targets`は計画どおり対象外とし、docstringに「自由入力列を追加する場合はcsv_safe_cell()を通すこと」を明記。マスタCRUD規約（CLAUDE.md）に確定文言をそのまま追記（同じPR） | 2026-08-27 | 実装済み（`backend/csv_utils.py`新規／`backend/routers/{masters,item_targets,export,targets}.py`／CLAUDE.md マスタCRUD規約節）。**検証**: `from main import app` OK。`csv_safe_cell()`単体を11ケース（`=`/`+`/`-`/`@`/タブ始まり・通常の日本語商品名・空文字・None・数値・負数）で実測し全件PASS。ローカルuvicorn起動でカテゴリ名`=1+1`・商品名`=cmd|/c calc`・ジャンル名`=HYPERLINK("http://evil")`を実データ投入し、4エンドポイント（`/api/master/categories/export`・`/api/master/products/export`・`/api/item-targets/export`・`/api/export/products`）すべてでCSVの該当セルが`'`前置で無害化されていることを生バイトで確認。通常の日本語カテゴリ名（`通常ジャンル`）は無変換のままでラウンドトリップ保証を壊していないことも確認。**本番実測完了（オーナー、2026-08-27）**: 本番のカテゴリマスタに `=1+1` のテスト行を作成しエクスポートしたCSVで、該当セルが `'=1+1`（シングルクォート前置）に無害化されていることを確認。Excelで開いても数式として発火しないことも確認。検証用のテスト行は確認後に削除済み。**これで実装完了に加え本番での効果確認まで完了、指摘は正式にクローズ**（`security/index.md`の該当行も解決済みへ更新） |

| **夜勤の初回巡回（PR #49）で見つけた候補2件をオーナーが急務へ昇格。あわせて夜勤の初週スコープ制限を解除**: ①`lp/about.html`の「料金を見る」リンク切れ（`index.html#pricing`が実在しない。実害あり・優先）②`lp/README.md`の陳腐化（工程画像shot-1〜5の使用状況・旧Vercel削除の記述が実態と不一致。優先度低）。`docs/gunrei_kouho.md`候補一覧の状態を「昇格済み」に更新し、`docs/office_map.html` QUESTSのstampを`kouho`→`wait`へ変更（①は実装まで進められるようCSVインジェクション普請より前に並べ替え、②はその後ろに追加）。**夜勤の初週スコープ制限は2026-08-26に解除**（巡回・普請とも1週間の挙動が安定したため——普請はCSVインジェクション対策の調査・計画書化を完走、巡回は初回で新規候補2件を発見しQUESTSへ正しく反映できた）。`.claude/commands/yakin.md`「⚠️ 現在のスコープ」節を「コード変更禁止・調査とドキュメントのみ」から通常運用（普請は急務1件について掟の範囲で実装まで可、検証コマンドを明記）へ書き換えた。**あわせてSTATUS自己点検の恒久ルールを追加**: 夜勤は毎回`docs/office_map.html`のQUESTSだけでなくSTATUS（評定・守り・軍費）も実態と照合し、古ければその場で更新する旨を`.claude/commands/yakin.md`前段の手順に明記した（マップ全体が自動で生き続けるようにするため） | 2026-08-26 | 実装済み（`docs/gunrei_kouho.md` / `docs/office_map.html` / `.claude/commands/yakin.md` / 本行）。**STATUS総点検の結果（2026-08-26）**: 評定＝「議案なし」で変更不要（オープンPR 0件を確認）。守り＝「8/24検分 高0」で変更不要（最新の`security/security_check_*.md`は8/24付のまま、次回定例は8/31）。軍費＝「月 約$33」で変更不要（`.claude/agents/infra-ops.md`のコスト内訳 Render Starter $7 + Supabase Pro $25 + Cloudflare約$1 + LP/メール$0 = $33 と一致。実際の請求額は本セッションにダッシュボードアクセス権限が無いため未確認、内訳との整合のみ確認）。領国は今回対象外（ユーザーからの指定なし） |

| **管理者閲覧機能（計画書 `docs/jisso_keikaku_admin_viewer_2026-08-26.md`）の評定確定**: 専用管理者アカウント（`admin@ureshiru.com`、Supabase側で手動作成予定）にアカウント一覧（メール・登録日・課金状態・最終ログイン・データ取込有無）＋選択アカウントの読み取り専用閲覧モードを提供する。実装方式は「service_role的な専用API」＝`tenancy.current_user_id`を対象ユーザーに一時的に差し替える方式を採用（RLS adminポリシー＋Data API直接公開は、2026-07に事故を起こした経路を再び開くことになるため不採用と結論）。管理者判定はメールアドレスではなく**Supabaseユーザーの UUID を環境変数 `ADMIN_USER_IDS` に固定する方式**（メールは変更・取り違えの余地があるためオーナーが判定方式自体を変更した＝評定でのQ3変更点）。閲覧モードは読み取り専用（GET以外403）を`UserContextMiddleware`拡張で強制。開始・終了・対象を`AdminViewSession`テーブル（監査ログ）に記録。セッション自動失効は2時間。閲覧バナーに「読み取り専用」を明記することを必須要件とした（書き込みボタンの個別disabled化は見送り＝評定でのQ2変更点）。`lp/privacy.html` 6章（安全管理措置）に「サポート対応および導入支援の目的で、当方の担当者が利用者の登録データを閲覧する場合があります。閲覧は必要な範囲に限り、閲覧記録を保存します。」を追記する（法的に必須ではないが透明性のため。文言はオーナー確定済み）。`EXEMPT_TEST_EMAILS`の運用（追加・削除手順、台帳）は独立ドキュメント `docs/unyou_exempt_test_emails.md` として新規整備し、CLAUDE.md側は参照ポインタのみに留める。**区切り1（バックエンド基盤）・区切り2（閲覧モード本体・ミドルウェア拡張）は対話セッション専任**（RLS・認証の根幹に触るため、無人実行のCoworkには委任しない）。区切り3（フロントエンド）はAPI確定後にCowork可。区切り4（法務文言）・区切り5（EXEMPT運用ドキュメント）はCowork委任・即日実施可（自己完結のタスク指示書 `docs/cowork_shiji_admin_viewer_ku4_privacy_2026-08-26.md` / `docs/cowork_shiji_admin_viewer_ku5_exempt_docs_2026-08-26.md` を用意済み） | 2026-08-26 | 計画確定・評定済み（`docs/jisso_keikaku_admin_viewer_2026-08-26.md`）。**区切り4・5はCowork納品パッチを`git am`で適用し、[PR #53](https://github.com/Shoichiro12/rakuten-kpi-app/pull/53)（privacy.html追記）・[PR #54](https://github.com/Shoichiro12/rakuten-kpi-app/pull/54)（EXEMPT_TEST_EMAILS運用ドキュメント化）としてマージ済み（2026-08-26）。** **区切り1（バックエンド基盤）・区切り2（閲覧モード本体）も対話セッションで実装済み・[PR #59](https://github.com/Shoichiro12/rakuten-kpi-app/pull/59)としてマージ済み（マージコミット `b085a39`、2026-08-27T14:28:48Z）。** **⚠️ 実装時にQ3からの変更が1点ある**: 管理者判定のenv変数名は決定事項欄の `ADMIN_USER_IDS`（複数想定）ではなく**`ADMIN_USER_ID`（単数形）で確定・実装**（オーナー指示。Renderに既に `ADMIN_USER_ID` を設定済みで、評定どおり管理者1人・UUID固定という前提と一致するため）。将来複数管理者に対応する際は `ADMIN_USER_IDS` へ改名する設計にしてあり、`backend/admin_guard.py` のコメントに明記済み。**新規ファイル**: `backend/admin_guard.py`（管理者判定）・`backend/supabase_admin.py`（Supabase Auth Admin APIの共通クライアント。`routers/account.py` の退会処理から鍵形式判定ロジックを切り出して共通化）・`backend/admin_view.py`（閲覧セッションのトークン発行・ハッシュ化・失効判定）・`backend/routers/admin.py`（`GET /api/admin/accounts` / `POST /api/admin/view-sessions` / `POST /api/admin/view-sessions/{id}/end` / `GET /api/admin/view-sessions`）。**変更ファイル**: `backend/models.py`（`AdminViewSession` モデル追加。`UserScopedMixin` 継承・`is_sample` 列は持たないため `sample_data.py` の対象から構造的に除外）・`backend/migrations.py`（`_USER_SCOPED_TABLES` へ `admin_view_sessions` 登録）・`backend/main.py`（管理者ルーター登録）・`backend/routers/account.py`（`supabase_admin` 経由へリファクタ）・`backend/.env.example`（`ADMIN_USER_ID` の説明追記）。**検証済み（ローカル・自作HS256トークンで実施）**: ①非管理者UUIDでアカウント一覧APIが403「管理者権限がありません。」②閲覧モード中の書き込み系（`POST /api/targets`）が403「閲覧モードは読み取り専用です。保存・削除はできません。」③期限切れ・終了済みトークンの再利用が401「閲覧セッションが無効です。再度開始してください。」④`GET /api/admin/view-sessions` で開始・終了・対象（admin_email/target_user_id/target_email/started_at/ended_at/expires_at）が実記録されることを確認⑤非管理者の通常利用（ヘッダなし）に回帰なし。**2時間失効はDBに期限切れタイムスタンプの行を直接INSERTして判定分岐（`expires_at > now()`比較）を踏む方法で検証**（実時間待ちではないが、判定が静的な時刻比較のため同値と判断）。**本番Supabase環境でのアカウント一覧の中身・`expires_at`計算のE2E経路は未検証**（区切り6で実施予定）。**区切り3（フロントエンド）は自己完結のCowork委任指示書 `docs/cowork_shiji_admin_viewer_ku3_frontend_2026-08-28.md` を用意済み（未着手）。区切り6（本番デプロイ・検証・オーナー目視）は実機確認チェックリスト `docs/honban_kakunin_admin_viewer_ku6_2026-08-28.md` を用意済み（未着手）** |

| **Cowork納品パッチ（管理者閲覧機能 区切り4・5）適用前の事実確認で判明した誤り**: パッチ適用前、オーナーからの指示で「Coworkがku5の関連ファイル参照から`security/security_check_2026-08-03.md`を除外したのは誤認では（8/24の突き合わせで実在を確認済みのはず）」という事実確認を依頼された。**結果は逆だった**: `git log --all --diff-filter=A --name-only`で全ブランチ・全履歴を検索したところ、`security/security_check_2026-08-03.md`という名前のファイルは一度も存在せず、**Coworkの判断が正しかった**。2026-08-24にCLAUDE.mdへ書いた「`security_check_2026-08-03.md`に...既に独立記録されており」という記述自体が、実在する`docs/security_taiou_2026-08-03.md`（作業報告）と存在しないファイル名を混同した事実誤認であり、これが3週間気づかれずに残っていた（上記「Coworkルーチン4週分の突き合わせ」行を訂正済み）。**申し送りルール6（参照は実在チェックしてから書く）の実例**: 一度誤って書かれた参照は、後続のセッションでも実在チェックなしに正しいものとして扱われ続け、今回は正しい判断を下したCowork側を誤って疑う結果になった。**教訓: 「以前確認した」という記憶ベースの主張も、実在チェック（`git log --all`等）をやり直してから信じること。** ku5パッチのレビューでは、当初の指示（参照を両方併記に直す）を撤回し、Coworkの参照差し替えをそのまま採用した | 2026-08-26 | 訂正完了（本コミット・[PR #53](https://github.com/Shoichiro12/rakuten-kpi-app/pull/53)ではなく`claude/ledger-correction-2026-08-26`ブランチ）。CLAUDE.mdの誤記述を修正し、経緯を本行に記録 |

| **Coworkのpush権限追加提案は不採用、パッチ運用を維持（2026-08-26）**: 管理者閲覧機能の区切り4・5でCowork納品パッチ（ku4・ku5）を`git am`で適用し、SHA256照合・`git apply --check`・PR作成まで問題なく完了した。2026-08-04決定（GitHub操作向けの認証トークンをCoworkセッションに渡す手段が無く、シークレット登録の仕組みも無いため、パッチ運用に固定する）を変更する必要は無いと判断し、現状維持を確認した | 2026-08-26 | 決定のみ。運用変更なし |

| **夜勤（無人・定期実行）の通常運用への移行後、初回の急務対応でLPのアンカーリンク切れを修正**: `docs/gunrei_kouho.md`（2026-08-25巡回で発見・2026-08-26に急務へ昇格）の候補「`lp/about.html`の『料金を見る』ボタンがリンク切れ」に対応。原因は2026-08-24のLPエッセイ型全面刷新で `index.html` の構造が変わった際、価格ブロック（`<div class="price">`）から `id="pricing"` が失われ、`about.html:91` の `href="index.html#pricing"` が宛先を失っていたこと。**修正は最小**: `lp/index.html` の価格ブロックに `id="pricing"` を1つ追加しただけ（LPはビルド工程の無い静的HTMLのため、他に波及するコード変更は無い）。あわせて `docs/office_map.html` のQUESTS・STATUSと `docs/gunrei_kouho.md` の状態列も、既にマージ済みだった PR #53・#54・#55（管理者閲覧機能 区切り4・5、台帳訂正）の「マージ待ち」表記が古いままだったのを合わせて更新した（評定チップ「議案なし」に更新、完了済みQUESTS 3件を除去） | 2026-08-26 | 実装済み（`lp/index.html` に `id="pricing"` 追加、[PR #56](https://github.com/Shoichiro12/rakuten-kpi-app/pull/56) マージ済み）。**検証**: ローカルで `lp/` を静的配信し、`index.html` に `id="pricing"` が1箇所のみ存在すること・`about.html` の `href="index.html#pricing"` と宛先が一致することを確認。**オーナー評価（2026-08-26）: 夜勤の初陣として合格。** あわせて、この回に含めたQUESTS/STATUSの是正（マージ済みPRの反映・評定チップ更新・完了項目の除去）は下記「夜勤のSTATUS・軍令帳・台帳の実態照合ルールを明確化」行が言う**「事実の更新」に該当し、普請1件のルールの外で行ってよい適切な対応だった**とオーナーが確認済み |

| **夜勤のSTATUS・軍令帳・台帳の実態照合ルールを明確化**: 夜勤（`/yakin`）が普請と無関係にその場で行ってよい範囲を、`.claude/commands/yakin.md` 前段ステップ3に追記して定義した。**「事実の更新」**（マージ済みPRのQUESTS/STATUSへの反映、日付の最新化、件数の実態合わせ等、確認すれば機械的に定まる更新）は普請1件のルールの外で毎回・複数箇所行ってよい。一方、**文言の言い回しの改善・構成の組み替え・新しい判断（優先順位付け等）を伴う修正**は事実の更新ではなく、通常の急務（普請）または巡回の候補として扱う。**判断に迷ったら候補側に回す**、という原則も明記した | 2026-08-26 | 実装済み（`.claude/commands/yakin.md` 前段ステップ3への追記）。オーナー指示（PR #56の是正内容を「事実の更新」の実例として承認）を受けて明文化 |

| **マスタ削除の一括化と未分類フロー（計画書 `docs/jisso_keikaku_master_bulk_delete_2026-08-28.md`）の評定確定**: オーナー評定済みの仕様6点。①カテゴリ・商品マスタの一覧に複数選択チェックボックスと一括削除。削除前に件数つき確認ダイアログ②カテゴリ削除時、紐づく商品はブロックせず「未分類」扱いに移す③未分類の商品が存在する間、「未分類の商品が◯件あります。カテゴリを再設定しますか？」という非強制アラートと再設定への導線を表示④KPI集計・GAP分析・診断では未分類を除外せず「未分類」グループとして集計に出す（カテゴリ削除の前後で売上合計が変わらないこと）⑤削除は自テナントのRLSスコープ内のみ。既存の単件削除と同じガード。マスタCRUD規約どおり両マスタ同じ作法⑥集計仕様（④）はkpi-analystにレビューさせる。**§2の核心調査結果（実装スコープを縮小した重要な発見）**: `Product.category_id`（FK）とGAP分析・診断が使う「ジャンル」（`RppWeekly.genre`/`MonthlyItemSales.genre_u1-3`のCSV取込時スナップショット文字列）は完全に別のデータ経路。`gap_analysis.py`・`evaluation.py`・`diagnosis.py`がいずれも`Product`/`ProductCategory`を一切参照しないことをgrepで確認済み。**したがって仕様④はGAP分析・ダッシュボード側のバックエンド変更が不要で、商品マスタ・アイテム別目標一覧のフロント表示強化（`category_id=null`を「未分類」ラベルで表示）のみで完結する。** 唯一のFK依存箇所は`item_targets.py`の`_resolve_genre()`（実績が1件も無い商品のフォールバック表示、実害の少ない1箇所のみ）。**kpi-analystレビュー（区切り4）で実測追試済み（2026-08-28）**: ローカルSQLite・サンプルデータでカテゴリ削除の前後にGAP分析API（`/api/gap/kpi-tree`・`/api/gap/genre`）を実際に叩き、JSON全体が完全一致することを実測確認。診断・提案系5ファイル（`diagnosis.py`/`evaluation.py`/`matrix_actions.py`/`product_recommendations.py`/`recommendations.py`）も個別に`category_id`/`Product`参照0件を確認。§2の結論を追認、穴なし。**2026-08-22の既存決定「削除は1件ずつ。全削除は原則置かない」（`docs/jisso_keikaku_input_ia_seiri_2026-08-22.md` Q1）を今回のオーナー評定が明示的に覆す**（蒸し返しではなく正式な再評定として記録する）。**オーナー評定（2026-08-28、4問中3問に実装条件を追加）**: Q1=一括削除APIは新設・POSTボディ方式（`POST /master/categories/bulk-delete` / `POST /master/products/bulk-delete`）で確定。**追加条件**: レスポンスは要求件数と実削除件数を両方返し、不一致ならフロントで「◯件中◯件を削除しました」と実数表示する（黙って成功扱いにしない）。Q2=全選択チェックボックスの対象は現在のページのみで確定。**追加条件**: ボタンラベルは「全選択」ではなく「このページを全選択」と明記する（絞り込み結果全体を選んだと誤解する事故の防止）。Q3=未分類アラートは「画面滞在中は再表示しない」（b案）で確定。Q4=kpi-analystレビューは実装前で確定・実施済み（上記）。**区切り構成**: 区切り0（確認事項確定・完了）→区切り4（kpi-analystレビュー・完了）→区切り1（バックエンド一括削除API）・区切り2（フロントUI）・区切り3（未分類アラート・ラベル表示）は夜勤（無人・定期実行）対象、区切り5（本番検証）は対話セッション専任。区切り1〜3は設計変更なしで夜勤に流してよい状態 | 2026-08-28 | 計画確定・評定済み（`docs/jisso_keikaku_master_bulk_delete_2026-08-28.md`）。区切り0・4は完了。区切り1〜3は夜勤対象として `docs/office_map.html` の軍令帳に積む予定（実装完了後にこの行をコミットハッシュ付きで更新すること）。区切り5（本番検証）は対話セッション専任 |

| **台帳整理3件（CSVインジェクション正式クローズ・管理者閲覧機能の進捗反映・区切り3/6の文書化）の指示が2セッションへ並行して渡り、重複PRが生まれた**: オーナーが同じ内容の指示を2つの対話セッションへ出した結果、両方が独立に着手し、[PR #61](https://github.com/Shoichiro12/rakuten-kpi-app/pull/61)（CLAUDE.md・security/index.mdの該当行更新＋ku3/ku6の2文書）と[PR #62](https://github.com/Shoichiro12/rakuten-kpi-app/pull/62)（同内容）が同時期にオープンした。**PR #62側のセッションが自ら差分を比較して報告し**（ku3指示書の完成度・ku6のcurl代替手順の有無・実サーバーでの検証有無を比較）、オーナーが内容を確認のうえ**PR #61をマージ・PR #62はクローズ**と判断（#62には「#61に統合、重複」のコメントを残してクローズ）。**#61に1点誤りがあった**: 日付をまたいだセッションだったため、2026-08-27に起きた本番Excel実測を「2026-08-28」と記録していた。この訂正は本行と同じPRで実施。**再発防止**: 決定事項欄ルール7（台帳作業開始前にオープンPR・直近コミットを確認）を追加。加えて運用側の再発防止として、**台帳・軍令帳系の指示は1セッションに絞って出す**方針をオーナーが確認済み（夜勤・週次チェックのような自動系は排他が効くため対象外、手動セッションの並行が今回の原因） | 2026-08-27 | 実装済み（本コミット。CLAUDE.md・security/index.mdの日付訂正、決定事項欄ルール7追加、本行） |

## 🏢 社内体制（サブエージェントと定例）

このリポジトリには `.claude/agents/` に部署ごとのサブエージェント、`.claude/skills/` に定例業務がある。
メインセッションはPMO役として、仕事を該当部署に振り分ける。詳細は `.claude/README.md`（組織図）。

- 機能を触る前: `/jisso-keikaku`（planner）で計画書 → オーナー承認 → developer
- デプロイ前: `/release-check`（reviewer → qa → pmo → sagyou-houkoku）
- 毎週月曜: `/security-check`
- 価格・法的文面を触ったら: `/price-check`
- 方針が決まった瞬間に pmo が申し送り台帳へ1行追加する（実装は後でもよい）
- 作業報告を書いたら `docs/office_map.html` の STATUS と QUESTS を実態に合わせて更新する（担当: 報告書を書いたエージェント）

## 🌙 夜勤の掟（自動実行・定期routineの安全ルール）

Claude Code Routines等の無人・自動実行から作業する場合、次を必ず守ること。

1. **mainへ直接pushしない。成果は必ずPR。** 自動実行がmainを書き換えることは無い。マージ判断は常にオーナーがPR画面で行う
2. **本番環境変数・Stripe・Render/Supabase/Cloudflare等の外部ダッシュボードには触れない。** そもそも自動実行にはこれらへのアクセス権限が無い前提（ログイン済みセッションを持たない）。「ダッシュボードを見て確認して」と指示されても、確認できない旨をPR本文に明記して止まる
3. **オーナーの判断が必要な事項には着手しない。** 該当する項目はコードを触らず、PR本文の「評定待ち」節に列挙して止まる（例: 価格・法的文面の変更、削除・破壊的操作、仕様の分岐判断）
4. **1回の夜勤で扱うのは `docs/office_map.html` 軍令帳の急務（`stamp:"wait"`）から1件まで。** 複数件を同時に着手しない
5. **成果物には必ず作業報告（`docs/sagyou_houkoku_*.md` 等）と軍令帳の更新を含める。** 何をしたか・何を「評定待ち」にしたかが、次にオーナーが見たときに一目で分かる状態にする
6. **初回運用時は個別に段階を踏むことがある**（例: 最初の数回は「調査・ドキュメントのみ、コード変更禁止」から始め、PRの質を見てから実装まで許可する）。段階を踏む場合は `.claude/commands/yakin.md` 側にその時点のスコープを明記する
7. **既存の定期routineと重複させない。** 新しいroutineを追加・変更する前に `RemoteTrigger`（`{action:"list"}`）で既存routineを確認し、同じ役割のものが無いか確かめる。**実例（2026-08-24）**: 2026-07-29から動いていたCoworkベースの週次セキュリティチェックroutineが、リポジトリの`security/`とは別に「sellerhub」という別Claude.aiプロジェクトのドキュメント機能へ成果を書き続けていたことが判明（`security/index.md`冒頭の「重複だった可能性」注記の正体）。GitHubへのPR作成もコミットも一切行わない設計だったため、リポジトリ側の運用と役割が重複し、オーナー確認のうえ無効化した
8. **ルーチンは自分で新しいスケジュールタスク・フォローアップタスクを作成しない。** 実行は与えられた1回で完結し、続きが必要ならPR本文に書いて止まる。**実例（2026-08-24）**: 夜勤・週次security-checkの初回テスト実行が、それぞれPR作成後に「1時間後にPRの状態を再確認する」`send_later`タスクを自発的に作成した。プロンプトが「問題なければ黙って再アーム」としており、マージされるまで無期限に繰り返される設計になっていた。無害だったが、7項目目の「7/29から気づかれず動き続けたCoworkルーチン」と同じ構図を自ら作りかけていた実例であり、オーナー確認のうえ両方`enabled:false`で停止した
9. **巡回は発見まで。候補を急務に変えるのはオーナーの裁可のみ。** 夜勤の後段「巡回」（`.claude/commands/yakin.md`参照）はコード・ドキュメント・台帳を見回って問題を見つけるが、修正はせず、`docs/office_map.html`のQUESTSに`stamp:"kouho"`（候補）として積み、詳細を`docs/gunrei_kouho.md`に書くだけで止まる。候補を急務（`stamp:"wait"`）へ昇格させる、あるいは却下して`docs/gunrei_kouho.md`の「却下済み」節へ移すのは、いずれもオーナーのみが行う。夜勤は次回以降も候補札そのものには手を出さない

## ⚠️ セキュリティ最優先事項: 新しいテーブルには必ずRLSを（顧客データ漏洩の防止）

**このプロダクトは他社（EC事業者）の売上データを預かる。データ漏洩は一度でも起こしてはならない。**

### 実際に起きたこと（2026-07）

`models.Base.metadata.create_all()` で作成したテーブルは **RLSが無効のまま** `public` スキーマに置かれる。
Supabase は `public` スキーマを Data API (PostgREST) 経由で公開するため、
**フロントのJSに埋め込まれた anon キーだけで、誰でも全データを読み書きできる状態だった。**

実際に未ログイン・anonキーのみで商品名・売上・目標値の実データが取得できることを確認済み。
Supabase Security Advisor に `rls_disabled_in_public` の Critical が9テーブル分出ていた。

補足: **anonキーが漏れていたわけではない。** anonキーは公開が前提の値で、
本来の防御はRLSが担う。そのRLSが無効だったため防御がゼロだった。

### 現在の防御（3重）

1. **起動時に自動強制** … `migrations._enforce_rls_pg()` が `pg_tables` を走査し、
   RLS未適用のテーブルを自動で `ENABLE ROW LEVEL SECURITY` する。
   **新しいモデルを追加してもデプロイすれば自動で塞がる。** 冪等。
2. **可視化** … `GET /api/security-status` が `unprotected` を返す。
   ここが空でなければ即対応が必要。
3. **このドキュメント**

### 新しいモデルを追加するときの必須確認

- `UserScopedMixin` を継承する（ユーザー単位のデータ分離。`tenancy.py` 参照）
- デプロイ後に `GET /api/security-status` で `ok: true` / `unprotected: []` を確認する
- **RLSを無効化するコードを書かない。** どうしても必要なら理由をここに追記すること
- **`backend/sample_data.py` を一緒に更新する。** 新しいテーブル・フィールドを追加する機能実装のたびに、`generate_sample_data()` に生成処理を足す（起動時削除も忘れずに）。サンプルデータが実スキーマから遅れると、デモ・動作確認・オンボーディングが壊れる。

### なぜアプリが壊れないか

バックエンド(FastAPI)は `DATABASE_URL` でテーブル所有者(`postgres`ロール)として直接接続しており、
**所有者はRLSをバイパスする**。そのためポリシーを1つも作らなくてもアプリの動作は変わらず、
Data API 経由の anon / authenticated アクセスだけが全拒否される。
（`FORCE ROW LEVEL SECURITY` は所有者にも適用されてしまうので使わないこと）

楽天（Rakuten）出店者向けのKPI管理アプリ。FastAPIバックエンド + React/Viteフロントエンドの2構成。楽天RMSからエクスポートしたCSVを取り込み、KGI→KPIのロジックツリー分解・GAP分析・RPP広告実績を可視化する。UI・コメント・エラーメッセージはすべて日本語。

## この製品が目指しているもの（必読）

**事業コンセプトは [`docs/VISION.md`](docs/VISION.md) を参照。実装や優先順位に迷ったらそこに立ち返る。**

要約すると、これは「楽天の分析ツール」ではなく **ECコンサルティングをAIで民主化する** ためのプロダクトで、
最終形は EC事業者の**意思決定OS**（AIストアマネージャー）。ダッシュボードを見せることが目的ではなく、
店舗が「次に何をすればいいか」を判断できる状態を作ることが目的。

そのため、コードを書くときは以下を判断基準にする（詳細は VISION.md 末尾）:

- **出力の最終地点は数値ではなく次のアクション。** 数値を並べただけの画面は未完成とみなす。
- **データが無いときこそ意思決定を止めない。** 「データがありません」で画面全体を隠さず、
  「今わかること」と「まだわからないこと」を切り分けて提示する。
  （実例: 商品分析データがあるのにRPP未取込というだけで月次が全面空白になる不具合があった）
- **将来のモール横展開（Amazon/Shopify等）に備え、モール固有の取込み層とKPI計算ロジック層を混ぜない。**

## Commands

開発（Windows）はリポジトリ直下の `start.bat` がバックエンドとフロントを別ウィンドウで同時起動する。個別に動かす場合:

```powershell
# バックエンド（cwd = backend/、ポート8000）
cd backend
py -3 -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
# → API: http://localhost:8000 / Swagger: http://localhost:8000/docs

# フロント（cwd = frontend/、ポート5173）
cd frontend
npm install      # 初回のみ
npm run dev      # 開発サーバー（/api を 127.0.0.1:8000 にプロキシ）
npm run build    # tsc 型チェック + vite build（CIの代わり。型エラー0必須）
```

依存導入: backend は `pip install -r backend/requirements.txt`。

**AIエージェント用スキル（UI作業時）**: skills.sh のデザインスキルを利用する。`.agents/` `.claude/skills/` `skills-lock.json` は第三者コンテンツのため**gitignore済み・コミットしない**（2026-08-03決定）。新しい環境では `npx skills add vercel-labs/agent-skills` で再導入し、使うのは `web-design-guidelines`（UIレビュー）・`vercel-react-best-practices`・`vercel-composition-patterns` の3つ（他は削除してよい）。shadcnスキルは**このリポジトリがshadcn/ui未使用のため対象外**。

**デザイントーンスキル（typeui.sh、2026-08-04導入）**: `.claude/skills/design-system/SKILL.md` と `.agents/skills/design-system/SKILL.md` は [typeui.sh](https://www.typeui.sh)（`bergside/typeui`、MIT）の `clean` プリセットを導入したもの。既存の数値表示規則（丸め・pt表記・弾丸グラフ＝`docs/ui_number_and_chart_rules_2026-08-04.md`）が「情報を絞る・読みやすくする」方向で一貫しているため、データ密度重視の`enterprise`より`clean`を採用（オーナー判断）。**第三者コンテンツのため上記の既存gitignoreパスでカバー済み・コミットしない。** 新しい環境では以下で再導入する:

```powershell
npx typeui.sh pull clean --format skill --providers claude-code
```

**注意**: このリポジトリの環境では `npx typeui.sh` 経由だと標準出力が出ない既知の問題があり（Windows npx シェルラッパー起因、パッケージ自体の問題ではない）、出力を確認したい場合は `npm exec typeui.sh -- pull clean --format skill --providers claude-code` を試すか、実行結果は `git status` / 生成ファイルの中身で確認すること。汎用プリセットのため楽天KPIアプリ固有の数値表示規則は含まれない（このCLAUDE.mdの規約が優先）。ウレシル側への導入は未定（候補選定保留中）。

**テストフレームワークは未導入**（pytest等なし）。検証は (1) `cd backend && py -3 -c "from main import app"` のimport確認、(2) `cd frontend && npm run build` の型チェック、(3) uvicorn起動して `curl` でエンドポイントを叩く、で行う。ロジック単体検証は対象関数をその場で `py -3 -c "..."` 呼び出しする。

## Windows固有の注意

- `vite.config.ts` のプロキシ先は `http://127.0.0.1:8000`（`localhost` 不可）。Windows+Node18+では `localhost` がIPv6 `::1` を先に引き、IPv4でbindするuvicornへのフォールバックが遅延して「Failed to fetch」になるため、IPv4直指定で固定している。**この設定を `localhost` に戻さないこと。**
- 「Failed to fetch」の典型原因は、**古い`uvicorn`プロセスがポート8000を掴んだまま旧コードを返している**ケース。新エンドポイントが404/挙動不一致なら、まず`:8000`を掴むプロセスを停止して再起動する。

## アーキテクチャ

### バックエンド（`backend/`）

- `main.py` がエントリ。起動時に `models.Base.metadata.create_all()` でSQLite（`rakuten_kpi.db`）にテーブルを自動生成する（マイグレーションツールなし＝モデル変更は手動でDB削除 or ALTER）。全例外は `global_exception_handler` が `{"detail": str(exc)}` のJSON 500に変換。CORSは `localhost:5173`/`3000` のみ許可。
- ルーターは `backend/routers/` 配下（dashboard / import_csv / targets / gap_analysis / products / actions / evaluation / export / account）。**規約: 全エンドポイントは常にJSONを返す（データ無しでも `{}`/`[]`）。** フロントはこれに依存している。
- **クエリパラメータの列挙値は `typing.Literal[...]` で型注釈する。** `Query(..., enum=[...])` はPydantic v2環境ではバリデーションされず不正値が素通りする既知の落とし穴があり、`period`/`level`/`period_type` 等はすべて `Literal` に統一済み。

### マルチテナント（ユーザー別データ分離）— `backend/tenancy.py`

- 全データテーブルは `UserScopedMixin` を継承し `user_id` 列（SupabaseユーザーUUID）を持つ。**新しいモデルを追加するときは必ず `UserScopedMixin` を継承すること**（継承しないと全ユーザー共有になる）。
- 絞り込みはSQLAlchemyイベントで自動適用: `do_orm_execute` が全 SELECT/UPDATE/DELETE に `user_id = 現在ユーザー` を付与（`with_loader_criteria`。集計クエリや `Query.delete()` にも効く）、`before_flush` がINSERT行に user_id をスタンプ。**ルーター側で user_id を意識する必要はない**が、生SQL（`text()`）には自動適用されないので手動で絞ること。
- 現在ユーザーは `auth.UserContextMiddleware`（ASGI）がContextVar `tenancy.current_user_id` にセットする。FastAPIの同期依存関係内でContextVarをセットしても伝播しない（スレッドプールのコンテキストコピー）ため、ミドルウェア方式。
- 認証無効（ローカル開発）時は `user_id IS NULL` の行のみ対象＝従来どおり単一ユーザーで動く。
- ユニーク制約は user_id 込み。既存DBは起動時の `migrations.run_migrations()` が user_id 列追加・Postgresの制約張り替えを冪等に実行。マルチテナント化以前のデータ（user_id NULL）は env `LEGACY_DATA_USER_ID` で特定ユーザーに割り当て可能。

### アカウント管理

- フロント: `/account`（`AccountSettings.tsx`）＝メール変更・パスワード変更（Supabase `updateUser`）・退会。`Login.tsx` にパスワードリセットメール送信、`ResetPassword.tsx` は `PASSWORD_RECOVERY` イベント時にApp.tsxが表示。
- バックエンド: `routers/account.py`。退会（`DELETE /api/account`）＝本人の全データ削除 + Supabase Admin APIでユーザー削除。**env `SUPABASE_SERVICE_ROLE_KEY` 必須**（未設定は501）。service_roleキーは絶対にフロントへ渡さない。

### KPI計算は `backend/calculations.py` が単一の真実

全KPIの計算式は `calc_kpis()` に集約。重複実装せずここを参照・修正する。定義上の注意:
- `roi = gp / ad_cost`（**粗利ベース＝ROASの粗利版**。財務的なROI=純利益/投資ではない）。アラート閾値 `roi < 100` は「広告費が粗利を超過＝赤字」の意味で正しい。
- `cvr = cv / ct`（クリック→注文）。一方 `MonthlyItemSales.cvr` は「訪問UU→注文」で**母数が異なる**。アクセス指標の2軸（`rpp_click` / `site_uu`）と低母数除外ルール（`is_reliable`）の定義は **`backend/access_definitions.py` が単一の真実**。アクセス関連の値を返す新APIは必ず `access_axis` を含め、同一画面で両軸を混在させない。
- `rev`（利益残）= `gp - (ad_cost + steady_cost)`、`steady_cost = gross * expense_rate`（`expense_rate` は `Target` 由来、既定0.15）。

### データモデル（`backend/models.py`）と取り込み2系統

CSVパースは `backend/routers/import_csv.py`。エンコーディング/スキップ行はRMSの書式に合わせて固定:

1. **RPP広告レポート（Shift-JIS, 先頭8行スキップ）** → 2テーブルに同時書き込み:
   - `RppWeekly` … 既存集計テーブル。dashboard / gap_analysis / products が**集計に使うのはこちらのみ**。
   - `RppSales` … 生データ保管。週次/月次両対応、720h/12hの2アトリビューション値（`gross_720`/`gross_12`等）を保持。`/api/import/rpp/{periods,sales,summary}` の新エンドポイント専用。
   - 計測期間文字列から週次/月次を自動判別（`2026年03月01日〜07日`=weekly / `2026年03月`=monthly）。
   - ⚠️ **二重計上注意**: 1インポートで両テーブルに書く設計のため、`RppSales` を使う新たな集計を足すと `RppWeekly` 由来の既存集計と二重計上になりうる。役割分離を守ること。
2. **月次商品分析（UTF-8 BOM, 先頭5行スキップ）** → `MonthlyItemSales`。ジャンルが大/中/小（`genre_u1/u2/u3`）に分割済み、アクセス・CVR等を保持。
   - `MonthlyAnalysis` は旧スキーマ（レガシー）。新規はなるべく `MonthlyItemSales` を使う。

### 週次/月次の期間ロジック（gap_analysis.py / dashboard.py）

- `weekly`: `RppWeekly.week_start`（日曜始まり）の完全一致でフィルタ。
- `monthly`: `func.strftime("%Y-%m", RppWeekly.week_start) == ym` で集計。⚠️ 月跨ぎの週は開始日の月に丸められるため、`MonthlyItemSales` の正確な月次値とは僅差が出る既知の制約。
- 前月（前期）は必ずリクエストの `year_month` から `_prev_month()` で導出する（`today` 依存にしない）。
- KGIツリー（`/api/gap/kpi-tree`）は `KGI = アクセス × CVR × 客単価` をすべて `RppWeekly` 由来で統一（access=クリック数ct）。`MonthlyAnalysis.access_count` は母数が異なるため使わない。
- ジャンルGAP（`/api/gap/genre`）は `RppWeekly.genre` の `/` 区切りを階層分解し、`level`(u1/u2/u3) と `parent` で絞り込む。既存レスポンスキーは維持し階層情報キーを追加する後方互換方針。

### フロントエンド（`frontend/`）

- `src/App.tsx` がルーティング（`/`=Dashboard, `/gap`=GapAnalysis, `/products`=ProductKPI, `/import`=DataImport, `/targets`=TargetSetting, `/rpp`=RppAnalysis）。
- **法的文書（特商法・プライバシーポリシー・利用規約）はこのアプリ内に持たない。** 正はLP（`https://ureshiru.vercel.app` = Stripeに「ビジネスウェブサイト」として登録しているサイト）側にあり、アプリからは `src/lib/links.ts` の `LEGAL_LINKS` を使って外部リンクで飛ばす（フッター・ログイン画面・`/billing` の申込ボタン付近）。
  - **アプリ内に法的ページを作り直さないこと。** 一度アプリ側にも同じページを作ってしまい、価格改定でLP側と食い違う状態を招いた（LP: ¥19,800 / アプリ: ¥22,000）。文書が2箇所にあると必ずズレる。
  - Stripeの審査担当者が見るのは**LP側**。特商法は「購入前」の表示を求めているが、購入導線（`/billing`）から外部リンクで到達できれば要件を満たせる。
  - 独自ドメインへ移行する場合は `lib/links.ts` の `LP_BASE_URL` の1行を直す。
- 価格（月額 ¥20,000税抜 / ¥22,000税込）の表示箇所は**アプリとLPにまたがる**。改定時は下記すべてとStripeのpriceを同時に直す。
  - アプリ: `backend/billing.py` の `PLAN_AMOUNT_LABEL`（＋診断が突き合わせる `PLAN_AMOUNT_JPY`＝税込22000 / `PLAN_AMOUNT_EXCL_TAX_JPY`＝税抜20000 / `TAX_RATE`）
  - **LP（別リポジトリ）**: 料金セクション、特商法の販売価格、利用規約の利用料金
  - Stripe: price の `unit_amount`**総額表示義務があるので税込金額を主表記から外さない**（税抜のみの表示にしない。税抜・税込は同じ視認性で並列表示）。
- **プラン名「ウレシル 月額プラン」も3箇所にまたがる。** 価格と同じく、変えるときは3つ同時に直す（2026-08-06 に統一済み。申し送り表参照）。
  - アプリ: `backend/billing.py` の `PLAN_LABELS`
  - **LP（別リポジトリ）**: 料金セクション＋JSON-LD の `Offer.name`
  - **Stripe: Product の名前**（price ではなく product 側。`prod_Uy8KgcvxLY3gkx`）。**これが Checkout 画面と請求書に出る顧客向けの表記**なので、アプリのラベルだけ直しても顧客の目に入る名前は変わらない。`GET /api/billing/diagnose` が `price.product_name` を返すので、突き合わせはそこでできる。
- **消費税は Stripe Tax（自動税計算）を使わない**（2026-07 に方針変更）。取引ごと0.5%の手数料がかかるため。代わりに次の2つを組み合わせる。
  - Price は【税込 ¥22,000】(`unit_amount=22000`・内税)で登録する。
  - **無料の「税率」(Tax rates)を手動で1つ作り**（10%・`inclusive=true`・日本）、Checkout の `subscription_data.default_tax_rates` に渡す（env `STRIPE_TAX_RATE_ID`）。これで請求書に「消費税 10% ¥2,000（内税）」の内訳が出る（総額は¥22,000のまま）。**適格請求書発行事業者として登録済みなので、顧客の仕入税額控除のために内訳が必要。** 登録番号(T+13桁)はStripeの請求書テンプレート側に設定する（コードでは扱わない）。
  - ⚠️ **`default_tax_rates` と `automatic_tax` は併用できない。** Stripe Tax を有効にするとCheckout作成が拒否される。`automatic_tax` は渡さない（既定=無効）。
  - ⚠️ **envに税率を設定しても既存の契約には遡って適用されない。** 税率を追加・変更したら契約を作り直す必要がある。診断が契約側の `default_tax_rates` を検査する。
- **Managed Payments は明示的に無効化する**（`managed_payments={"enabled": False}` を Checkout に渡す）。Stripeアカウントでは**既定で有効**で、有効だと `default_tax_rates` が `Unsupported parameter` で拒否される。使わない理由は、**Managed Payments が日本国内取引の税務を代行しないから**（Stripeのドキュメントに、間接税を扱うのはシンガポールB2B国内と日本の全国内取引を「除く」国と明記）。国内向けの当サービスでは消費税の責任は自分に残る一方、税率指定と請求書発行はStripe側に握られ、適格請求書（登録番号・税額内訳）を自分で制御できなくなる。アカウント設定でも切れるが、既定が有効なのでコード側で明示しておく。
  - ⚠️ **`PLAN_AMOUNT_EXCL_TAX_JPY`（税抜20000）は表示のための手計算値**で、Stripeの設定から導かれる値ではない（22000 ÷ 1.1）。**消費税率が変わっても自動追従しない。** 税率改定時は Stripeのprice金額・`PLAN_AMOUNT_JPY`・`PLAN_AMOUNT_EXCL_TAX_JPY`・`TAX_RATE`・`PLAN_AMOUNT_LABEL`・特商法ページ・利用規約を**すべて手で**直す。診断が税抜表示と税込金額の整合性を検査するので、価格まわりを触ったら `GET /api/billing/diagnose` を必ず実行する。
- **すべてのAPI呼び出しは `src/lib/api.ts` の `request()` / `parseJson()` ヘルパー経由にする。** `res.text()` → 空ならフォールバック → `JSON.parse` をtry/catch、`Failed to fetch` も捕捉して日本語メッセージ化し、空レスポンスやパース失敗でUIをクラッシュさせない。新しいfetchを直書きしない（FormDataアップロードも同パターンを踏襲）。
- 各ページ・グラフ（Recharts）は空配列/undefined時に「データなし」を表示するガードを入れる。
- 型は `src/types/index.ts` に集約。

#### 画面幅の規約（2026-08-04 決定）

サイドバー折りたたみ（`Sidebar.tsx`、224px ⇄ 64px）を活かすため、**ページ直下のコンテナには `max-w-*` を付けない（全幅）**。幅の制御はブロック（カード）単位で行う。

| ブロックの性格 | 上限幅 | 例 |
|---|---|---|
| 列数の多いテーブル（8列以上） | 付けない（全幅） | 商品マスタの商品一覧、商品別KPI、RPP、GAP |
| 中くらいのテーブル（6〜7列） | `max-w-5xl`（1024px） | アイテム別目標、年間目標プランナー（詳細9列） |
| 列の少ないテーブル（〜5列） | `max-w-2xl`〜`max-w-3xl` | 年間目標プランナー（サマリ5列）、設定済み目標一覧 |
| フォーム系カード | `max-w-3xl`（768px） | 対象月・KGI・KPI目標値・経費設定・店舗設定 |
| 説明・注記テキスト | `max-w-3xl` | 年間目標プランナー周辺の注記3箇所 |

- フォーム行の共通コンポーネント（`TargetSetting.tsx` の `Field`）は `grid-cols-3` なので、上限を付けないとラベル列が全体の1/3（1800px画面なら約600px）を占め、ラベルと入力欄が離れる。**`Field` 自体に `max-w-3xl` を持たせてある**
- **ページ直下に `max-w-*` を戻さないこと。** 戻すとサイドバー折りたたみの意味が無くなる（2026-08-03 に実際にこれで指摘を受けた）
- 逆に、**ブロック単位の `max-w-*` を「全幅にすると決めたのに矛盾している」と消さないこと。** 全幅化の副作用（列の少ない表の間延び、1行が長すぎる注記）への対策として意図的に入れている
- 数値の列を持つ表には `tabular-nums` を付ける（桁位置を揃える）
- **データ取込み（`max-w-5xl`）とレポート出力（`max-w-4xl`）はページ直下の上限を残したまま据え置き**。フォーム中心の画面で広げる必要が薄いため（2026-08-04 判断）。統一したくなったらこの2画面も上表のルールに寄せる

#### 数字とグラフの規約（2026-08-04 決定）

**根拠と全文は `docs/ui_number_and_chart_rules_2026-08-04.md`。** ここは実装で必ず守る点だけ。

- **金額は `lib/format.ts` を通す。** 万・億で丸める（`¥965.0K` ではなく `96.5万円`）。日本語圏は1万単位で数えるため。カードとグラフの軸は丸め、**表・ツールチップ・CSVは丸めない**。CSVは `forExport()`（桁区切り・単位なし）を使い、表示用の関数を流用しないこと
- **割合の指標（CVR/CTR/ROAS/ROI/GPR/達成率）の前期比は「%」ではなく「pt」。** `formatPoint()` を使う。`3.42% → 3.24%` は `-5.3%` ではなく `-0.18pt`。これは見た目ではなく数値の正しさの問題
- **良い方向は `lib/metrics.ts` の `direction` から引く。** `up = 緑` を決め打ちしない（CPC・CPO・広告費は下がったら緑、クリック数・注文件数・アクセス・売上原価・店舗運営経費は中立で色を付けない）
- **目標に対する進捗は `components/kpi/BulletChart.tsx`（弾丸グラフ）を使う。進捗バーを使わない**（上限100%なので目標超過を表現できない）
- **デルタは絶対値＋矢印。マイナス記号を使わない。** 比較できないときは空欄にせず4状態を出し分ける（`components/kpi/Delta.tsx`）
- **大きい数字は1画面に1つ、多くて2つ。** `Stat` の `size="hero"` は1画面1回まで
- **日本語ラベルに `uppercase` / `tracking-wide` を付けない**（効かないか間延びする）
- **グラフは `components/chart/defaults.ts` を import する。** グリッドは実線の極薄（破線にしない）、単系列に凡例を出さない、未確定期間は点線
- **しきい値の判定は生の数値で行う**（表示用に丸めた文字列で比較しない）
- 表: 数値は右寄せ＋`tabular-nums`、単位は見出しに1回、**ゼブラ縞は使わない**（背景色は警告行専用に空けておく）

#### デザイントークン（2026-08-22 決定・ダッシュボードのドリルダウン再設計）

**根拠と全文は `docs/jisso_keikaku_dashboard_drilldown_2026-08-22.md`。** `frontend/tailwind.config.js` の `theme.extend.colors` に実装済み。

| トークン | 値 | 用途 |
|---|---|---|
| paper | #fdfcf9 | カード地 |
| bg-alt | #f4f1ea | 画面地・ホバー |
| line | #e5dfd4 | 罫線・枠 |
| ink / ink-strong | #383731 / #2e2d29 | 本文 / サイドバー地・主ボタン |
| sub / muted | #504b42 / #6b6559 | 補足 / 弱い補足 |
| sage / sage-soft / sage-deep | #78927b / #eef2ec / #4c6850 | 装飾・アクティブ指示 / 淡地 / リンク・テキスト緑 |
| alert / alert-bg | #c2382f / #fbe9e7 | 未達・悪化 / 未達ピル地 |
| up / up-bg | #17714d / #e3f0e8 | 達成・改善 / 達成ピル地 |

状態色は予約語（この4系以外に流用しない）。`emerald`/`sky`/`indigo`/`violet`/`orange` 等のTailwind色ファミリは新画面では使用禁止。旧トークンの `rakuten-red`（主ボタン・フォーカスリング・必須マーク・サイドバーのアクティブ表示）は `ink-strong` / `alert` / `sage-deep` / `sage` へ置換済み（2026-08-22、16箇所）。**`rakuten.red`/`rakuten.gold` は削除しない**（LP側との表記整合の参照用に残置。新規使用は禁止）。

フォント: 本文 `font-sans` = BIZ UDPGothic（400/700）、数値 `font-num` = Inter（500/600/700）+ `tabular-nums`。`index.html` の Google Fonts link と `tailwind.config.js` の `fontFamily` に導入済み。`font-display`（Poppins）・`font-mono`（Inconsolata）は既存箇所で使用中のため残置。

商品別KPI/RPP/データ取込み/商品マスタ・原価/目標設定の5画面に残っていた `gray`/`blue` 系は区切り5で新トークンへ機械的置換済み（2026-08-22）。GAP分析・請求・アカウント設定等の残りは対象外（GAP分析はQ1で当面残す方針、他は未着手）。ドリルダウン本体（`components/diagnosis/`）は区切り0〜5ですべて実装済み: HeroKgi（段1）→ DrillDown（段2〜5のコンテナ）→ KpiTriage/GenreDrill/ProductDrill/ActionRx。「今日やるべきこと」（TodayActions.tsx）は段5が後継のため削除済み。

### マスタCRUD規約（2026-08-22 決定・計画書 `docs/jisso_keikaku_input_ia_seiri_2026-08-22.md`）

対象3マスタ: `products`（商品マスタ）/ `product_categories`（カテゴリマスタ）/ `targets`（目標マスタ）。新しいマスタテーブルを追加するときもこの規約に従う。

- **削除は原則ソフトデリート（`archived_at`列）。** 物理削除しない。実績集計・過去データは保持し、一覧・診断・提案・ドリルダウン・アイテム別目標の母集団からは除外する
- **商品の状態はユーザー概念として「販売中／廃盤」の2値のみ**（`is_active`）。「アーカイブ」という状態・表記はUIに出さない。削除操作は単に「削除」と呼び一覧から消える。`archived_at` は内部実装専用（Q7）
- **母集団除外は共通関数に集約する。** `masters.inactive_management_nos()` が `is_active=False`（廃盤）と `archived_at is not None`（削除済み）の両方を判定して除外対象を返す単一の真実。**新しく商品の母集団を絞り込む場所を足すときは必ずこの関数を呼び、`is_active` や `archived_at` を個別に判定しないこと**（画面ごとに実装すると除外漏れが起きる）。`Target` は共通関数を経由しないクエリが多い（`db.query(Target).filter(...)`が dashboard/evaluation/gap_analysis/revenue_plan/period_utils に分散）ため、**新しく Target を参照するときは必ず `Target.archived_at.is_(None)` を明示的に条件へ加えること**
- **upsert（POST）は削除済み行を復活させる。** カテゴリ・目標マスタとも「同じキーで保存し直す」＝ユーザーの復元意思とみなし、`archived_at` を `None` に戻す（find-or-create の再利用。ユニーク制約があるため、削除済み行を残したまま同キーで新規作成すると制約違反になる）。復元専用のUIは無い
- **CSVエクスポート/インポートは商品マスタ（`routers/masters.py`）の作法が単一の型。** BOM付きUTF-8出力（Excelでそのまま開ける）／取込は `utf-8-sig → utf-8 → cp932 → shift_jis` の順に自動判別／キー列でupsert／削除済みはエクスポートに含めない／**インポートは追加・更新のみで行の削除は絶対にしない**（アップロード事故で既存データが消える事態を構造的に防ぐ）。ラウンドトリップ保証（エクスポート→無編集で戻す→同じ状態になる）を壊さないこと
- **CSVエクスポートで文字列セルを出力するときは必ず `csv_safe_cell()`（`backend/csv_utils.py`）を通す。** 対象は `=` `+` `-` `@` タブ・CR で始まる値の無害化（CSVインジェクション対策）。数値のみの列は対象外だが、自由入力列を追加した時点で必須になる
- **CSVルートは `/{key}` より前に定義すること。** FastAPIはルート定義順にマッチを試みるため、`GET /export` を `GET /{id}` の後ろに置くと `id="export"` としてパスパラメータに吸われる（実装時に踏んだ）
- **1行の検証エラーで全体をロールバックしない実装（アイテム別目標等、行ごとの厳密な検証があるCSV）は `db.begin_nested()`（SAVEPOINT）で1行ずつ隔離する。** 他行の取込を継続しつつ `error_rows` にエラー行番号と理由を返す
- **全削除系（実績データ全削除・サンプル削除等）は `ConfirmDeleteModal`（`components/ConfirmDeleteModal.tsx`）を使う。** `window.confirm` ではなくチェックボックス必須の確認画面を挟む（誤操作防止をもう1段強める）。1件ずつの行削除（商品・カテゴリ等）はここまで要求しない

### `.claude/agents/`

**⚠️ 2026-08-24訂正:** 以前ここには `backend-engineer` / `frontend-engineer` / `data-analyst`(読取専用) / `qa-debugger` という4エージェント構成を記載していたが、実体（`.claude/agents/*.md`）がこのリポジトリのどのブランチ・worktreeにも存在しないことが判明した（申し送り台帳ルール6が警告する「幽霊参照」の実例）。**「ウレシル社」体制（`pmo` / `planner` / `developer` / `reviewer` / `qa` / `security` / `designer` / `kpi-analyst` / `legal-finance` / `customer-success` / `infra-ops` の11エージェント + `kickoff` / `jisso-keikaku` / `release-check` / `security-check` / `price-check` の5定例スキル）を2026-08-24に導入し、この節はそちらに置き換えた。** 詳細・組織図は `.claude/README.md`、冒頭「🏢 社内体制」節も参照。

## 楽天RMS CSVフォーマット仕様（インポート処理の実装時は必ず参照）

### ① RPP広告レポート（週次）
- **文字コード:** Shift-JIS
- **skiprows:** 8（9行目がヘッダー、10行目からデータ）
- **期間の取得:** 5行目「集計期間: 全期間で集計 YYYY-MM-DD - YYYY-MM-DD」を正規表現でパース
  - パターン: `集計期間:.*?(\d{4}-\d{2}-\d{2})\s*-\s*(\d{4}-\d{2}-\d{2})`
  - ※「計測期間」という列名はデータ行に存在しない

- **主要列名（実際の列名）:**
  | 用途 | 列名 |
  |------|------|
  | 日付 | `日付`（形式: 2026年05月24日〜2026年05月30日）|
  | 商品管理番号 | `商品管理番号` |
  | クリック数 | `クリック数(合計)` |
  | 広告費 | `実績額(合計)` |
  | 売上金額 | `売上金額(合計720時間)` ★12時間版ではなく720時間を使う |
  | 売上件数 | `売上件数(合計720時間)` |
  | CVR | `CVR(合計720時間)(%)` |
  | ROAS | `ROAS(合計720時間)(%)` |

---

### ② 商品分析レポート（月次）
- **文字コード:** UTF-8 BOM付き（utf-8-sig）
- **skiprows:** 5（6行目がヘッダー、7行目からデータ）
- **期間の取得:** 3行目「表示期間,2026年05月から2026年05月」をパース
  - パターン: `表示期間,(\d{4})年(\d{2})月から`

- **主要列名（実際の列名）:**
  | 用途 | 列名 |
  |------|------|
  | 商品管理番号 | `商品管理番号`（RPPとの結合キー）|
  | 商品名 | `商品名` |
  | ジャンル | `ジャンル`（例: 靴 > 靴ケア用品 > 靴ひも）|
  | 売上 | `売上` |
  | 売上件数 | `売上件数` |
  | アクセス人数 | `アクセス人数` |
  | ユニークユーザー数 | `ユニークユーザー数` |
  | 転換率 | `転換率`（形式: "13.45%" → float変換時に%を除去）|
  | 客単価 | `客単価` |
  | 在庫数 | `在庫数` |
  | 在庫0日日数 | `在庫0日日数` |

---

### 両レポートの結合キー
- RPP `商品管理番号` ＝ 商品分析 `商品管理番号`（例: fs01, ns01）

### よくある間違い（禁止事項）
1. RPPの売上に `売上金額(合計12時間)` を使わない → 必ず `720時間`
2. RPPの期間を列から取得しない → 必ず5行目からパース
3. 商品分析をShift-JISで読まない → utf-8-sig
4. 商品分析の転換率をそのままfloat変換しない → %除去してから変換
5. skiprowsをRPP/商品分析で混同しない → RPP=8、商品分析=5
