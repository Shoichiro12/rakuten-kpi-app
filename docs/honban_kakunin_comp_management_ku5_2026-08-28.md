# 本番実機確認チェックリスト: 無償提供（comp）管理 区切り5

**✅ 2026-08-31、区切り5・機能クローズ。** ⑤（demo@移行）が完了し、③の「対象以外のアカウントが
無傷であること」も対象≠操作者のケースで完全に立証できた（詳細は各節末尾の「→完了」記載を参照）。
**①②④は「テスト用メールへ付与→確認→解除」の使い捨てフローとしては引き続き未実施**（8/29の
admin@自己付与・8/31のdemo@付与とも実際の恒久付与で、使い捨てテストフローそのものではない。
実運用上支障はないためこのまま実施しない判断とする）。⑥（security-status再確認）は別PR
（週次セキュリティチェックPR #79）で対応中。

対象: `docs/jisso_keikaku_comp_management_2026-08-28.md` の区切り5（本番デプロイ・検証・demo@移行）。
区切り1・2（バックエンド）は[PR #70](https://github.com/Shoichiro12/rakuten-kpi-app/pull/70)、
tenancy誤帰属バグ修正・バリデーション修正は[PR #72](https://github.com/Shoichiro12/rakuten-kpi-app/pull/72)、
区切り3（フロントエンド・`/admin`のcomp管理セクション）は[PR #73](https://github.com/Shoichiro12/rakuten-kpi-app/pull/73)
でマージ済み。3PRとも `main` にマージ済みだが、**このチェックリスト作成時点では本番デプロイの完了自体を
Claude Code側で確認できていない**（Renderの自動デプロイ状況にアクセス権限が無いため）。①に着手する前に
Renderダッシュボードでデプロイが緑（完了）になっていることを確認すること。

**①〜⑥の順で実施する。** ①〜④は`admin@ureshiru.com`でのログインが前提（管理者閲覧機能の
区切り6チェックリスト＝`docs/honban_kakunin_admin_viewer_ku6_2026-08-28.md`と同じ前提）。

---

**⚠️ 2026-08-29追記: ①〜④は「テスト用メールへ付与→確認→解除」の使い捨てフローとしては未実施だが、
別経路で部分的にカバーされた。** オーナーが本番で`admin@ureshiru.com`自身へcomp付与を実施（恒久付与・
削除しない）。パターンB（既存アカウント・`Subscription`行なし）を実際に通過し、閲覧モードのスクリーン
ショットで新規`Subscription`行の作成・`comp`ステータス表示の正常動作を確認できたが、**付与者=対象が
同一アカウントのため、③の「対象以外のアカウント（特にadmin@自身）が無傷であること」の検証としては
成立しない**（詳細は計画書§15）。③のこの項目は、対象≠操作者となる別ユーザーへの付与機会があるときに
改めて満たすこと。

**⚠️ 2026-08-31追記: ⑤（demo@移行）が完了し、対象≠操作者のケースでこの③の項目も完全に立証できた。**
詳細は下記⑤の実施結果を参照。⑥（security-status再確認）は別PR（週次セキュリティチェックPR #79）で
対応中。

---

## ① `/admin`でcomp管理セクションの表示を確認

- [ ] `https://app.ureshiru.com/admin` を開き、アカウント一覧の下に「無償提供の管理」セクションが
      表示されることを確認する
- [ ] 初期状態で一覧が0件なら「無償提供中のアカウントはありません」の空状態表示になっていること
      （既存のcomp付与実績がある場合は一覧に表示されることを確認）

## ② テスト用メールへの付与（note入力・確認ダイアログ）

**実顧客のメールは使わないこと。** 社内の検証用メール、または未登録の使い捨てメールを使う。

- [ ] メールアドレス・理由（note）を入力せずに「付与する」ボタンが押せない（disabled）ことを確認
- [ ] メールアドレス・理由の両方を入力すると「付与する」ボタンが押せるようになることを確認
- [ ] 「付与する」を押すと確認ダイアログが表示され、対象メール・変更内容（未契約→無償提供）・影響の
      説明が表示されることを確認
- [ ] チェックボックスを入れないと実行ボタンが押せないことを確認
- [ ] 実行すると成功メッセージが表示され、一覧に付与した行（メール・状態・付与した管理者・付与日時・
      理由）が追加されることを確認
      - 対象が既存アカウント（Supabase Authに実在）なら状態は「サインアップ済み」
      - 未登録メールなら「先行登録中（未サインアップ）」

## ③ アカウント一覧への反映確認

- [ ] 上部のアカウント一覧テーブルで、付与した対象の「課金状態」列が「無償提供」になっていることを確認
- [ ] **付与対象以外のアカウント（特に`admin@ureshiru.com`自身）の課金状態が変わっていないこと**を
      確認する（[PR #72](https://github.com/Shoichiro12/rakuten-kpi-app/pull/72)で修正した
      tenancy誤帰属バグ＝対象ではなく管理者自身にcompが付いてしまう不具合の本番での再発が無いことの確認）
- [ ] 対象が既存アカウントの場合、そのアカウントで実際にログインして「請求・プラン」画面を開き、
      バッジ「無償提供」・「無償提供中（期間の定めなし）」・「解約をご希望の場合」カードが
      **表示されない**こと・「お支払い方法の変更」ボタンが表示されないことを確認

**→ 2026-08-31、demo@ureshiru.comへの付与（⑤参照）で完了。** Chrome委任での確認: アカウント
一覧でdemo@=「無償提供」・admin@自身は不変（課金状態が変わっていない）、管理者閲覧モードで
demo@の「請求・プラン」画面を開き「無償提供中」表示を確認、ダッシュボードが402にならず正常表示。
**今回は付与者（admin@）≠対象（demo@）のため、PR #72が修正したtenancy誤帰属バグの本番再発防止
確認としてもこれで完全に立証できた**（8/29のadmin@自己付与では付与者=対象が同一のため区別
できなかった。計画書§15参照）。

## ④ 解除→再付与→解除の一巡

- [ ] ②で付与した行の「解除」ボタン→確認ダイアログ（対象・変更内容=無償提供→未契約・影響の説明）→
      チェック→実行、で一覧から消える（または解除済み扱いになる）ことを確認
- [ ] アカウント一覧の課金状態が「未契約」に戻ることを確認
- [ ] 対象が既存アカウントの場合、そのアカウントの「請求・プラン」画面で通常のプランカード
      （申し込みボタン）が再表示されることを確認
- [ ] 同じメールに再度付与し、正常に「無償提供」へ戻ることを確認（冪等でない新規行として記録されること。
      一覧に2件目の履歴が残っていても問題ない）
- [ ] 最後にもう一度解除し、**検証用に作った付与を本番に残さないこと**（テスト用データの片付け）

## ⑤ demo@ureshiru.comのcomp移行

**計画書§3「demo@ の移行手順」に従う。** 移行前に、現在`demo@ureshiru.com`が
`EXEMPT_TEST_EMAILS`経由で`status="trialing"`（期限つき）のまま動いていることを
`GET /api/billing/diagnose`等で再確認しておくとよい。

- [x] `/admin`のcomp管理画面から`demo@ureshiru.com`にcompを付与する
      （理由例:「社内デモ用アカウント」。§8のStripe整合チェックは通る＝demo@は
      `stripe_customer_id`を持たないため409にはならない想定）
      **→ 完了（2026-08-31 15:55、オーナー実施。付与理由「demo」）**
- [x] 付与後、`demo@ureshiru.com`でログインし`GET /api/billing/status`が`status: "comp"`を返す
      ことを確認（またはBilling画面で「無償提供」バッジを確認）
      **→ 完了。demo@自身のログインではなく、管理者閲覧モード（読み取り専用）でdemo@の
      「請求・プラン」画面を開き「無償提供中」表示を確認（Chrome委任）**
- [x] ダッシュボード等、通常の機能が引き続き使えることを確認（回帰なし）
      **→ 完了。閲覧モードでdemo@のダッシュボードが402にならず正常表示されることを確認**
- [x] **env変更（要）**: Renderの環境変数`EXEMPT_TEST_EMAILS`から`demo@ureshiru.com`を削除し、
      再デプロイする（他にEXEMPT対象のメールが残っている場合はそれらは残す。demo@のみ削除）
      **→ 完了。ただし手順を1点変更**: 「値からdemo@のみ削除」ではなく、**`EXEMPT_TEST_EMAILS`を
      変数ごと削除**した（demo@が唯一の登録だったため、値を空文字にするのと変数自体を削除するのは
      実質同じ結果＝`backend/billing.py`の既定値である空文字と同義。他にEXEMPT対象が増えたときは
      改めて変数を作ればよい）
- [x] 再デプロイ後、`demo@ureshiru.com`が引き続き無償で使えること（comp経由に切り替わっていること）
      を再確認する
      **→ 完了（上記のダッシュボード・Billing画面確認と同時に実施）**
- [x] `docs/unyou_exempt_test_emails.md`の台帳に削除日を記入し、「以後は`/admin`のcomp一覧が正の記録」
      と注記を追加する
      **→ 完了**
- [x] 万一問題が起きた場合のロールバック手順を認識しておく: Renderの`EXEMPT_TEST_EMAILS`に
      `demo@ureshiru.com`を戻すだけで即座に復旧できる（既存コードは触っていないため安全）
      **→ 認識済み（今回は不要だった）**

**⚠️ 手順の順序に関する教訓（2026-08-31）**: 正しい順序は「①compを付与する→②EXEMPT_TEST_EMAILSから
外す」であるべきだが、**今回は逆順（①EXEMPT変数を先に削除→②comp付与を後で実施）になった**。
`EXEMPT_TEST_EMAILS`削除の再デプロイが完了してから実際にcomp付与（15:55）が反映されるまでの
数分間、demo@ureshiru.comは「EXEMPTでもcompでもない＝通常の未契約ユーザー」として扱われ、
有料機能（`_paid`エンドポイント）に対して402が返る状態になっていた可能性がある。実害は小さい
（社内デモ用アカウントで、この数分間に実際にアクセスがあったかは未確認・実質的な問題は起きて
いない）が、**次回以降、同種の「無償提供の切り替え」を行うときは必ず「新しい無償化の仕組みを
先に有効化してから、古い仕組みを外す」順序を守ること。**

## ⑥ security-statusでcomp_grantsのok:true再確認

- [ ] `GET /api/security-status`（管理者アカウントでログイン後にアクセス、または`curl`）で
      `comp_grants`が`protected`配列に含まれ、`ok: true`であることを再確認する
      （[PR #70](https://github.com/Shoichiro12/rakuten-kpi-app/pull/70)マージ直後に一度確認済みだが、
      ⑤のRender再デプロイ後に念のため再確認しておく）

---

## 完了後にやること

- [x] 本チェックリストの結果を本ファイル冒頭に追記する（管理者閲覧機能ku6チェックリストと同じ形式）
- [x] `CLAUDE.md`の申し送り台帳・`docs/office_map.html`の軍令帳を「区切り5完了」に更新する
- [ ] 区切り4（`docs/unyou_exempt_test_emails.md`更新の正式反映・legal-financeレビュー実施）に着手するか判断する
      **→ 未着手のまま（今回のクローズ作業では判断していない。オーナー判断待ち）**
