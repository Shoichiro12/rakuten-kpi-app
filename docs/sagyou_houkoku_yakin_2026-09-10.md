# 夜勤 作業報告（2026-09-10）

## 前段: 普請（急務対応）

`docs/office_map.html` の `QUESTS` を確認したところ、`stamp:"wait"`（急務）は**0件**だった
（既存の急務は前回までにすべて解消済み。残るのは `stamp:"kouho"` の候補6件と `stamp:"later"`
の後日2件のみ）。着手対象が無いため、前段は何も行わなかった。

STATUS（領国・評定・守り・軍費）も実態と照合した:

- 評定「議案なし」… GitHub上のオープンPRが0件であることを確認。実態と一致、変更不要
- 守り「9/7検分 高0」… `security/security_check_2026-09-07.md` に「重大度『高』の新規指摘は無し」
  の記載を確認。実態と一致、変更不要
- 軍費「月 約$33」… 前回までの点検から変更なし（今回は再点検の対象にしていない）

いずれも変更不要だったため、この節での更新は無し。

## 後段: 巡回（発見専用フェーズ）

`docs/gunrei_kouho.md`（候補一覧・却下済み）と `docs/office_map.html` QUESTS を確認したうえで、
コード・ドキュメントを見回った。まず機械的なチェックを2本実施:

1. CLAUDE.md・`docs/*.md` が参照する `docs/*.md` / `security/*.md` / `.claude/*.md` ファイルパスを
   洗い出し、実在しないものを抽出 → 検出7件はすべて既知（Hallmark監査2件は意図的な「失われた記録」
   の参照としてルール6の対象外、`security_check_2026-08-03.md`と`security_check_2026-08-17.md`は
   CLAUDE.md本文で既に経緯説明済みの幽霊参照、`design-system/SKILL.md`はgitignore対象で存在しない
   のが正常、`mail_ureshiru_addresses_2026-07-30.md`は参照元が既に修正済みで残存確認できず、
   `security_check_YYYY-MM-DD.md`はsellerhub側（リポジトリ外）を指すプレースホルダ、
   `sagyou_houkoku_lp_renewal_2026-XX-XX.md`は計画書内の手順テンプレート）。新規の幽霊参照なし
2. `backend/`全体で使われている環境変数（`os.environ.get`/`os.getenv`）27件を洗い出し、
   `.env.example`・CLAUDE.mdへの記載有無を確認 → ほぼ全件が既に文書化済み、または軍令帳に
   既存候補として記録済み（`ALLOW_ORIGINS`）。`RMS_INBOX_DIR`はコード自体のdocstringで
   十分自己説明的、`MALWARE_SCAN_TIMEOUT`は既定値20秒で動く軽微な調整項目のため、いずれも
   候補に挙げるほどの実害はないと判断

その後コードとドキュメントを個別に見比べ、以下を新規候補として発見した（1件）:

- **`lp/index.html:23-24`のog:image TODOコメントが旧デザイン前提のまま**: コメントは
  「新デザイン（ライブデモ主体のヒーロー）に合わせて撮り直す」と書いているが、実際の
  マークアップ（`lp/index.html:340-372`）を確認したところ、2026-08-31のLPリニューアル
  （デザイン案2a・CLAUDE.md記載の「静置ヒーロー 1b′」）で`.hero`節は見出し＋バッジのみの
  完全な静止テキストになっており、インタラクティブなライブデモは`.hero`の外側、別の
  `.demo-band`節に分離されていることを実マークアップで確認した。TODOコメントは2026-08-24
  時点（エッセイ型刷新でヒーローとデモが一体だった頃）の設計を前提に書かれたまま、8/31の
  2a刷新（レイアウトのみの変更だったため、このコメント自体は更新対象に入らなかったと推測）
  を経ても取り残されている。放置すると、将来この宿題（1200×630の新og:image撮影）に
  着手する人が「ヒーロー内のライブデモ」を撮ろうとして実際の構造と食い違い、混乱する
  おそれがある。実害は小さい（誰も着手していない宿題のため優先度低）

`docs/office_map.html` QUESTS に `stamp:"kouho"` として1行追記、`docs/gunrei_kouho.md`
候補一覧に詳細行を追加した。既存の急務・候補・却下済み・CLAUDE.md記載事項とは重複しないことを
確認済み。

## 評定待ち

なし。

## 巡回で見つけた候補

1件（上記「後段: 巡回」参照）。夜勤は候補札（`kouho`）に手を出さない方針のとおり、
昇格・却下はオーナー判断を待つ。

## 次にやること

- `docs/office_map.html` QUESTS の急務が0件のため、次回の夜勤はまず既存の`kouho`候補が
  オーナーにより昇格されているかを確認するところから始まる
- 次回のセキュリティ定例チェックは2026-09-14（月）
