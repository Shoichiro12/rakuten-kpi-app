# 夜勤 作業報告（2026-09-23）

## 前段: 普請（急務対応）

`docs/office_map.html` QUESTS を確認したところ、`stamp:"wait"`（急務）は0件だった
（`stamp:"kouho"`25件・`stamp:"later"`2件のみ、うち3件は本日の巡回で追加）。CLAUDE.md
「📌 申し送り」台帳にも新規の未実装・未実施項目は見当たらなかった。**対象なし。普請は
実施しなかった。**

あわせてSTATUS（領国・評定・守り・軍費）を実態と照合した:
- 評定 = 「議案なし」… GitHub上のオープンPRは0件（`list_pull_requests`で確認）。変更不要
- 守り = 「9/21検分 高0」… `security/`配下の最新は`security_check_2026-09-21.md`のままで、
  今日時点でも最新。変更不要
- 軍費 = 「月 約$33」… `.claude/agents/infra-ops.md`のコスト内訳と一致（ただし本日の巡回で
  同ファイルの別の記述に古い箇所が見つかった。下記参照。コスト表自体には手を入れていない）

いずれもSTATUSの値自体は実態と一致していたため、STATUS側の更新は行っていない。

## 後段: 巡回（発見専用フェーズ）

`docs/gunrei_kouho.md`（候補一覧22件・却下済み1件）とCLAUDE.md申し送り台帳を読んだうえで、
既存の候補・却下済み・台帳記載と丹念に照合しながらコード・ドキュメントを見回った。新規に
裏取りできた候補は3件（上限どおり）。いずれも「ドキュメントの記述が、別の一次情報（CLAUDE.md
の決定記録）と食い違っている」型の実例。

1. **`.claude/agents/infra-ops.md:27`** — インフラ運用部エージェントの「守ること」節が
   「旧 Render(Oregon)・旧 Vercel の削除は未実施（申し送り参照）」と書いているが、参照先の
   CLAUDE.md（60行目）はこの作業を**完了（2026-08-24）**と明記している。エージェント自身が
   「申し送り参照」と書きながら、その参照先と正反対の内容を1ヶ月以上残している。

2. **`.claude/agents/infra-ops.md:26`** — 同じ節が「`EXEMPT_TEST_EMAILS` は本番
   `demo@ureshiru.com`」と書いているが、CLAUDE.md（管理画面からの無償アカウント招待、区切り5
   完了記録）とその運用台帳`docs/unyou_exempt_test_emails.md`によれば、この環境変数自体が
   **2026-08-31にオーナーがRenderから削除済み**で、demo@ureshiru.comは`/admin`画面からのcomp
   （無償提供）管理へ移行済み。本番に`EXEMPT_TEST_EMAILS`という変数は現存しない。

3. **`DEPLOY.md:7` と `DEPLOY.md:76`の自己矛盾** — 同一ファイル内で、冒頭（7行目）は
   「現状は『ログインで保護＋データは全員共有』。将来、各テーブルに`user_id`を足せば
   ユーザー別データに拡張できる設計」とマルチテナント分離が未実装であるかのように書く一方、
   ステップ4（76行目）は「データはユーザーごとに分離されている（マルチテナント）」と実装済み
   前提で書いている。CLAUDE.mdの「マルチテナント（ユーザー別データ分離）」節が示すとおり
   `tenancy.py`によるユーザー単位分離は2026-07から本番稼働中の基盤機能であり、7行目は
   その導入前の記述が取り残されたまま、76行目だけが後から実態に合わせて更新されたと見られる。

3件とも `docs/office_map.html` の QUESTS に `stamp:"kouho"` として追記し、`docs/gunrei_kouho.md`
の候補一覧に詳細（何が・どこで・なぜ・放置するとどうなるか）を追記した。修正・昇格・却下は行って
いない（オーナー裁可待ち）。

他に検討したが候補として挙げなかった論点（裏取りの結果、問題なしと確認したもの）:
- `.claude/agents/infra-ops.md`のコスト表（Render $7 + Supabase $25 + Cloudflare 約$1 = $33）
  … 既存候補（2026-08-28、Supabaseプラン食い違いの可能性）で既に扱われており重複のため対象外
- `.claude/agents/`の他ファイル（customer-success.md・designer.md・developer.md・
  kpi-analyst.md・legal-finance.md・planner.md・pmo.md・qa.md・reviewer.md・security.md）の
  記述と実装の突き合わせ … qa.mdの管理者権限記載漏れは既存候補（2026-09-21）で既出のため対象外、
  他ファイルに新規の食い違いは見つからなかった
- `.claude/README.md`の組織図・定例業務表 … 実態（11エージェント・5スキル）と一致、問題なし
- `backend/migrations.py`の`_USER_SCOPED_TABLES`辞書に新たな登録漏れがないか … 既存候補
  （2026-09-17、action_logs/rpp_action_checks）以外の漏れは無し

## 次にやること

- 本日追加した3件の候補について、オーナーが急務への昇格または却下を判断する
- 既存の急務候補25件のうち優先度の高いものがあれば、次回の夜勤の普請対象として昇格を検討する
