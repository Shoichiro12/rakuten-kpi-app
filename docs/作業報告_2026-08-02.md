# 作業報告 2026-08-02

対象タスク: アクション提案ロジック 第4段階v2（売上予算按分＋ギャップ逆算）。指示書v2と実装計画書（プロジェクトdocs `jisso_keikaku_uriage_gap_2026-08-02.md`）のレビュー承認を受けての実装。

**デプロイ状況: GitHubへpush済み（origin/main = `b7fc631`）。Renderの自動デプロイ完了と本番動作は未確認。**

## やったこと（3行）

年間売上予算を季節指数で月次按分し、「月次売上予算 → 必要アクセス → 想定広告費 → 許容額超過ならCVR・客単価の改善代替案」までを一気通貫で見せる機能を実装した。旧3-G（広告予算の按分・上限管理）は指示書v2で不要と確定したため作っていない。区切り1〜4を個別コミットにして全区切りでビルド・curl確認済み。

## コミット（origin/main上のハッシュ）

| ハッシュ | 内容 |
|---|---|
| `ae7d697` | 区切り1: 季節指数按分・予算年度起点・目標設定画面の按分プレビュー |
| `9013304` | 区切り2: 月次予算→必要アクセス→想定広告費の一気通貫（ダッシュボード新パネル） |
| `fd81a9d` | 区切り3: ギャップ逆算 案A/案B・アイテム別目標の整合性チェック |
| `b7fc631` | 区切り4: 新規店舗の導線・CLAUDE.md申し送り台帳更新 |

※開発環境（クラウド）からGitHubへの認証が無くpushできなかったため、パッチを `git am` でローカルリポジトリに適用してpushした。この過程でコミットハッシュが作業時（9fbeb51〜d27739a）から変わっている。CLAUDE.md台帳のハッシュは本報告と同時に実ハッシュへ修正済み。

## 変更ファイル（14ファイル、+1,270 / -17行）

### 新規

| ファイル | 役割 |
|---|---|
| `backend/revenue_plan.py` | 本体（648行）。季節指数・按分・店舗全体の目標CVR/客単価・ギャップ逆算・整合性チェック |
| `backend/routers/revenue_plan.py` | `GET /api/revenue-plan`（認証+課金ガード付きで登録） |
| `frontend/src/components/dashboard/RevenuePlanPanel.tsx` | ダッシュボードの一気通貫パネル＋許容広告費入力＋案A/案Bカード |

### 変更

| ファイル | 内容 |
|---|---|
| `backend/models.py` / `migrations.py` | `shops.annual_sales_budget`・`budget_year_start_month` 追加（`_EXTRA_COLUMNS`方式・冪等ALTER） |
| `backend/calculations.py` | 季節指数の信頼区分定数（24ヶ月/12ヶ月）とLiteral型 |
| `backend/routers/masters.py` | `/api/shops/me` のGET/PUTに2項目追加（0以下・nullで予算未設定に戻せる） |
| `backend/sample_data.py` | MonthlyItemSalesを2→14ヶ月分に拡張（12月1.40・3月1.15の季節カーブ）、デモ年間予算3,000万円 |
| `frontend/.../TargetSetting.tsx` | 年間売上予算・起点月の入力＋12ヶ月按分プレビュー表 |
| `frontend/.../Dashboard.tsx` / `api.ts` / `types/index.ts` | パネル組込み・APIクライアント・型定義 |
| `CLAUDE.md` | 申し送り台帳にv2の6決定を追記 |

## 見つけて直したバグ

既存コードのバグは今回発見していない。自分で入れかけて直した問題が2件。

| 影響度 | 内容 | 原因と対処 |
|---|---|---|
| 中 | サンプルデータの季節カーブが按分プレビューでほぼ読めない | 月次CVRを毎月0.8〜3.0%で引き直していたため、乱数ノイズが季節係数（±40%）を埋もれさせていた。商品ごとの基準CVRを固定し月±5%の揺らぎに変更。修正後は12月指数1.40が按分にそのまま出ることを実測確認 |
| 小 | UU母数不足デモ（ACC-001、UU<100）が12月に壊れる | 季節係数1.40でUU上限95×1.40=133となり100を超えるため。ACC-001のみ上限95で固定 |

## 調査・切り分けの結論

- 既存 `/api/evaluation/access-plan` は店舗全体の集計値から直接逆算する実装（計画書の案1側）で、アイテム積み上げはしていない。今回の必要アクセス算出はこれと同じ式で、実績CVR・客単価を目標値（Target手入力優先→MIN(現状,前年)）に差し替えた形。
- クラウド環境からのpush失敗はトークンの権限不足（環境要因）でコード側の問題ではない。

## 検証状況

| 項目 | 結果 |
|---|---|
| バックエンドimport確認・`npm run build`（型エラー0） | 実施済み・全区切りで通過 |
| 季節按分（14ヶ月データ→confidence=medium、指数がシェア合計1.0） | 実施済み（curl実測） |
| 予算年度起点の切替（1月→4月で12ヶ月窓が2025-04〜2026-03に変化） | 実施済み |
| 目標CVR/客単価のbasis 3系統（manual / rule / 混在の優先順位） | manual・ruleは実測確認。mixedは未検証（CVRのみ手入力等のケース） |
| ギャップ逆算3分岐（許容内 / 案Aのみ / 案A上限超過→案B） | 実施済み（許容100万/30万/0円で全分岐を実測） |
| 新規店舗案内（空DB→collect_data）・予算未設定（no_budget）・均等按分（3ヶ月→flat） | 実施済み |
| 既存APIの回帰（dashboard / alerts / matrix / access-plan / item-targets / shops）| 実施済み・全て200 |
| **実画面（ブラウザ）での表示確認** | **未実施**（curl・ビルドのみ。レイアウト崩れの可能性は残る） |
| **confidence=high（24ヶ月以上）の経路** | **未検証**（サンプルは14ヶ月。ロジックは分岐1箇所のみ） |
| **RPPフォールバック（source="rpp"）での按分** | **未検証**（商品分析なし・RPPのみの状態を作っていない） |
| **本番（Postgres/Supabase）でのカラム自動追加・動作** | **未実施**（デプロイ後に要確認） |

## 残作業

### 手作業が必要なもの

- Renderのデプロイ完了確認 → `GET /api/security-status` が `ok: true / unprotected: []` のままであること（新テーブルなし・shopsへの列追加のみなので問題ないはず）
- 本番デモアカウントのサンプルデータ再生成（データ取込み画面から）。再生成しないと14ヶ月データと年間予算デモが入らず、売上予算プランは no_budget 表示のまま
- 実画面での表示確認（ダッシュボードの売上予算プランパネル、目標設定の年間予算セクション）

### 判断待ち・別チケット（従来からのもの）

- 目標設定の一括入力UI、KW単位への粒度拡張（3-E'）、NATIONS 17パターン詳細アクション集、ABCランク分析・商品タイプ分離（いずれも前回報告から変化なし）

## 注意点・申し送り

- **広告予算の上限管理は「作らない」が確定仕様**（2026-08-02）。許容広告費は都度入力・保存しない。予算カラムを足したくなったら台帳のv2決定行を先に読むこと
- **按分値・季節指数は保存しない**。`/api/revenue-plan` が毎回動的算出する。DBに月次予算を保存する実装を後から足すと「保存値と算出値どちらが正か」問題が再発する
- CVR改善上限 = MAX(現状, 前年)、目標 = MIN(現状, 前年)。**MAXとMINで対になっている**のは意図的（過去に到達した水準を上限、保守的な水準を目標にする）
- 必要アクセス(UU軸)の不足を広告クリック(rpp_click軸)で1:1に埋める近似は既存access-planの前提を踏襲したもの。UIでは「試算」と明記している
- 設計ドキュメント `action_logic_unified_2026-08-01.md` はプロジェクトdocs・リポジトリ・ドライブのいずれにも見当たらない（第4段階は指示書v2・実装計画書から要件を再構成した）。原本があるなら次セッションのためにプロジェクトdocsへ入れてほしい
- 新カラム追加の規約（`_EXTRA_COLUMNS`・sample_data更新・デプロイ後security-status確認）は今回も踏襲した
