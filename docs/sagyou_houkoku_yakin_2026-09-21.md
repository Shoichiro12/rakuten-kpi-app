# 夜勤 作業報告（2026-09-21）

## 前段: 普請（急務対応）

`docs/office_map.html` QUESTS を確認したところ、`stamp:"wait"`（急務）は0件だった
（`stamp:"kouho"`19件・`stamp:"later"`2件のみ）。CLAUDE.md「📌 申し送り」台帳にも新規の
未実装・未実施項目は見当たらなかった。**対象なし。普請は実施しなかった。**

あわせてSTATUS（領国・評定・守り・軍費）を実態と照合した:
- 評定 = 「議案なし」… GitHub上のオープンPRは0件（`list_pull_requests`で確認）。変更不要
- 守り = 「9/21検分 高0」… 直近の週次セキュリティチェック（`security/security_check_2026-09-21.md`、
  コミット `2eb8f1f`）が今日付で「高0・中0・低0」を記録済み。変更不要
- 軍費 = 「月 約$33」… `.claude/agents/infra-ops.md`のコスト内訳と一致（今回は変更なし）

いずれも実態と一致していたため、STATUS側の更新は行っていない。

## 後段: 巡回（発見専用フェーズ）

コード・ドキュメントを見回り、`docs/gunrei_kouho.md`（候補19件・却下済み1件）・CLAUDE.md申し送り
台帳と照合したうえで、重複しない新規候補を3件発見した（上限の3件）。いずれも自分で裏取り
（該当行の実読・`git log`での実測）を行ったうえで記録している。

1. **`.claude/agents/qa.md:13,22`** — 本番検証チェックリストが `/api/health`・
   `/api/security-status`・`/api/billing/diagnose` を同格の無認証GETチェックとして並記しているが、
   `/api/billing/diagnose` は2026-09-03から `admin_guard.require_admin` 必須（管理者トークンが
   無ければ403）になっている。qaがこの非対称性を知らずに一般ユーザー・compユーザーのセッションで
   検証すると、この1項目だけ原因不明の失敗として扱われかねない。
2. **`docs/office_map.html:269`** — ファイル冒頭の「最終更新」コメントが2026-09-07のまま
   2週間以上更新されていなかった。中身（STATUS・QUESTS）は09-08〜09-21まで少なくとも8回のコミットで
   実際に更新され続けており、鮮度表示だけが実態と乖離していた。**このコメント自体は「確認すれば
   機械的に定まる事実の更新」に該当するため、今回のPRで併せて09-21へ更新した**（普請の対象カウントには
   含めていない）。
3. **`.claude/skills/security-check/SKILL.md:14`** — 「リポジトリ側`security/index.md`と
   プロジェクトdocs側`security_index.md`の二重管理問題は、どちらが正か決まるまで両方に同じ内容を
   書く」という運用指示が残っているが、この指示を実行する手段（旧Coworkルーチンの`project_write`
   相当）自体がどのセッションにも存在せず、CLAUDE.md本文では既に「repo側が正、sellerhub側への
   反映は不要」と決着済み。指示書側だけが2026-08-24時点の古い前提のまま取り残されている。

3件とも `docs/office_map.html` の QUESTS に `stamp:"kouho"` として追記し、`docs/gunrei_kouho.md`
の候補一覧に詳細（何が・どこで・なぜ・放置するとどうなるか）を追記した。修正・昇格・却下は行って
いない（オーナー裁可待ち）。

## 次にやること

- 上記3件の候補について、オーナーが急務への昇格または却下を判断する
- 既存の急務候補19件のうち優先度の高いものがあれば、次回の夜勤の普請対象として昇格を検討する
