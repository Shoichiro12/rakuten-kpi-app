# 夜勤 作業報告（2026-09-11）

## 前段: 普請（急務対応）

`docs/office_map.html` の `QUESTS` を確認したところ、`stamp:"wait"`（急務）は**0件**だった
（残るのは `stamp:"kouho"` の候補8件と `stamp:"later"` の後日2件のみ）。着手対象が無いため、
前段は何も行わなかった。

STATUS（領国・評定・守り・軍費）も実態と照合した:

- 評定「議案なし」… GitHub上のオープンPRが0件（`list_pull_requests` で確認）であることを確認。
  実態と一致、変更不要
- 守り「9/7検分 高0」… `security/security_check_2026-09-07.md` が現時点で最新の週次チェック
  （次回定例は2026-09-14）であることを確認。実態と一致、変更不要
- 軍費「月 約$33」… `.claude/agents/infra-ops.md` のコスト表（Render $7 + Supabase $25 +
  Cloudflare 約$1 + LP/メール $0 = $33）と一致することを再確認。変更不要

いずれも変更不要だったため、この節での更新は無し。

## 後段: 巡回（発見専用フェーズ）

`docs/gunrei_kouho.md`（候補一覧・却下済み）と `docs/office_map.html` QUESTS を先に読み、
既存の候補8件・却下済み1件を把握したうえで見回った。

機械的なチェックとして、`backend/` 全体で使われている環境変数（`os.environ.get`/`os.getenv`）を
洗い出し、`.env.example`（コメントアウト行も含む）への記載有無を突き合わせた。前回（9/10）の
巡回でも同種のチェックを行い「ほぼ全件が既に文書化済み」と結論していたが、今回は個々の変数の
**フォールバック値の危険度**まで見た点が異なる。その結果、次を新規候補として発見した（1件）:

- **`backend/billing.py:18`（`APP_BASE_URL`）が未文書化で、既定値が`http://localhost:5173`**:
  この環境変数は無償アカウント招待機能（2026-08-31導入）の招待リンク
  （`{APP_BASE_URL}/invite?t=<hashed_token>`、`routers/admin_comp.py::_build_invite_link()`）の
  組み立てに使われるが、`backend/.env.example` にも `.claude/agents/infra-ops.md`「守ること」節
  （本番Renderで死守すべき設定の一覧）にも一度も記載が無い（grep両方0件）。未設定時は開発用の
  `http://localhost:5173` にフォールバックし、かつメール送信自体はエラーなく成功するため、
  もしRenderのサービス再作成やenv棚卸しでこの変数が万一外れても、アプリ側・送信結果のどちらにも
  異常の兆候が一切出ない。これは2026-09-02に発覚した `CLOUDMERSIVE_API_KEY` 未設定インシデント
  （重要な本番env変数がインフラのチェックリストに載っておらず、本番未設定のまま数週間気づかれな
  かった）と同じ型のリスクである。**なお2026-09-04の本番検証では実際に`app.ureshiru.com/invite?t=...`
  形式で正しいリンクが届くことを確認済みのため、現時点でRender側の設定自体に問題があるわけではない**。
  問題は「その設定が正しいことを次に誰かが確認できる文書上の手掛かりが無い」点

`docs/office_map.html` QUESTS に `stamp:"kouho"` として1行追記、`docs/gunrei_kouho.md` 候補一覧に
詳細行（発見日2026-09-11）を追加した。既存の急務8件・候補・却下済み1件・CLAUDE.md記載事項とは
重複しないことを確認済み（`ALLOW_ORIGINS`の既存候補とは別変数・別リスク種別のため独立記録とした）。

## 評定待ち

なし。

## 巡回で見つけた候補

1件（上記「後段: 巡回」参照）。夜勤は候補札（`kouho`）に手を出さない方針のとおり、昇格・却下は
オーナー判断を待つ。

## 次にやること

- `docs/office_map.html` QUESTS の急務が0件のため、次回の夜勤はまず既存の`kouho`候補（現在9件）が
  オーナーにより昇格されているかを確認するところから始まる
- 次回のセキュリティ定例チェックは2026-09-14（月）
