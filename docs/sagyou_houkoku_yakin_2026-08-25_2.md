# 作業報告: 夜勤（2026-08-25・無人実行・2回目）

## 前提

CLAUDE.md「🌙 夜勤の掟」・`.claude/commands/yakin.md` に従い実行。現行スコープは**コード変更禁止**
（調査・ドキュメント・実装計画の下書きのみ）。ソースコード（`backend/`・`frontend/src/`）への
`Edit`/`Write` は行っていない。前段=普請・後段=巡回の2段構成で実行した（巡回は今回が実質初回）。

## 前段: 普請

1. `git status` / `git log` で現状確認。直近は `about.html` の共有利用補足の追加・撤回（PR #46〜#48）。
2. `docs/office_map.html` の軍令帳（QUESTS）を確認。急務（`stamp:"wait"`）は1件のみ:
   「CSVインジェクション普請のこと」。`m` 欄に「8/25夜勤で普請案の下書き済み・評定待ち」とあり、
   実体（`docs/jisso_keikaku_csv_injection_fix_2026-08-25.md`）を確認したところ、すでに調査・修正方針・
   オーナー確認事項3点が整理済みで、今回新たに足せる情報はなかった。
3. `.claude/commands/yakin.md` の手順「既に評定待ちで新規に足せる情報が無ければ、この段は何もせず
   後段（巡回）へ進む」に従い、**前段は何も行わず後段へ進んだ**。

## 後段: 巡回（発見専用）

`docs/gunrei_kouho.md`（候補一覧・却下済み）と `docs/office_map.html` QUESTS、CLAUDE.md申し送り台帳を
確認したうえで、コード・ドキュメントを見回り、以下2件の新規候補を発見した（重複チェック済み。
1晩3件までの上限内）。

### 候補1: `about.html`の「料金を見る」リンクが壊れている

- **場所**: `lp/about.html:91`（`<a class="btn-quiet" href="index.html#pricing">料金を見る</a>`）／
  `lp/index.html`（価格ブロック `<div class="price">`、306行目付近）
- **何が**: `index.html` に `id="pricing"` が存在しない（実際に存在するidは `main`/`demoBtn`/`ds1`/`ds2`/
  `faq` の5つのみ）。
- **なぜ問題か**: 2026-08-24のLPエッセイ型全面刷新で `index.html` の構造が変わった際、価格ブロックから
  `id="pricing"` が失われたが、`about.html` 側のリンクは更新されなかった。
- **放置するとどうなるか**: `about.html` から料金を探しに来た訪問者が「料金を見る」を押しても
  `index.html` の先頭に飛ぶだけで価格セクションへスクロールしない。CVに直結する導線が機能不全のまま
  公開され続ける。

### 候補2: `lp/README.md`が2026-08-24の変更に追随できていない

- **場所**: `lp/README.md`（ファイル構成節・デプロイ節）／`lp/shot-1-intake.jpg`〜`shot-5-learn.jpg`
- **何が**: (a) READMEの「shot-1〜5-*.jpg…掲載は主役工程の2.0・4.0のみ、1/3/5はファイルのみ保持」が
  実態と食い違う。`index.html`・`help.html`・`about.html` 全体を検索しても `shot-hero.jpg`
  （og:image用）以外の画像参照は0件で、shot-1〜5は全て未使用（計約386KB）。(b) デプロイ節の
  「Vercelの旧プロジェクトはStripe審査完了後に削除する」も、CLAUDE.md記載のとおり2026-08-24に
  旧Vercelプロジェクト（`ureshiru`/`ureshiru-lp`）は既に削除済みで古い。
- **なぜ問題か**: READMEは`lp/`の「現在の構成」を説明する唯一のドキュメントだが、直近2つの実態変化
  （エッセイ型刷新・Vercel削除）に追随できていない。
- **放置するとどうなるか**: 次に`lp/`を触るセッションがREADMEを信じてshot-2/4がまだ使われていると
  誤認する、または未使用ファイルに気づかず放置し続ける。Vercelについても「まだ削除していない」という
  誤った前提で作業してしまう可能性がある。

いずれも**発見のみ・修正はしていない**。`docs/office_map.html` QUESTS へ `stamp:"kouho"` として2件追加、
`docs/gunrei_kouho.md` の候補一覧へ詳細を追記した。昇格・却下はオーナー判断待ち。

## 評定待ち

なし（前段は既存の評定待ち事項をそのまま維持。後段の候補2件は「候補」であって「評定待ち」ではない
——候補の昇格・却下自体がオーナー判断のため、この報告では判断を求めていない）。

## 次にやること

- オーナーが `docs/gunrei_kouho.md` の新規候補2件を確認し、急務への昇格または却下を判断する
- 候補1（`about.html`リンク切れ）は軽微な修正で直せる見込みだが、夜勤の現行スコープ
  （コード変更禁止）では実装していない
- 候補2（README陳腐化）はドキュメント修正のみで完結する見込み

## 触れなかったもの（掟どおり）

- 本番環境変数・Stripe・Render/Supabase/Cloudflareのダッシュボード
- 価格・法的文面
- 破壊的操作を伴う実装
- ソースコードそのもの
- 候補札（`kouho`）の昇格・却下（オーナーのみが行う）
