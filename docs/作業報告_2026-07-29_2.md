# 作業報告 2026-07-29（2回目）

対象タスク: 機能ロック実装 / past_due専用UI / CLAUDE.md申し送り台帳の新設 / 独自ドメイン移行の完了（Supabase・Render・Stripe Webhook）

**デプロイ状況: すべて本番デプロイ済み。** コミット 471004f（機能ロック）は Render で「Deploy live」を確認。ドキュメント更新2件（e3003d9, f438a67）もプッシュ済み。

## やったこと（3行）

1. 未契約者が主要APIを使えない「機能ロック」を実装し、本番反映まで確認した（公開前必須の決定事項）。
2. past_due/unpaid を「未契約」と区別して案内する専用UIを実装した。
3. app.ureshiru.com への移行を完了した（Supabase Site URL/Redirect URLs、Renderドメイン検証、Stripe Webhook URL）。

## 変更ファイル（コミット 471004f、142行追加 / 23行削除）

| 区分 | ファイル | 内容 |
|---|---|---|
| 新規 | `backend/subscription_guard.py` | `require_active_subscription`。契約が trialing/active 以外なら 402 |
| 変更 | `backend/main.py` | `_paid` 依存を主要13ルーター＋3ルートに適用。例外は billing / account / consulting / feedback |
| 変更 | `frontend/src/lib/api.ts` | 402受信で `/billing` へリダイレクト（billing系は402を返さないためループなし） |
| 変更 | `frontend/src/pages/Billing.tsx` | past_due/unpaid 時の赤カード＋ポータル導線。新規登録カードは非表示 |
| 変更 | `CLAUDE.md` | 「📌 申し送り」台帳を新設（決定事項と実装状態の表） |

コード外の設定変更（ダッシュボード操作）:

| 対象 | 変更 |
|---|---|
| Supabase | Redirect URLs に `https://app.ureshiru.com/**` 追加（旧URLと併存、計2件）。Site URL も `https://app.ureshiru.com` に切替 |
| Render | カスタムドメイン app.ureshiru.com を Verify 再実行 → Verified / Certificate Issued |
| Stripe | Webhook 送信先 URL を `https://app.ureshiru.com/api/stripe/webhook` に変更。**既存送信先の編集なので whsec は不変**（Render環境変数の変更不要） |

## 見つけて直した問題

### past_due UI と機能ロックが「未実装のまま決定事項だけ消えていた」件（影響度: 大）

- 症状: past_due_ui_fix.md と require_active_subscription の設計書を渡した認識がオーナーにあるが、リポジトリにもセッション記録にも存在せず、実装もされていなかった。同種の「決まったはずが消える」事故が4回目。
- 原因: 決定事項がコードにもドキュメントにも書かれず、セッションの文脈が切れると消える運用だったため。
- 対応: 両方をゼロから実装（上表）。再発防止として CLAUDE.md 冒頭に「📌 申し送り」台帳を新設し、「決定したらその場で1行追加、実装したらコミットハッシュ付きで更新」をルール化した。

### 機能ロック検証時の偽陽性（影響度: 中、検証手順のバグ）

- 症状: 未契約状態で `GET /api/dashboard/summary` が 200 を返し、ロックが効いていないように見えた。
- 原因: このパスは存在せず、SPAフォールバックが index.html を200で返していた。実在するパスは `/api/dashboard`。
- 対応: 実在パスで再検証し、5状態×6エンドポイントのマトリクスで期待どおりの 402/200 を確認した。

## 検証状況

| 項目 | 結果 |
|---|---|
| ローカル: 契約なし/past_due/canceled → ロック対象API | 402（メッセージも未契約/支払い失敗で出し分け） |
| ローカル: trialing/active → ロック対象API | 200 |
| ローカル: billing/account/consulting/feedback | 契約状態によらず200（例外設定どおり） |
| ローカル: 認証無効（開発環境） | 素通し200（開発体験を壊さない） |
| 本番: Render「Deploy live for 471004f」 | 確認済み |
| 本番: `https://app.ureshiru.com/api/health` | 200 |
| Supabase 保存確認 | Redirect URLs 計2件、Site URL 切替とも画面上で確認 |
| **未検証**: 本番でのブラウザ実操作（未契約アカウントでログイン→402→/billing誘導） | Stripe審査中で新規登録フローが完走できないため未実施。審査完了後の実カードテストと合わせて確認する |
| **未検証**: 新Webhook URLへの実イベント配信 | 審査中でイベントが発生しないため未実施。審査完了後、Stripeの送信先ページでエラー率0%を確認する |

## 残作業

### 手作業が必要（オーナー側）

- SMTP設定: Gmailアプリパスワード発行 → Render `rakuten-kpi-app-sg` の環境変数に設定。設定後、本番のフィードバック窓口からテスト送信すればメール疎通確認を兼ねられる
- Stripe審査完了待ち（画面表示は2〜3日）。完了後: 実カードで 登録→trialing→請求書プレビュー（¥22,000・税額内訳・登録番号）→解約 の一連テスト

### 決定済み・時期待ち（詳細は CLAUDE.md 申し送り表）

- 専門家確認（法務文面・インボイス様式）: 審査完了と機能ロック実装後にまとめて手配、と決定済み。機能ロックは完了したので審査完了が残条件
- Stripe顧客の重複防止: 公開後でよい、と決定済み
- 旧Render(Oregon)・旧Vercelの削除: 新環境の安定運用を数日確認してから
- 4万SKU CSV取込のメモリ実測 → Starter/Standard判断

## 注意点・申し送り

- **新しいルーターを追加するときは `_paid` を使う。** `_auth` のみにすると未契約者に開放される。例外にしてよいのは「契約が無くても使えるべきもの」（契約導線・退会・問い合わせ）だけ。main.py にコメントで明記済み
- **Webhook の whsec は変えていない。** URL変更は既存送信先の編集で行ったため。送信先を作り直すと whsec が変わり Render の環境変数更新が必要になるので、今後も「編集」で行うこと
- 旧URL `rakuten-kpi-app.onrender.com` は Render Subdomain 有効のまま到達可能。Supabase Redirect URLs にも旧URLを残しているので、旧URL経由の認証メールリンクもまだ生きている。旧環境の削除時に合わせて整理する
- 法務・税務まわり（特商法・利用規約・適格請求書の様式）は専門家未確認。公開後早めに確認を入れる前提の状態
