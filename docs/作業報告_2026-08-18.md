# 作業報告 2026-08-18

対象タスク: セキュリティ残タスク3件（npm audit更新／CSPヘッダー追加／コンサル問い合わせフォームの文字数上限）。
`security/security_check_2026-08-17.md` の継続指摘を解消する作業。

**デプロイ状況: コミット済み・push/PR/本番デプロイは未実施。** タスクごとに3コミットに分けてあります
（`c5e38bc` consulting.py／`1a6fc7c` npm audit／`c9b3809` CSP）。この後push・PR作成・マージ・
本番デプロイ確認まで進めます（特にタスク2のCSPは本番でSupabase/Stripeの実オリジンが効くため、
本番での実測確認が必須です）。

---

## 先に結論

3件とも指示どおり対応できましたが、**指示書が参照していた `security/security_check_2026-08-17.md`
という原本ファイルが、このリポジトリのどのブランチ・履歴にも存在しませんでした**（詳細は下の
「見つけた問題」参照）。`security/` ディレクトリ自体を本セッションで新規作成し、`security/index.md`
に指摘事項の索引を作りました。

もう1件、react-router / react-router-dom の脆弱性はv6系にパッチが存在せず、v7への
メジャー移行が必要と判明したため、指示どおり**今回は見送り**にしています。

---

## 変更ファイル

| ファイル | 内容 |
|---|---|
| `backend/routers/consulting.py` | `InquiryPayload` の各項目に文字数上限チェックを追加 |
| `frontend/src/components/ConsultingInquiryForm.tsx` | 対応するinput/textareaに`maxLength`を追加 |
| `frontend/package-lock.json` | `npm audit fix`（--forceなし）による依存更新 |
| `backend/main.py` | `security_headers` ミドルウェアにCSPヘッダーを追加 |
| `security/index.md`（新規） | セキュリティ指摘事項の索引・解決状況の台帳 |

```
backend/main.py                                   | 49 +++++++++++++++++++++++
backend/routers/consulting.py                     | 32 +++++++++++++--
frontend/package-lock.json                        | 20 ++++-----
frontend/src/components/ConsultingInquiryForm.tsx |  8 ++--
4 files changed, 93 insertions(+), 16 deletions(-)
```
（`security/index.md` は新規ファイルのため上記diff統計には含まれていません）

---

## 見つけた問題（依頼にはなかったが報告するもの）

**指示書が参照する `security/security_check_2026-08-17.md` が実在しない**

作業前に `security/index.md` を読もうとしたところ、`security/` ディレクトリ自体が
このリポジトリに存在しませんでした。`git log --all -- 'security/*'` でも0件、
全ブランチ（`backup/masters-2026-07-23` 含む）を確認しても見つかりません。

- 影響度: 中（実害はないが、CLAUDE.md 申し送りルール4が禁じている「成果物が実装より先に
  失われる」パターンの再発です）
- なぜそうなったか: 推測ですが、2026-08-17のチェック自体は（別セッション・別ツール等で）
  実施されたものの、その結果ドキュメントをコミットしないまま次のセッションに引き継がれた
  と考えられます。指示書に書かれていた指摘内容（nanoid/postcss/vite/react-routerの脆弱性、
  CSP欠如、consulting.pyの上限欠如）は実際のコードと完全に一致していたため、チェック自体は
  実在した内容だと判断できます
- 対応: `security/index.md` を新規作成し、この欠落自体を記録したうえで、今回の3件の
  指摘と解決状況を書きました。元の `security_check_2026-08-17.md` の実体が別の場所
  （ローカルの別ディレクトリ、別セッションのチャットログ等）から見つかった場合は、
  このディレクトリに追加してください

---

## 検証状況

| 項目 | 結果 |
|---|---|
| `cd backend && py -3 -c "from main import app"` | OK（3コミットとも） |
| `cd frontend && npm run build`（tsc型チェック） | 型エラー0 |
| npm audit（作業前） | 5件（high 3: nanoid/postcss/vite、moderate 2: react-router/react-router-dom） |
| npm audit（作業後） | 2件（moderate: react-router/react-router-dom のみ） |
| consulting.pyの文字数超過バリデーション | ローカルuvicornで実行し、5000字超のmessageで400＋日本語エラー、正常値で200を確認 |
| ローカル実画面（vite dev、5画面） | ダッシュボード/targets/import/products/rpp、コンソールエラー0 |
| ローカル実画面（本番同様＝backend単体配信＋frontend/dist、5画面） | ダッシュボード/targets/import/billing/products/rpp、コンソールエラー0、CSP violation 0件 |
| CSPヘッダーの応答確認（curl） | `Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data:; font-src 'self' https://fonts.gstatic.com; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'` を確認（ローカルはSupabase未設定のためconnect-srcは'self'のみ） |
| Google Fontsの実読み込み | `document.fonts` で Roboto / Noto Sans JP が `status: "loaded"` であることを確認（CSPでブロックされていないことの直接証拠） |
| **本番デプロイ確認** | **未実施**（この報告書の時点ではpush前）。特にCSPはSupabase/Stripeの実オリジンが本番でしか効かないため、次のアクションとして必須 |

---

## CSPポリシー全文（本番でこのまま出るヘッダー値）

```
Content-Security-Policy:
default-src 'self';
script-src 'self';
style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
img-src 'self' data:;
font-src 'self' https://fonts.gstatic.com;
connect-src 'self' https://<SUPABASE_URLのホスト> wss://<SUPABASE_URLのホスト>;
frame-ancestors 'none';
base-uri 'self';
form-action 'self'
```

`connect-src` のSupabaseオリジンは環境変数 `SUPABASE_URL`（無ければ `VITE_SUPABASE_URL`）
から実行時に導出しています（`auth.py`/`routers/account.py` と同じ優先順位）。ハードコード
していないので、本番のSupabaseプロジェクトを変更してもコード変更は不要です。想定外の
追加接続先が必要になった場合は、新しい環境変数 `CSP_CONNECT_SRC`（カンマ区切り）で
追加できます。

`script-src` に `'unsafe-inline'` / `'unsafe-eval'` は入れていません。棚卸しの結果、
インラインscriptを使っている箇所が見つからなかったためです。もし本番で動的な
インラインscript実行が必要な箇所が後から見つかった場合は、nonceベースの許可を
検討してください（`'unsafe-inline'` を足すのは最後の手段にすること）。

`style-src` にだけ `'unsafe-inline'` を残しています。Viteのビルド出力・一部ライブラリが
インラインstyleを使うためで、外すには個別に洗い出しが必要です（今回のスコープ外）。

---

## 残作業

**コード側で対応不可（判断待ち・別作業）**

- react-router / react-router-dom のv7移行（今回見送り。`security/index.md` に記録済み）
- `style-src` から `'unsafe-inline'` を外せるか（別チケット。今回未着手）

**次のアクション（このセッションでこの後やります）**

1. push → PR作成
2. マージ → Render本番デプロイの反映待ち
3. **本番でCSPヘッダーとconnect-srcのSupabaseオリジンを実測確認**（ログイン→ダッシュボード→
   CSVインポート→ログアウトの一連をヘッドレスChromeで実行し、DevTools ConsoleにCSP
   violationが出ないこと。出て即時に直せない場合はCSPコミットのみrevertして再報告）
4. CLAUDE.md 申し送り表に1行追加

---

## 注意点・申し送り

- CSPの `connect-src` はSupabaseの実URLに依存するため、**ローカル（Supabase未設定）での
  検証だけでは不十分**です。本番デプロイ後の確認を飛ばさないでください
- `_CSP_EXEMPT_PATHS`（`/docs`, `/redoc`, `/openapi.json`）はCSP自体を丸ごと外しています。
  `ENABLE_DOCS=1` を本番で有効にする運用は元々想定されていませんが、もし将来有効にする
  場合はこの3パスにだけCSPが掛かっていないことを踏まえてください
- `security/index.md` は本セッションで新規作成した索引です。今後セキュリティ関連の
  指摘・対応を行う際はここに追記していく運用にしてください（CLAUDE.md 申し送り表と
  役割分担: CLAUDE.mdは製品全体の意思決定ログ、`security/index.md` はセキュリティ指摘の
  台帳という切り分けです）
