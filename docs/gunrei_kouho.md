# 軍令候補（巡回で見つけたもの）

夜勤の「巡回」フェーズ（`.claude/commands/yakin.md` 参照）が毎晩コード・ドキュメント・
CLAUDE.md申し送り台帳を見回って発見した問題の記録。**発見のみで、修正はしない。**

候補の扱い:
- 昇格（`docs/office_map.html` QUESTS の急務 `stamp:"wait"` に格上げ）・却下は**オーナーのみ**が行う
- 夜勤は候補の札（`stamp:"kouho"`）に手を出さない。昇格も却下も自分ではしない
- 1晩あたり新規3件まで。既存の軍令（QUESTSの急務・後日）・候補（このファイル）・
  台帳（CLAUDE.md）記載と重複するものは挙げない
- 巡回は新しい候補を書く前に、必ず本ファイルの「却下済み」節と照合し、
  一度却下されたものを再提案しない

## 候補一覧

| 発見日 | 場所（ファイル:行） | 何が | なぜ問題か | 放置するとどうなるか | 状態 |
|---|---|---|---|---|---|
| 2026-08-25 | `lp/about.html:91`／`lp/index.html`（価格ブロック、306行目付近、`id="pricing"`なし） | `about.html` の「料金を見る」ボタンが `href="index.html#pricing"` を指すが、`index.html` に `id="pricing"` が存在しない（`main`/`faq`のidはあるが`pricing`は無い） | 2026-08-24のLPエッセイ型全面刷新で`index.html`の構造が変わった際、価格ブロック（`<div class="price">`）から`id="pricing"`が失われたが、`about.html`側のリンクは更新されなかった（アンカーリンク切れ） | `about.html`から料金を探しに来た訪問者が「料金を見る」を押してもページ先頭に飛ぶだけで価格セクションへスクロールしない。CVに直結する導線が機能不全のまま公開され続ける | **実装済み（2026-08-26夜勤・`lp/index.html`の価格ブロックに`id="pricing"`を追加）** |
| 2026-08-25 | `lp/README.md`（ファイル構成節・デプロイ節）／`lp/shot-1-intake.jpg`〜`shot-5-learn.jpg`（計約386KB） | (a) READMEの「shot-1〜5-*.jpg…掲載は主役工程の2.0・4.0のみ、1/3/5はファイルのみ保持」という記述が実態と食い違う——2026-08-24のエッセイ型刷新で「5つの工程」セクション自体が廃止され、`index.html`内の画像参照は`shot-hero.jpg`（og:image）のみになり、shot-1〜5は全て未使用。(b) 同READMEのデプロイ節「Vercelの旧プロジェクトはStripe審査完了後に削除する」という未来形の記述も、CLAUDE.md記載のとおり2026-08-24に旧Vercelプロジェクトは既に削除済みで古い | READMEは`lp/`の「現在の構成」を説明する唯一のドキュメントだが、直近2つの実態変化（エッセイ型刷新・Vercel削除）に追随できていない | 次に`lp/`を触るセッションがREADMEを信じてshot-2/4がまだ使われていると誤認する、または未使用ファイルに気づかず放置し続ける。Vercelについても「まだ削除していない」という誤った前提で作業してしまう可能性がある | **実装済み（2026-08-31夜勤・`lp/README.md`のファイル構成節を実態〈shot-hero.jpgのみ使用・shot-1〜5全未使用〉に修正、デプロイ節のVercel削除記述を過去形に修正）** |
| 2026-08-27 | `frontend/src/pages/Login.tsx:139,152`／`pages/ResetPassword.tsx:66,79`／`pages/AccountSettings.tsx:162,188,201,248`／`components/ConsultingInquiryForm.tsx:97,101,120,130` | ログイン・パスワード再設定・アカウント設定・コンサル問い合わせの各フォームで、`<input>` に `id`/`name` 属性が付いていない（`autoComplete` は一部設定済みだが `id`/`name` が無い）。`<label>` も `htmlFor` で紐付いていない | ブラウザDevToolsが「A form field element should have an id or name attribute」を警告する状態。`id`/`name` が無いとパスワードマネージャーがユーザー名/パスワードの組を正しく認識できないことがあり、`autoComplete` 属性の効果も薄れる。`label`と`input`が`htmlFor`/`id`で結びついていないためスクリーンリーダーでラベル読み上げが働かない（クリックでのフォーカス移動もできない）a11y上の欠陥でもある | パスワードマネージャーの自動入力・保存の精度低下（特にログイン・パスワード変更フォームは実害が大きい）が改善されないまま残る。スクリーンリーダー利用者にとってラベルとフィールドの対応が取れない状態が放置される | 候補（未裁可） |
| 2026-08-28 | `backend/routers/admin.py:135,179`／`backend/admin_view.py:54`（`datetime.utcnow()`。より広くは`models.py`の`DateTime`列全般が同じ性質） | `AdminViewSession`等の日時フィールドが`datetime.utcnow()`（タイムゾーン情報を持たないnaive datetime）でシリアライズされ、JSONでは`"2026-08-27T16:16:37.857329"`のようにZやオフセットが付かないISO文字列で返る | JSON上でこの時刻がUTCなのかサーバーのローカル時刻なのか、値だけでは区別できない。フロントで素朴に`new Date(value)`へ渡すとブラウザのタイムゾーンとして解釈される。実測確認済み（管理者閲覧機能 区切り3の受け入れ時）: JST環境でZ補完なしに解釈すると32,400,000ms＝ちょうど9時間のずれが生じる | 今回は`frontend/src/lib/adminView.ts`の`parseServerDate()`で個別に吸収したが対症療法。バックエンドAPI全体でISO8601のタイムゾーン明示（Z付き）に標準化されていないため、今後日時を返す新しいAPIを追加するたびに、実装者がこの慣習を知らなければ同じ9時間ずれのバグを繰り返し踏む設計上の負債 | 候補（未昇格） |
| 2026-08-28 | `.claude/agents/infra-ops.md:3,16`（コスト表「DB・認証 \| Supabase Pro（東京） \| $25」）。`CLAUDE.md:129`が軍費$33の内訳としてこの数値を引用・STATUS点検で「変更不要」と判定済み | インフラ運用部の担当描写・コスト表がSupabaseを「Pro（東京）$25/月」としているが、オーナーによると実態は無償枠（FREE/NANO）プランの可能性がある食い違いが指摘された | 軍費（月額約$33）の内訳の正当性がこの前提に依存しており、もし実際はFREE/NANOならコスト試算そのものが誤っている。過去に`render.yaml`の`plan: free`が実態（Starter）と食い違っていた事故と同型（ドキュメントの記載が実態と乖離する再発パターン） | 誰かがこの記述を信じて予算判断や請求額の見込みを立てると誤った前提で意思決定してしまう。Supabaseダッシュボードでの実プラン確認が必要だが、Claude Codeにはアクセス権限が無く自力では確認できない | 候補（未昇格。オーナーによる実プラン確認が先） |
| 2026-08-28 | （新規提案。既存ファイルなし。関連: `.claude/commands/yakin.md`の掟・`docs/honban_kakunin_*.md`系のオーナー向けチェックリスト群） | 本番デプロイ後の定型確認（Render/Supabaseの状態確認等、`docs/honban_kakunin_comp_management_ku5_2026-08-28.md`等のチェックリストが要求する類の読み取り専用作業）を、Cowork（無人・定期実行ではない委任先）に委任できないかというオーナー提案 | 明確な不具合ではなく効率化の余地。現状はオーナーが毎回手動でチェックリストを実施しており、読み取り専用の確認作業自体は委任可能かもしれない | 現状のまま手動確認が続くだけで実害はない（優先度低はこのため）。委任化するなら読み取り専用の確認手順書（Render/Supabaseへの書き込みを一切行わない設計）を別途整備する必要がある | **解決済み（2026-08-29）**: PR #75の本番確認で実際にCowork/Chrome拡張への委任を実施（Claude Codeセッションが本番URLへ疎通できなかったための代替手段として発生）。読み取り専用+管理者閲覧モードの開始/終了ボタンのみという許可範囲で機能することを確認し、手順書 `docs/unyou_cowork_honban_kakunin_2026-08-29.md` として定型化した。`docs/office_map.html` QUESTSからは除去済み（候補としては解消） |
| 2026-08-31 | `lp/index.html`（`<link rel="stylesheet" href="style.css">`。about.html/help.html/tokushoho.html/privacy.html/terms.htmlの各ファイルも同様） | `lp/style.css` への参照にキャッシュバスター（`?v=日付`等）が付いていない | LPリニューアル(2a)本番反映直後の検証で、ブラウザが保持していた旧`style.css`のキャッシュにより下層ページのヘッダーが旧色（生成り系）に見える事象を確認した（オーナーのキャッシュ強制無視での再読込で解消することを確認済み・実害は一過性）。静的HTMLでビルド工程が無いため、CSSを変更してもファイル名が変わらずブラウザ・CDN双方に古いキャッシュが残りうる | 次にstyle.cssの見た目に関わる変更をしたとき、同じ「変更が反映されない」に見える事象が起き、原因調査に時間を取られる可能性がある。実害は小さい（強制リロードで解消・最終的にはキャッシュ有効期限切れで自然解消）ため優先度は低い | 候補（未昇格。優先度低。次にstyle.cssを触るときに同梱） |
| 2026-09-01 | `docs/jisso_keikaku_comp_invite_2026-08-31.md:185` | 計画書「## 8. 注意」節が `Gmail側の「名前を指定して送信」に登録済みであることが条件（2026-07-30に3アドレス登録済み。`docs/mail_ureshiru_addresses_2026-07-30.md`）` と書いているが、この参照先ファイルが本リポジトリの全ブランチ・全履歴に一度も存在しない（`git log --all --diff-filter=A --name-only` で確認。CLAUDE.mdルール6が警告する「幽霊参照」の型） | 2026-07-30時点でGmailの「名前を指定して送信」に3アドレス（`info@`/`sales@`等）を登録した作業自体はCLAUDE.md本文の随所（招待メール差出人・認証メール差出人の記述）から実施済みと分かるが、その登録内容を記録したドキュメントは最初から作られなかった可能性が高い。参照だけが独り歩きしている状態 | 次にこの計画書を読むセッションが詳細を確認しようとしてもファイルが無く、「登録済み3アドレス」の具体的な中身（どのアドレスをいつ・誰が登録したか）を追跡する手段が無い。Gmail側の「名前を指定して送信」設定が誤って変更・削除された場合、復旧の手掛かりとなる記録が存在しない | **解消済み（2026-09-02・オーナー指示）**: 実体はリポジトリ内ファイルではなくClaudeプロジェクト内の資料（リポジトリ外）で管理されているとの確認を受け、計画書の参照先を「Claudeプロジェクト内の資料（リポジトリ外）」に書き換え、リポジトリ内ファイルへの偽装参照を解消した。ファイルを新規作成する対応は取らない（実体がリポジトリ外にある以上、リポジトリ内に空のダミーを作ってもかえって紛らわしいため） |
| 2026-09-01 | `backend/malware.py`（機能全体、2026-08-22導入）／`backend/routers/masters.py:485,581`／`backend/routers/targets.py:90`／`backend/routers/item_targets.py:313` | (a) アップロードCSV/ZIPのマルウェアスキャン機能（Cloudmersive API、`CLOUDMERSIVE_API_KEY`等の環境変数で制御）がCLAUDE.md・`docs/`配下のどこにも一切記載が無い。(b) 実カバレッジにも漏れがある——`backend/routers/import_csv.py`内の4箇所は`scan_bytes()`を呼ぶが、同じ2026-08-22（マスタ設計統一）に追加されたカテゴリマスタCSV取込（`masters.py:485` `import_categories`）・商品マスタCSV取込（`masters.py:581` `import_master_products`）・目標マスタCSV取込（`targets.py:90` `import_targets`）・アイテム別目標CSV取込（`item_targets.py:313` `import_item_targets`）の計4エンドポイントは`await file.read()`の直後に`scan_bytes()`を呼んでいない（該当ファイルに`scan_bytes`のimportすら無い） | `malware.py`のdocstringはこの機能を「Stripe 審査の『マルウェア対策』項目のエビデンスになる」ものと位置づけている。CLAUDE.mdに記載が無いため、次にCSVアップロード系エンドポイントを追加する実装者がこの規約自体を知らずスキャン無しで実装してしまう可能性が高く、実際に4エンドポイントで既にそれが起きている。また本番Render環境変数`CLOUDMERSIVE_API_KEY`が実際に設定されているかもCLAUDE.mdに記録が無く確認できない（未設定ならimport_csv.py側の4箇所も含め全て無効なまま気づかれない） | 新しいCSVアップロード機能が追加されるたびにスキャン漏れが積み重なる。Stripe審査でエビデンスとして提示した場合、実際には主要な取込み経路の半数近くがスキャン対象外という説明のつかない状態になる | **解消済み（2026-09-02夜勤）**: (b)の懸念が的中し、9/2にオーナーがRender環境変数を確認したところ `CLOUDMERSIVE_API_KEY` が未設定＝本番で`import_csv.py`側の4箇所も含め全エンドポイントのスキャンが無効だったと判明（キーは9/2にオーナーが設定済み）。同日夜勤の普請で3点とも実装完了: ①`masters.py`（カテゴリ/商品マスタimport）・`targets.py`（目標マスタimport）・`item_targets.py`（アイテム別目標import）の4エンドポイントに`await file.read()`直後の`scan_bytes()`を追加 ②CLAUDE.mdマスタCRUD規約に「CSV/ZIPを受け取るエンドポイントは必ず`scan_bytes`を呼ぶ」規約を明記 ③`GET /api/security-status`に`malware_scan_active`（キー設定有無）を追加。**実装中に`grep -rn "UploadFile" routers/*.py`で全アップロード先を洗い出したところ、当初の想定に無かった5件目（`routers/costs.py`の原価率CSV一括登録）も同じ穴が見つかり、同じ夜勤内で追加修正した**（詳細はCLAUDE.md該当行）。ローカル検証済み。`docs/office_map.html` QUESTSからは除去済み |
| 2026-09-03 | `CLAUDE.md:102`（「ウレシル社」サブエージェント体制の導入・2026-08-24エントリ） | CLAUDE.mdは「`.claude/skills/` はリポジトリの `.gitignore`（18-19行目）で追跡除外なので、新設した5スキルの `SKILL.md` はこのマシン上のみ有効でコミットされない…別環境でこの体制を使うには `.claude/skills/{5スキル}/` を都度コピーする必要がある」と書いているが、実際には5スキルとも `git ls-files` でリポジトリに追跡済みであることを確認した。原因を`git log -p --follow -- .gitignore`で辿ると、この記述と同じ導入コミット`e4363f3`（コミットメッセージ「サブエージェント体制（ウレシル社）を導入、幽霊参照を訂正、**skillsのgit追跡を有効化**」）自体が`.gitignore`を`.claude/skills/`（丸ごと除外）から`.claude/skills/design-system/`（design-systemのみ除外）へ書き換えており、5スキルは意図的に追跡対象に変更されていた | CLAUDE.mdの記述が、同じコミットが行った変更と正反対のことを主張している（申し送りルール6が警告する「記述と実装の食い違い」の実例）。実際にはこのリポジトリをcloneするだけで5スキルはついてくるのに、記述を信じたセッションが「別環境では都度コピーが必要」という誤った前提で余計な作業をしたり、逆に「コミットされないはずなのになぜ手元にあるのか」と混乱したりしうる。関連して同ファイル281行目「`.agents/` `.claude/skills/` `skills-lock.json` は第三者コンテンツのため**gitignore済み・コミットしない**（2026-08-03決定）」も、`.claude/skills/`が今は`design-system/`サブディレクトリのみの除外に変わっているため、2026-08-03時点の決定としては正しいが現状の`.gitignore`全体を指すと誤解を招く言い回しになっている | 候補（未昇格） |
| 2026-09-08 | `backend/main.py:136-139,143`（`_default_origins`/`_extra_origins`／環境変数`ALLOW_ORIGINS`） | CORSの許可オリジンは `_default_origins`（`localhost:5173`/`3000`の2件固定）に加え、env `ALLOW_ORIGINS`（カンマ区切り）で追加できる仕組みがある。コード自身のコメント（136-137行目）は「同一サービスでフロントを配信する構成では本来CORS不要だが、フロントを別ドメインに置く場合に備え」た拡張だと説明しているが、この`ALLOW_ORIGINS`という変数名・存在は`CLAUDE.md`・`backend/.env.example`・`.claude/agents/infra-ops.md`のいずれにも一度も記載が無い（grepで確認、3箇所ともヒット0件） | CLAUDE.md「アーキテクチャ」節は「CORSは `localhost:5173`/`3000` のみ許可」と言い切っており、この拡張経路の存在を示唆していない。現在の本番構成（backendが`frontend/dist`を同一オリジンで配信）ではCORS自体が実質不要なため実害は無いと考えられるが、将来モール横展開やフロントの別ドメイン分離（VISION.mdの将来像）でこの変数が必要になったとき、存在を知る手がかりがコードのコメント以外どこにも無い | 別ドメインfrontendを立てる将来のセッションが「CORSはlocalhostのみ」というCLAUDE.mdの記述だけを信じ、`ALLOW_ORIGINS`という既存の解決策に気づかず車輪の再発明をする、あるいは原因調査に時間を浪費する可能性がある。実害は小さい（現状は同一オリジン配信のため使われていない可能性が高い）ため優先度は低い | 候補（未昇格。優先度低） |
| 2026-08-28 | `backend/.env.example:29` | `ADMIN_USER_ID` の説明コメントが「ここに設定した Supabase ユーザーの UUID（1件のみ）だけが **GET** /api/admin/\* を使える。」と書かれているが、実際には無償提供（comp）管理の書き込みAPI（`POST /api/admin/comp-grants`・`POST /api/admin/comp-grants/{id}/revoke`。`backend/routers/admin_comp.py`、`admin_guard.require_admin_write` 経由）も同じ `ADMIN_USER_ID` 判定で保護されており、GETに限らない | コメントは2026-08-26の管理者閲覧機能（読み取り専用の閲覧モード）導入時に書かれたもので正しかったが、2026-08-28の無償提供（comp）管理機能追加で `/api/admin/*` 配下にPOST（書き込み）エンドポイントが増えた際、このコメントは更新されなかった。GETのみと書かれていると、読んだ人が「このUUIDが漏れても読み取りしかできない」と誤解しかねないが、実際は無償提供の付与・解除という金銭的影響のある操作も同じ1つのUUIDで保護されている | 次にこの環境変数を扱う人（オーナー・別セッション問わず）が、コメントを信じて `ADMIN_USER_ID` の重要度を「閲覧専用の軽い権限」と過小評価するおそれがある。実際は付与操作まで含む強い権限のため、値の管理（誰が知っているか、Renderでの見え方等）への注意水準もそれに応じて引き上げるべき情報が、コメントの記載不足で伝わらない | 候補（未昇格） |
| 2026-09-10 | `lp/index.html:23-24`（og:image直前のTODOコメント）／実際のマークアップ: `lp/index.html:340-372`（`.hero`節と`.demo-band`節） | TODOコメントは「新デザイン（**ライブデモ主体のヒーロー**）に合わせたog:imageを撮り直す」と書いているが、2026-08-31のLPリニューアル（デザイン案2a・CLAUDE.md記載の「静置ヒーロー 1b′」）で`.hero`節はh1見出し＋バッジのみの完全な静止テキストになり、インタラクティブなライブデモ（「今週の売上 vs 予算」のカード＋詳しく見るボタン）は`.hero`の外側、別の`.demo-band`節に分離されている（実マークアップで確認済み） | TODOコメントは2026-08-24時点（エッセイ型刷新でヒーローとデモが一体だった頃）の設計を前提に書かれたまま、8/31の2a刷新（レイアウトのみの変更のため本文のTODOコメント自体は更新対象に入らなかった）を経ても取り残された。コメントが参照する計画書`docs/jisso_keikaku_lp_essay_2026-08-23.md`区切り2も8/24時点の設計のもの | 将来この宿題（1200×630の新og:image撮影）に着手する人が、コメントを信じて「ヒーロー内のライブデモ」をキャプチャしようとし、実際には静止テキストのみの`.hero`と別節の`.demo-band`のどちらを・どう合成して撮るべきか迷う、または誤って`.demo-band`単体を撮ってヒーローの見出しが写らない画像を作ってしまう可能性がある。実害は小さい（今のところ誰も着手していない宿題のため） | 候補（未昇格。優先度低。次にog:image撮影に着手するときに合わせて文言修正） |
| 2026-09-11 | `backend/billing.py:18`（`_APP_BASE_URL = os.environ.get("APP_BASE_URL", "http://localhost:5173")`）／使用箇所: `backend/routers/admin_comp.py`の`_build_invite_link()`（招待リンク組み立て） | 管理画面からの無償アカウント招待（2026-08-31導入・2026-09-04本番検証済み機能）が送るメールのリンク本体は `{APP_BASE_URL}/invite?t=<hashed_token>` の形で組み立てられるが、この`APP_BASE_URL`という環境変数が `backend/.env.example` にも `.claude/agents/infra-ops.md`「守ること」節にも一度も記載が無い（grepで確認、両方ヒット0件）。未設定時のフォールバック値は`http://localhost:5173`という開発用の値 | Renderのサービス再作成・環境変数の棚卸し・envの誤コピー等でこの変数が万一外れても、コードはエラーを出さずに黙って`http://localhost:5173/invite?t=...`をメール本文へ埋め込む。SMTP送信自体は成功する（`notifications.send_invite()`は宛先到達可否しか見ない）ため、アプリ側にもメール送信結果にも異常の兆候が一切出ない。まさに2026-09-02に発覚した`CLOUDMERSIVE_API_KEY`未設定インシデント（重要な本番env変数がインフラのチェックリストに載っておらず、本番未設定のまま気づかれなかった）と同じ型のリスク | 次に無償招待を実際に使う顧客が「アカウントを有効化する」ボタン・予備URLどちらを押しても`localhost:5173`（本人のPCではまず何も応答しない、または全くの無関係な別サービスが動いていればそちらに繋がる）に到達し、招待が機能しないことに誰も気づかないまま顧客対応の問い合わせを受けて初めて発覚する可能性がある | 候補（未昇格）。**なお2026-09-04の本番検証では`app.ureshiru.com/invite?t=...`形式で正しく届くことを確認済みのため、現時点でRender側の設定自体に問題は無い**。問題は「その設定が正しいことをチェックリストで担保する仕組みが無い」点 |
| 2026-09-14 | `backend/notifications.py:30`（`_INVITE_EXPIRES_LABEL_DEFAULT = "1時間"`）／`backend/routers/admin_comp.py:321,447`（`send_invite()`呼び出し。`expires_label`引数を渡していないため常にこの既定値を使う） | 招待メール本文（テキスト・HTML両方）は「有効期限: 発行から1時間」と顧客に断定的に案内するが、この「1時間」は2026-08-31の実装時点で「仮の既定値」として置かれた値で、Supabaseの`generate_link`（type=invite）が実際に発行するトークンの有効期限と突き合わせて実測されたことが一度も無い。2026-09-01・2026-09-04の本番検証はリンクが機能すること（`verifyOtp`成功・パスワード設定・ログイン到達）は確認したが、いずれも招待発行後すぐにクリックする形で行われており、「表示どおり1時間で本当に切れるか／実際はもっと短い・長いか」は検証対象に入っていない | 表示中の「1時間」がSupabase側の実際のトークンTTLと異なっていた場合、2通りの実害があり得る: (a) 実際はもっと短く切れる場合、案内を信じて1時間以内に開いたのにリンクが無効というクレーム・問い合わせが発生する（招待メール自体は無償提供のオンボーディング用なので、ここでの離脱は機会損失に直結する）。(b) 実際はもっと長い場合は実害は無いが、案内自体が不正確なまま顧客に送られ続ける | 顧客が「1時間以内に」と案内されたにもかかわらずリンクが早期に切れて開けない、という問い合わせが来て初めて実際のTTLとの食い違いに気づく可能性がある。招待は無償提供の入口なので、ここでの躓きは印象を損ないやすい | 候補（未昇格）。実測には管理者アカウントでの実招待発行→時間を空けてからのクリック、という本番操作が必要なため、Claude Codeセッション単独では検証できない |
| 2026-09-14 | `CLAUDE.md:153`（「⚠️ 招待メール文面は2箇所にある」行） | この行は招待メールの文面生成関数を`backend/mail_templates.py::invite_body()`という単一の名前で参照しているが、この関数は現存しない。2026-09-04の招待メールHTML化（軍令帳「招待メールをHTML化・自社ドメインリンク化」、CLAUDE.md該当行）で`invite_body_text()`と`invite_body_html()`の2関数に分割された（`grep -n "^def " backend/mail_templates.py`で確認、`invite_body(`という名前の関数・呼び出しはリポジトリ全体で`docs/cowork_shiji_comp_invite_ku3_frontend_2026-08-31.md`（8/31時点の指示書・履歴的スナップショット）にのみ残存）。あわせて同じ行の見出し「文面は2箇所にある」も、9/4のHTML化以降は実質3箇所（`invite_body_text()`・`invite_body_html()`・フロント`AdminAccounts.tsx`の`buildInvitePreview()`）になっている。なお`buildInvitePreview()`自体は現行の`invite_body_text()`と文面が一言一句一致しており（実際に突き合わせ済み）、機能上のバグは無い——ずれているのは説明文だけ | CLAUDE.mdルール6（「ファイルパスを参照として書くときは、その時点で実在を確認する」）が警告する「記述と実装の食い違い」の実例。この行を読んだ人が`invite_body()`という関数を探しても見つからず混乱する、または「2箇所直せば揃う」という前提で`invite_body_html()`側の文面変更を見落とす（プレビューは`invite_body_text()`のみを移植しているため、HTML版だけを変えてもプレビューとの食い違いには気づけるが、そもそも「2箇所」という数え方がHTML版の存在を意識させない） | 次に招待メールの文面を触るセッションが、この行の「2箇所直せば揃う」という説明を鵜呑みにして`invite_body_html()`の存在を見落とし、テキスト版とプレビューだけ直してHTML版を古いまま残す可能性がある。実害は小さい（現状は3箇所とも実際には揃っている）が、記述の不正確さ自体が次の見落としの種になる | 候補（未昇格）。修正時はCLAUDE.md:153の関数名を`invite_body_text()`/`invite_body_html()`に直し、「2箇所」を「3箇所（テキスト・HTML・フロントのプレビュー）」に更新する程度の軽微な文言修正で足りる |
| 2026-09-15 | `CLAUDE.md:366`（「アーキテクチャ」節、`src/App.tsx` がルーティング…の行）／実装: `frontend/src/App.tsx:40-53`（`<Routes>`） | CLAUDE.mdはApp.tsxのルーティングを「`/`=Dashboard, `/gap`=GapAnalysis, `/products`=ProductKPI, `/import`=DataImport, `/targets`=TargetSetting, `/rpp`=RppAnalysis」の6ルートとして説明しているが、実際の`<Routes>`は11ルートあり、`/master`（MasterSettings）・`/master/categories`（CategoryMaster）・`/billing`（Billing）・`/reports`（Reports）・`/account`（AccountSettings）・`/admin`（AdminAccounts）の6ルートが記載から漏れている | この行は2026-08-04頃（マスタ設計統一・請求/アカウント画面・管理者閲覧機能の導入前）に書かれたまま、その後の複数の機能追加（商品マスタ4タブ化、カテゴリマスタ独立ページ、Billing、Reports、AccountSettings、AdminAccounts）を反映していない。ちょうど半数のルートが説明から欠落している状態 | 次にこのCLAUDE.md該当行だけを見てアプリの画面構成を把握しようとするセッションが、`/master`系・`/billing`・`/reports`・`/account`・`/admin`の存在に気づかず、既存画面と重複する新規ページを作ってしまう、または管理者閲覧機能（`/admin`）のような重要な画面の存在を見落とす可能性がある | 候補（未昇格）。修正時は現行11ルートを列挙し直すだけの軽微な文言修正で足りる |
| 2026-09-15 | `CLAUDE.md:367`（「法的文書はこのアプリ内に持たない」行、LP参照先`https://ureshiru.vercel.app`）／実装: `frontend/src/lib/links.ts:18`（`LP_BASE_URL = 'https://ureshiru.com'`） | CLAUDE.mdは法的文書の正がLP側にあるとし、そのURLを「`https://ureshiru.vercel.app` = Stripeに『ビジネスウェブサイト』として登録しているサイト」と説明しているが、実際の`links.ts`の`LP_BASE_URL`は`https://ureshiru.com`（独自ドメイン）になっている。しかもCLAUDE.md自身の別行（「旧Render(Oregon)・旧Vercelプロジェクトの削除」2026-07-29→完了2026-08-24）に、旧Vercelプロジェクト（`ureshiru`/`ureshiru-lp`）は2026-08-24に削除済みと明記されている——`ureshiru.vercel.app`というURL自体、もう存在しないVercelプロジェクトの可能性が高い | この行の末尾は「独自ドメインへ移行する場合は `lib/links.ts` の `LP_BASE_URL` の1行を直す」と将来形で書かれているが、実際にはこの移行は既に完了済みで、参照先の記述だけが移行前のまま取り残されている。Stripeの「ビジネスウェブサイト」登録がこの独自ドメイン移行時に追随して更新されたかどうかもこの記述からは読み取れない（コードからは確認不能・オーナー確認が必要な論点） | 次にこの行を読んだセッションが、既に死んでいる可能性のある`ureshiru.vercel.app`というURLを正しいLPだと信じて参照したり、Stripe審査担当者が実際に見るサイトを誤認したりする可能性がある。またStripe側の「ビジネスウェブサイト」登録が独自ドメインへ更新されているかどうかが未確認のまま放置される | 候補（未昇格）。コード側の文言修正（`ureshiru.com`への訂正）に加え、Stripe側の「ビジネスウェブサイト」登録が実際に独自ドメインを指しているかのオーナー確認が必要 |
| 2026-09-16 | `CLAUDE.md:321`（「アーキテクチャ」節、`backend/routers/` 配下のルーター一覧）／実装: `backend/routers/`（`__init__.py`を除く21ファイル） | CLAUDE.mdはルーター一覧を「dashboard / import_csv / targets / gap_analysis / products / actions / evaluation / export / account」の9本として説明しているが、実際の`backend/routers/`には21ファイルあり、`admin.py`・`admin_comp.py`・`billing.py`・`consulting.py`・`costs.py`・`feedback.py`・`inventory.py`・`item_targets.py`・`masters.py`・`recommendations.py`・`revenue_plan.py`・`rpp_diagnosis.py`の12本が記載から漏れている（`ls backend/routers/`で確認） | 隣接する`CLAUDE.md:367`のApp.tsxルーティング一覧陳腐化（2026-09-15候補、同じ種類の記述漏れ）と同型。この行は初期の9ルーター構成のまま、その後の機能追加（管理者閲覧・comp管理・請求・コンサル問い合わせ・原価・在庫・アイテム別目標・マスタ設計統一・商品提案・売上予算プラン・RPP診断）を反映していない。半分以上のルーターが説明から欠落している状態 | 次にこの行だけでバックエンドの機能構成を把握しようとするセッションが、`admin`/`admin_comp`（管理者閲覧・無償提供付与という強い権限を持つ機能）や`billing`（課金）の存在に気づかず、重複する新規ルーターを作ってしまう、または重要な既存機能を見落とす可能性がある | 候補（未昇格）。修正時は現行21ルーターを列挙し直すだけの軽微な文言修正で足りる |
| 2026-09-16 | `CLAUDE.md:320`（「アーキテクチャ」節、`main.py`がエントリの行、「全例外は`global_exception_handler`が`{"detail": str(exc)}`のJSON 500に変換」の記述）／実装: `backend/main.py:200-206`（`global_exception_handler`） | CLAUDE.mdはグローバル例外ハンドラが常に`{"detail": str(exc)}`（例外メッセージそのもの）を返すと説明しているが、実装は`env EXPOSE_ERROR_DETAIL`が真のときだけ`str(exc)`を返し、既定（未設定）では`"サーバーエラーが発生しました"`という定型文を返す。この`EXPOSE_ERROR_DETAIL`ゲート自体は`c4ea5b7`（2026-08-03）で導入され、CLAUDE.md自身の別行（120行目、Coworkルーチン4週分の突き合わせ）に「同コミットの『中』（`global_exception_handler`の`EXPOSE_ERROR_DETAIL`対応）...実装は健在...訂正のみで対応済みとする」と記載があるが、その「訂正」は120行目の履歴記述のみで、320行目のアーキテクチャ説明自体は更新されないまま残っていた | 本番（既定でEXPOSE_ERROR_DETAIL未設定）では実際には例外メッセージが漏れない設計になっており、これはCORS制限や`.env.example`のコメント（「1のときだけ500エラーのレスポンスに例外の詳細文を含める。本番では未設定にすること」）と整合する意図的なセキュリティ設計だが、320行目の記述だけを読むと本番でも生の例外文字列がクライアントに返る（＝内部実装の詳細が漏れる）ように誤解できる | 次にこの行を読んだセッションが、500エラーのレスポンスに機微な内部情報（DBエラー文言等）が常に含まれると誤解し、無用にエラーメッセージを丸めるような不要な対策を追加してしまう、あるいは逆に「既定で安全」という前提を知らずに`EXPOSE_ERROR_DETAIL`を本番で誤って有効化するリスクの重大さを過小評価する可能性がある | 候補（未昇格）。修正時は「既定は定型文、`EXPOSE_ERROR_DETAIL=1`のときだけ`str(exc)`」という条件を1文加える程度の軽微な文言修正で足りる |
| 2026-09-17 | `backend/.env.example`（全45行）／実装: `backend/malware.py:1-30`（`CLOUDMERSIVE_API_KEY`・`MALWARE_SCAN_FAIL_CLOSED`・`MALWARE_SCAN_TIMEOUT`） | アップロードCSV/ZIPのマルウェアスキャン機能を制御する3つの環境変数（`CLOUDMERSIVE_API_KEY`＝キー未設定ならno-op、`MALWARE_SCAN_FAIL_CLOSED`＝API障害時にフェイルオープンかフェイルクローズか、`MALWARE_SCAN_TIMEOUT`＝タイムアウト秒）が、`backend/.env.example`に一切登場しない（grepでヒット0件）。`.claude/agents/infra-ops.md`にも記載なし。一方、同ファイルには`EXEMPT_TEST_EMAILS`・`TRIAL_WITHOUT_CARD`・`ADMIN_USER_ID`・`EXPOSE_ERROR_DETAIL`・`ENABLE_DOCS`という性質の似た「旗（フラグ）系」env変数がすべて、区切りコメント＋注意書き＋コメントアウトされた設定例つきのブロックとして書かれている | CLAUDE.md本文には2026-08-22にマスタCRUD規約として`scan_bytes()`呼び出しルールが明記され、`GET /api/security-status`の`malware_scan_active`でキー設定有無を確認できる仕組みも整ったが、`.env.example`という「ローカル開発時に何を設定すべきか一覧できる唯一のファイル」側にはこの機能の存在自体が反映されないまま残っている。まさに2026-09-02に発覚した「本番`CLOUDMERSIVE_API_KEY`が未設定のままマルウェアスキャンが全エンドポイントで無効だった」インシデント（CLAUDE.md該当行に記録済み）の再発防止という観点で、この種のチェックリスト漏れが今もなお残っている状態 | 次に新しい環境を立ち上げる人・本番env変数の棚卸しをする人が`.env.example`だけを見て「設定すべきenvの一覧」と信じた場合、マルウェアスキャンの3変数の存在に気づけない。一度実際に本番で数週間気づかれず無効だった実績のある種類の見落としが、ドキュメント側の空白として今も残っている | 候補（未昇格）。修正時は既存の`ADMIN_USER_ID`ブロック等と同じ体裁（区切りコメント＋注意書き＋コメントアウト例）を1ブロック追加する程度で足りる |
| 2026-09-18 | `backend/routers/account.py:37-38`（`_ALL_MODELS`）／`backend/routers/account.py:78-127`（`delete_account`）／実装: `backend/models.py`（`UserScopedMixin`継承20テーブル） | 退会API（`DELETE /api/account`）の説明「本人の全データと Supabase ユーザーを削除する」（`account.py:79`のdocstring。CLAUDE.md:335にも同趣旨の記載あり）に反し、実際に削除されるのは`_ALL_MODELS`に列挙された7テーブル（`RppWeekly`/`RppSales`/`MonthlyItemSales`/`MonthlyAnalysis`/`Target`/`ActionCheck`/`InventoryStatus`）のみ。`UserScopedMixin`を継承する20テーブルのうち、`Shop`（店舗設定）・`Product`（商品マスタ）・`ProductCategory`（カテゴリマスタ）・`ProductCost`（原価率）・`ItemTarget`（アイテム別目標）・`GenreBenchmark`（ジャンルベンチマーク）・`ActionLog`（施策実施記録）・`RppActionCheck`（RPP診断チェック状態）・`Subscription`（契約情報）・`AdminViewSession`（管理者閲覧監査ログ）の10テーブルが削除対象から漏れている（`comp_grants`は同関数内で別途revoke処理済み、`consulting_inquiries`・`feedbacks`は2026-07-28決定により営業記録として意図的に保持——この2つとcomp_grantsは正当な除外） | `_ALL_MODELS`は2026-07-30の退会機能導入時点のテーブル構成のまま固定されており、その後追加されたテーブル（Shop/Product/ItemTarget/GenreBenchmark/ActionLog/RppActionCheck/AdminViewSessionはいずれも2026-08以降の追加）が反映されていない。CLAUDE.md「新しいモデルを追加するときの必須確認」チェックリストは`UserScopedMixin`継承・`sample_data.py`更新は明記しているが、「`account.py`の`_ALL_MODELS`への追加」は項目に無く、同種の見落としが今後も再生産されうる構造 | 退会したユーザーの商品マスタ・原価率・店舗設定・アイテム別目標・施策実施履歴・RPP診断状態が、Supabase側のログインアカウントが消えた後もDBに残り続ける。`AccountSettings.tsx`の案内文言「アカウントと登録済みの全データ（RPP実績・月次売上・目標設定など）を完全に削除します」を信じて退会した利用者の、商品名・原価率を含む事業データが削除されないまま残存する。プライバシー・データ最小化の観点で説明と実装が食い違っている | 候補（未昇格） |
| 2026-09-18 | `backend/routers/costs.py:154-159`（`import_costs`） | 原価率CSV一括登録エンドポイントに、アップロードファイルのサイズ上限チェックが無い（`await file.read()`の直後は`scan_bytes()`〈2026-09-02追加〉のみで、サイズ判定が無い） | CLAUDE.md記載の2026-08-24セキュリティ指摘バックログ②「新設CSVインポート3本（カテゴリ・目標・アイテム別目標）にファイルサイズ上限が無い」は、同じ欠陥を持つ商品マスタCSVインポートには「既存パターンの踏襲」として触れているが、`costs.py`の`import_costs`（原価率CSV一括登録）はこのバックログのどの一覧にも挙げられていない。実装を確認すると同一の欠陥（サイズチェック無し）を持つ5件目のエンドポイントである | 次にCSVアップロード系の棚卸し・対応をする人が「バックログの3本＋商品マスタ」だけを対象と考え、この5件目を見落として対応が漏れる。実害は他の4件と同水準で低い（既存パターンの踏襲、`_paid`ガード配下のため悪用インセンティブは低いと考えられる）が、対応時にバックログの一覧が不完全なまま処理される | 候補（未昇格） |
| 2026-09-17 | `backend/migrations.py:25-57`（`_USER_SCOPED_TABLES`辞書）／実装: `backend/models.py:102`（`RppActionCheck.__tablename__ = "rpp_action_checks"`）・`backend/models.py:216`（`ActionLog.__tablename__ = "action_logs"`） | `_USER_SCOPED_TABLES`辞書は18テーブルを登録しているが、`UserScopedMixin`を継承する20モデルのうち`action_logs`（施策実施記録・学習ループ。`backend/routers/recommendations.py`・`backend/learning.py`で能動的に使用中）と`rpp_action_checks`（RPP診断パネルのアクションチェック状態。`backend/routers/rpp_diagnosis.py`で使用中）の2テーブルだけがこの辞書に無い | この辞書は`_add_user_id_columns()`（既存DBへのuser_id列・インデックスの冪等追加）と`_assign_legacy_data()`（env `LEGACY_DATA_USER_ID`が設定されたときの既存NULL行への一括割当）が反復する対象そのもの。この2テーブルは登録漏れのため、これらの起動時マイグレーションの対象外になっている。なお`_enforce_rls_pg()`（RLS強制）は`pg_tables`を独立に全走査するため無関係＝RLS自体は両テーブルとも保護されている（`GET /api/security-status`のunprotectedが空であることからも裏付けられる）。影響が生じるのは「user_id列が無い状態でテーブルが先に存在していた既存DB」に限られる、という条件付きのリスク | 何らかの理由でこの2テーブルにuser_id列が欠けた既存データベース（例: 将来のDB移行・レプリカ復元・手動での中間状態）に対して起動時マイグレーションを走らせても、この2テーブルだけuser_id列・インデックスが自動追加されない。またオーナーが`LEGACY_DATA_USER_ID`を使って過去データをどこかのユーザーへ割り当てる操作をした場合も、この2テーブルの既存行だけ割当対象から漏れる。CLAUDE.md冒頭が最優先事項として掲げる「新しいモデルを追加するときの必須確認」チェックリストが、この辞書への登録を明示的な項目として挙げていないことも一因と考えられる | 候補（未昇格）。修正時は`_USER_SCOPED_TABLES`に`"action_logs": []`・`"rpp_action_checks": []`（どちらもユニーク制約なし）を1行ずつ追加する程度で足りる。あわせてCLAUDE.mdの「新しいモデルを追加するときの必須確認」節に「`_USER_SCOPED_TABLES`への登録」を項目として明記すると同種の見落としの再発を防げる |
| 2026-09-21 | `.claude/agents/qa.md:13,22` | qa.md は「本番はGET系（`/api/health`、`/api/security-status`、`/api/billing/diagnose`）の確認に留める」「課金設定 \| `GET /api/billing/diagnose` が `ok: true`」と、この3エンドポイントを同格の無認証GETチェックであるかのように並記しているが、`backend/routers/billing.py:305-306`のとおり`/api/billing/diagnose`だけは2026-09-03から`admin_guard.require_admin`必須（`ADMIN_USER_ID`のJWTが無ければ403）になっている | `/api/health`・`/api/security-status`は無認証で叩けるが`/api/billing/diagnose`だけ管理者トークンが要る、という非対称性にqa.mdが一切触れていない。2026-09-03の変更はCLAUDE.md本文には記録済みだが、実際にこのチェックリストを毎回参照するqaエージェントの指示書側には反映されていない | qaが一般ユーザー・compユーザーのセッション（管理者トークンなし）で本番検証を行うと、`/api/billing/diagnose`だけ403になり「課金設定\|ok:true」の項目が原因不明の失敗として扱われる、または管理者トークンの用意という追加手順が必要なことに気づかないまま検証が漏れる | 候補（未昇格）。修正時は該当2行に「管理者トークンが必要」の一言を添える程度の軽微な文言修正で足りる |
| 2026-09-21 | `docs/office_map.html:269`（`// 最終更新: 2026-09-07`コメント） | ファイル冒頭の「最終更新」コメントが2026-09-07のまま書き換えられていないが、`git log --format='%h %ad' -- docs/office_map.html`で確認すると実際にはその後も09-08・09-11・09-14・09-15・09-16・09-17・09-18・09-21（今日）と繰り返し更新され続けており、直近の09-21更新でも`STATUS`の「守り」欄（`docs/office_map.html:276`）は今日付に正しく更新されている | このコメントは「データはここだけ直せば更新できる」という編集者向け案内の一部で、ファイルの鮮度を示す目印として機能することが期待されている。中身（STATUS・QUESTS）は正しく更新され続けているのに、鮮度表示のコメントだけが2週間以上前の日付で止まっている | 次にこのファイルを編集するセッションが「最終更新 9/7」を信じて軍令帳が長期放置されていると誤認する、あるいは逆にこのコメント自体の更新を今後も忘れ続け、ドキュメントの信頼性が少しずつ低下する | 候補（未昇格）。修正時は当該コメントを実際の更新日に合わせるだけの軽微な修正で足りる（更新のたびに直すか、いっそコメント自体を削除するかは判断が要る） |
| 2026-09-21 | `.claude/skills/security-check/SKILL.md:14` | 「リポジトリ側`security/index.md`とプロジェクトdocs側`security_index.md`の二重管理問題（2026-08-24 指摘）は、どちらが正か決まるまで両方に同じ内容を書く」という運用指示が残っているが、この指示を実行するツール（プロジェクトdocs側=旧Coworkルーチンの`project_write`相当）自体が対話型・夜勤問わずどのClaude Codeセッションにも存在せず、実際に「両方へ同じ内容を書く」が行われたことは一度もない（`security/security_check_2026-08-24.md:131`・`2026-08-31.md:206`はこの論点に言及しているのみで、プロジェクトdocs側へ書き込んだ実績ではない） | CLAUDE.md本文（「sellerhub側に残る旧『security/index.md』マスター文書の扱い」行）は既に2026-08-24〜09-02にかけてこの論点を「Coworkルーチン時代の遺物」「repo側`security/index.md`が正、sellerhub側への遡及反映は不要と決定」「対応するツール・セッションが無いため未実施」と整理・決着させているのに、週次で必ず参照されるSKILL.md側の指示だけが「決まるまで両方に書く」という2026-08-24時点の古い前提のまま取り残されている | 次に`/security-check`を実行するセッションがCLAUDE.mdの決着済みの記述を読まずSKILL.mdだけを参照した場合、存在しないツールを探して混乱する、あるいは「両方に書く」という未達成かつ実行不能な指示が指示書に残り続けることで、指示・CLAUDE.mdの結論・実際の運用実態の3つが噛み合わないまま放置される | 候補（未昇格）。修正時はSKILL.md:14を「CLAUDE.md記載のとおりrepo側`security/index.md`が正、プロジェクトdocs側への反映は不要」という決着済みの内容に書き換える程度の軽微な修正で足りる |
| 2026-09-22 | `CLAUDE.md:320`（「アーキテクチャ」節、`main.py`がエントリの行、「マイグレーションツールなし＝モデル変更は手動でDB削除 or ALTER」の記述）／実装: `backend/migrations.py:62-109`（`_EXTRA_COLUMNS`辞書・`_add_extra_columns()`） | CLAUDE.mdはモデル変更について「マイグレーションツールなし＝モデル変更は手動でDB削除 or ALTER」と言い切っているが、実際には`_add_extra_columns()`という確立した自動ALTER機構が存在する。docstringどおり「モデルに後から追加した通常カラムを、無ければ ALTER TABLE で足す（冪等）」もので、`_EXTRA_COLUMNS`辞書にテーブル名・列名・DDLを1行足すだけで起動時に自動反映される。2026-08-01（アクション提案ロジックの`products`拡張4列）を皮切りに、`shops`（売上予算プラン）・`targets`（月次予算手動補正）・`comp_grants`（招待メール機能）と、少なくとも4つの機能追加でこの仕組みが使われてきた実績がある（`git log`ベースで確認可能） | この行はCLAUDE.md:320の同じ一文の中で、隣接する別の条項（`global_exception_handler`のEXPOSE_ERROR_DETAILゲート＝2026-09-16候補、CORSの`ALLOW_ORIGINS`拡張＝2026-09-08候補）と同様に、実装が用意した「自動化された経路」の存在に触れないまま古い前提を言い切っている。次にモデルへ新しい列を1つ追加したいだけの実装者が、この行を鵜呑みにして「DB削除して作り直すか、手でALTER文を書くしかない」と思い込み、確立した`_EXTRA_COLUMNS`パターンに気づかずローカルDBを毎回消す、または本番運用中のDBに直接手でALTERを打つ（本来`_EXTRA_COLUMNS`に登録すれば起動時に安全に冪等適用される作業） | 候補（未昇格）。修正時は「新しいテーブルはcreate_allで自動生成、既存テーブルへの列追加は`migrations.py`の`_EXTRA_COLUMNS`に登録すれば起動時に自動ALTERされる」という一文に置き換える程度の軽微な修正で足りる |
| 2026-09-23 | `.claude/agents/infra-ops.md:27` | インフラ運用部エージェントの「守ること」節が「旧 Render(Oregon)・旧 Vercel の削除は未実施（申し送り参照）。削除時は Supabase Redirect URLs の旧URLも整理」と記載しているが、CLAUDE.md本文（60行目「旧Render(Oregon)・旧Vercelプロジェクトの削除」行）はこの作業を**完了（2026-08-24）**と明記している（Render側は元々旧Oregonサービスが存在せず作業不要、Vercel側は`ureshiru`/`ureshiru-lp`の2プロジェクトを同日削除済み） | このエージェントファイル自身が「申し送り参照」と書いているのに、参照先（CLAUDE.md）が既に完了と記録している事実と正反対の「未実施」を書いたまま1ヶ月以上取り残されている。CLAUDE.mdルール6（参照は実在チェックしてから書く）と同種の「記述と実装（この場合は他ドキュメントの記録）の食い違い」の実例 | 次にオーナーからインフラ相談を受けたinfra-opsエージェントが、既に完了済みの旧Render/旧Vercel削除を「まだやることが残っている」と誤案内し、存在しないタスクの手順書を作ってしまう、あるいはSupabase Redirect URLsの整理が本当に未了なのか確認漏れなのかの区別がつかなくなる | 候補（未昇格） |
| 2026-09-23 | `.claude/agents/infra-ops.md:26` | 「守ること」節が「`EXEMPT_TEST_EMAILS` は本番 `demo@ureshiru.com`。`TRIAL_WITHOUT_CARD` は本番未設定を維持」と記載しているが、CLAUDE.md本文（管理画面からの無償アカウント招待の行、区切り5完了記録）によれば**2026-08-31にオーナーがRenderの`EXEMPT_TEST_EMAILS`を変数ごと削除**し、demo@ureshiru.comは`/admin`画面からのcomp（無償提供）管理へ移行済み。`docs/unyou_exempt_test_emails.md`の台帳にも「2026-08-31（`/admin`のcomp管理へ移行のため。`EXEMPT_TEST_EMAILS`変数自体を削除）」と削除日が明記されている | この行は2026-07-30〜08-06頃（comp管理機能導入前）の本番状態を記述したまま、2026-08-31の運用移行を反映していない。現在の本番には`EXEMPT_TEST_EMAILS`という環境変数自体が存在しない | 次にinfra-opsエージェントが本番env変数の棚卸しやトラブルシュートを行うとき、存在しない`EXEMPT_TEST_EMAILS`を探して「設定漏れ」と誤診断する、またはdemo@ureshiru.comの無償扱いの根拠を古い仕組み（env変数）に求めてしまい、正しい確認先（`/admin`画面のcomp一覧）に気づかない | 候補（未昇格） |
| 2026-09-23 | `DEPLOY.md:7`（冒頭「全体像」節の前）／`DEPLOY.md:76`（ステップ4） | 同一ファイル内で自己矛盾している。7行目は「現状は『ログインで保護＋データは全員共有』。将来、各テーブルに `user_id` を足せばユーザー別データに拡張できる設計（認証基盤は導入済み）」と、マルチテナント分離が**未実装であるかのように**書いているが、76行目（ステップ4）は「**データはユーザーごとに分離**されている（マルチテナント）。レビュアーは自分のアカウントでサインアップし、自分でデータを取込む（他ユーザーのデータは見えない）」と、実装済みであることを前提に書いている | CLAUDE.md「マルチテナント（ユーザー別データ分離）— `backend/tenancy.py`」節が示すとおり、`UserScopedMixin`によるユーザー単位データ分離は2026-07に実装され現在も本番で稼働中の基盤機能。7行目はこの機能が導入される前（当初のデプロイ手順書執筆時点）の記述がそのまま取り残されたものと考えられ、同じファイルの76行目だけが後から実態に合わせて更新された結果、ファイル内で矛盾が生じている | このファイルを冒頭から読んだ人（新しい環境を立ち上げようとする人、またはこのファイルでプロダクトの現状把握をしようとするセッション）が「データは全員共有＝マルチテナントはまだ将来の話」という誤った前提を最初に刷り込まれ、既に実装済みのマルチテナント機能（RLS・`tenancy.py`）の存在を疑ってかかる、または重複して同じ機能を作ろうとする可能性がある | 候補（未昇格） |
| 2026-09-24 | `.claude/agents/kpi-analyst.md:14` | 「アクセス軸は2つ。`site_uu`（`MonthlyItemSales.cvr`、ページ全体）と `rpp_click`（`RppSales.cvr_720` 等、広告経由）」と書かれているが、`backend/access_definitions.py:6-7`（このファイル自体が「アクセス軸の定義は単一の真実」と位置づけられている）によれば実際は `site_uu` は `MonthlyItemSales.access_uu` 由来（`cvr` はそこから `cv/access_uu` で導出される値であって軸の出典ではない）、`rpp_click` は `RppWeekly.ct` 由来（`RppSales.cvr_720` ではない）。`backend/routers/gap_analysis.py` を実際に確認すると `RppWeekly` のみをimportしており `RppSales` は一切使っていないことも確認済み | CLAUDE.md「データモデル」節が明記するとおり `RppWeekly`（集計用・GAP分析/ダッシュボードが使う）と `RppSales`（生データ保管・`/api/import/rpp/*`専用）は役割が分離されており、「1インポートで両テーブルに書く設計のため...新たな集計を足すと二重計上になりうる。役割分離を守ること」という明示的な警告がある。kpi-analystはこのファイル自身が「アクセス軸の定義を担当」する番人と位置づけられているのに、自分の知識ファイルが軸の出典テーブルを取り違えている | 「この数字おかしくない？」と聞かれたkpi-analystが、GAP分析やダッシュボードの `rpp_click` の値がおかしいと相談された際に、実際には無関係な `RppSales`（生データ保管専用テーブル）を調査対象にしてしまい、本来見るべき `RppWeekly` 側の実装を素通りする、あるいは二重計上リスクのある `RppSales` の値を誤って引き合いに出して説明してしまう可能性がある | 候補（未昇格） |
| 2026-09-24 | `CLAUDE.md:373,377`（「LP（別リポジトリ）」の2箇所）／`CLAUDE.md:83`（同種の表現） | 価格3箇所整合・プラン名3箇所整合の節で、LPを繰り返し「LP（別リポジトリ）」と表記しているが、実際には `lp/` はこのリポジトリ直下に実在するディレクトリで、別リポジトリではない（`ls lp/`で`index.html`/`terms.html`/`privacy.html`等が実在することを確認済み）。CLAUDE.md自身の申し送り台帳にも、`lp/index.html`等をこのリポジトリの通常のPR（例: PR #37, #56, #81, #82）で直接編集・mainマージしてきた実績が多数記録されている。`.claude/agents/infra-ops.md:14`の「LP \| Cloudflare Pages `ureshiru-lp`（Root=`lp`、pushで自動デプロイ）」という記述とも整合する（同じリポジトリへのpushでCloudflare Pagesが`lp`をRootにビルドする構成であり、別リポジトリを想定していない） | 「別リポジトリ」という言葉は、過去にLPが本当に別のVercelプロジェクト（`ureshiru`/`ureshiru-lp`、2026-08-24削除済み）としてホストされていた時代の名残と考えられるが、今はGitレベルでも同一リポジトリの`lp/`ディレクトリとして管理されている。CLAUDE.md:367の「LP参照先が`ureshiru.vercel.app`のまま」という既存候補（2026-09-15発見）とは別の論点——URLの古さではなく「別リポジトリかどうか」という構造の誤認そのものを指摘するもの | 次に価格改定・プラン名変更に着手するセッションが「LPは別リポジトリだから別途cloneしてPRを作る必要がある」と誤解し、実際には同じリポジトリの`lp/`を直接編集すれば済む作業を不必要に複雑化する、または「別リポジトリのはずなのに見当たらない」と混乱して作業が止まる可能性がある | 候補（未昇格） |
| 2026-09-24 | `lp/privacy.html:64`（第6条・安全管理措置の委託先一覧） | 個人情報保護法に基づく委託先開示で「ウェブサイトのホスティング：Vercel Inc.」と記載されているが、CLAUDE.md本文（「旧Render(Oregon)・旧Vercelプロジェクトの削除」行、2026-08-24完了）のとおり、LP（`ureshiru.com`）は同日付でVercelから削除され、現在は`.claude/agents/infra-ops.md:14`にあるとおりCloudflare Pages（`ureshiru-lp`）でホストされている。1行下の63行目「アプリケーションのホスティング：Render, Inc.」は現状と一致しており、64行目だけが移行前の事業者名のまま取り残されている | `.claude/agents/legal-finance.md`の担当チェック項目自体が「委託先一覧（privacy.html）が実態と合っているか（Vercel → Cloudflare Pages に移行済みなら要更新）」と明記しており、この巡回はその項目がまだ未対応のまま残っていることを裏取りしたもの。個人情報保護法上、委託先（第三者提供に準ずる取扱いの委託先）の開示は実態と一致している必要があり、既に契約関係の無い事業者名を残すのは不正確な開示にあたる | 現に存在しない委託先（Vercel）を開示し続け、実際の委託先（Cloudflare）を開示していない状態が公開ページに残り続ける。利用者や今後の監査・専門家確認（CLAUDE.md記載の「専門家確認の未了事項」）でこの食い違いを指摘された場合、他の法的文書の信頼性にも疑念を持たれかねない | 候補（未昇格） |

## 却下済み

却下された候補をここに移す。理由を添えて残し、削除しない（同じ指摘が再度出てくるのを防ぐ台帳）。

| 発見日 | 却下日 | 場所 | 何が | 却下理由 |
|---|---|---|---|---|
| 2026-08-25 | 2026-08-25 | `lp/index.html`・`lp/llms.txt` の「1店舗・1アカウント」表記 と `lp/terms.html` 第2条2項 | LP「1店舗・1アカウント」表記と規約第2条2項「同一店舗内の複数ユーザー利用は可能です」の字面の重なり | 意味上は矛盾なし（アカウント数の話と、そのアカウントの店舗内共有許諾の話でレイヤーが違う）。共有利用は規約上可能だが、LPでは訴求も明示もしない方針でオーナー判断済み（CLAUDE.md参照）。**再提案不要** |
