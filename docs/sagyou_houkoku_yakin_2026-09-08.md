# 夜勤 作業報告（2026-09-08）

## 前段: 普請（急務対応）

`docs/office_map.html` の `QUESTS` を確認したところ、`stamp:"wait"`（急務）の項目は0件だった
（`kouho`＝候補7件、`later`＝後日2件のみ）。よって普請は対象なし。

STATUS（領国・評定・守り・軍費）も実態と照合した:
- 評定＝「議案なし」… GitHub上のopen PRを確認したところ0件で、記載どおり
- 守り＝「9/7検分 高0」… `security/security_check_2026-09-07.md` が最新で、記載どおり
- 軍費＝「月 約$33」… `.claude/agents/infra-ops.md` のコスト内訳と一致（前回同様、実請求額そのものはダッシュボードアクセス権限が無いため未確認）

いずれも実態と一致しており、更新は不要だった。

## 後段: 巡回（発見専用フェーズ）

コード・ドキュメントを見回り、新規候補を1件発見した（3件枠のうち1件。無理に3件へ埋めていない）。

### 発見: CORS拡張用env `ALLOW_ORIGINS` がどこにも文書化されていない

- **場所**: `backend/main.py:136-139,143`
- **何が**: `_default_origins`（`localhost:5173`/`3000`固定）に加え、環境変数 `ALLOW_ORIGINS`
  （カンマ区切り）で許可オリジンを追加できる仕組みが実装されている。コード自身のコメントは
  「同一サービスでフロントを配信する構成では本来CORS不要だが、フロントを別ドメインに置く場合に
  備え」た拡張だと説明している。この変数名・存在自体が `CLAUDE.md`・`backend/.env.example`・
  `.claude/agents/infra-ops.md` のいずれにも一度も出てこない（絶対パスでのgrep、3箇所ともヒット0件）
- **なぜ問題か**: CLAUDE.md「アーキテクチャ」節は「CORSは `localhost:5173`/`3000` のみ許可」と
  言い切っており、この拡張経路の存在を示唆していない
- **放置するとどうなるか**: 現在の本番構成（backendが `frontend/dist` を同一オリジンで配信）では
  CORS自体が実質不要なため実害は小さいと考えられるが、将来モール横展開やフロント分離
  （VISION.mdの将来像）でこの変数が必要になったとき、存在を知る手がかりがコードのコメント以外
  どこにも無い。優先度は低いと判断した

`docs/office_map.html` QUESTS（`stamp:"kouho"`）と `docs/gunrei_kouho.md` の候補一覧に追記済み。
既存の急務・候補・却下済み・CLAUDE.md記載との重複がないことを確認したうえで追加した。
候補札の昇格・却下は行っていない（オーナー判断待ち）。

## 次にやること

- 上記候補の要否をオーナーが判断（昇格 or 却下）
- 急務（`stamp:"wait"`）が積まれ次第、次回夜勤の普請で対応
